@page "/admin"
@attribute [Authorize(Policy = PartiesUiAuthorization.AdminPolicy)]

@*
    Story 1.3 — transitional Admin area entry point. Establishes the /admin route + the per-area
    [Authorize(Policy = Admin)] pattern (enforced by AuthorizeRouteView in Routes.razor). Replaced by
    the embedded AdminPortal pages (/admin/parties*) in Story 2.1.
*@

<h1>Administration</h1>
<p>The Parties administration area is coming soon.</p>

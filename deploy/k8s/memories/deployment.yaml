---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: memories
  labels:
    app: memories
  annotations:
    dapr.io/enabled: 'true'
    dapr.io/config: accesscontrol-memories
    dapr.io/app-id: memories
    dapr.io/app-port: '8080'
    dapr.io/enable-api-logging: 'true'
spec:
  minReadySeconds: 60
  replicas: 1
  selector:
    matchLabels:
      app: memories
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: memories
      annotations:
        dapr.io/enabled: 'true'
        dapr.io/config: accesscontrol-memories
        dapr.io/app-id: memories
        dapr.io/app-port: '8080'
        dapr.io/enable-api-logging: 'true'
    spec:
      imagePullSecrets:
      - name: zot-pull-secret
      containers:
      - name: memories
        image: registry.hexalith.com/memories:0.0.0-preview.0.353
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8080
        envFrom:
        - configMapRef:
            name: memories-env
      terminationGracePeriodSeconds: 180
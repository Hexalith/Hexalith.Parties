---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eventstore
  labels:
    app: eventstore
  annotations:
    dapr.io/enabled: "true"
    dapr.io/config: accesscontrol
    dapr.io/app-id: eventstore
    dapr.io/enable-api-logging: 'true'
spec:
  minReadySeconds: 60
  replicas: 1
  selector:
    matchLabels:
      app: eventstore
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: eventstore
      annotations:
        dapr.io/app-port: '8080'
        dapr.io/enabled: "true"
        dapr.io/config: accesscontrol
        dapr.io/app-id: eventstore
        dapr.io/enable-api-logging: 'true'
    spec:
      imagePullSecrets:
      - name: zot-pull-secret
      containers:
      - name: eventstore
        image: registry.hexalith.com/eventstore:0.0.0-preview.0.474
        imagePullPolicy: IfNotPresent
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          timeoutSeconds: 10
          periodSeconds: 10
          failureThreshold: 30
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 120
          timeoutSeconds: 10
          periodSeconds: 10
          failureThreshold: 30
        ports:
        - name: http
          containerPort: 8080
        - name: https
          containerPort: 8443
        env:
        - name: Authentication__JwtBearer__SigningKey
          valueFrom:
            secretKeyRef:
              name: hexalith-jwt-signing
              key: value
        envFrom:
        - configMapRef:
            name: eventstore-env
      terminationGracePeriodSeconds: 180
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: parties-mcp
  labels:
    app: parties-mcp
spec:
  minReadySeconds: 60
  replicas: 1
  selector:
    matchLabels:
      app: parties-mcp
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: parties-mcp
    spec:
      containers:
      - name: parties-mcp
        image: registry.hexalith.com/parties-mcp:0.1.1-preview.0.3
        imagePullPolicy: IfNotPresent
        readinessProbe:
          httpGet:
            path: /health
            port: http
        livenessProbe:
          httpGet:
            path: /health
            port: http
        ports:
        - name: http
          containerPort: 8080
        - name: https
          containerPort: 8443
        envFrom:
        - configMapRef:
            name: parties-mcp-env
      imagePullSecrets:
      - name: zot-pull-secret
      terminationGracePeriodSeconds: 180

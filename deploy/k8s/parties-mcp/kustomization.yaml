resources:
- deployment.yaml
- service.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: parties-mcp-env
  literals:
    - OTEL_DOTNET_EXPERIMENTAL_OTLP_RETRY=in_memory
    - ASPNETCORE_FORWARDEDHEADERS_ENABLED=true
    - HTTP_PORTS=8080
    - EVENTSTORE_HTTP=http://eventstore:8080
    - services__eventstore__http__0=http://eventstore:8080
    - PARTIES_HTTP=http://parties:8080
    - services__parties__http__0=http://parties:8080
    - Parties__Mcp__EventStoreGatewayBaseUrl=http://eventstore:8080
    - Authentication__JwtBearer__Authority=http://keycloak.hexalith-parties.svc.cluster.local:8080/realms/hexalith
    - Authentication__JwtBearer__Issuer=http://keycloak.hexalith-parties.svc.cluster.local:8080/realms/hexalith
    - Authentication__JwtBearer__Audience=hexalith-parties-mcp
    - Authentication__JwtBearer__TokenValidationParameters__ValidAudiences__0=hexalith-parties-mcp
    - Authentication__JwtBearer__TokenValidationParameters__ValidAudiences__1=hexalith-eventstore
    - Authentication__JwtBearer__RequireHttpsMetadata=false
    - ASPNETCORE_URLS=http://+:8080;


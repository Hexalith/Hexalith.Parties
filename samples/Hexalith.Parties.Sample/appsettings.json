{
  "Parties": {
    "_BaseUrlComment": "EventStore HTTPS gateway URL. Replace the placeholder port with the eventstore HTTPS port shown in the Aspire dashboard on first run.",
    "BaseUrl": "https://localhost:7050",
    "Tenant": "tenant-a"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}

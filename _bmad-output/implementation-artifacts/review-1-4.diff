diff --git a/src/Hexalith.Parties/Validation/AddContactChannelValidator.cs b/src/Hexalith.Parties/Validation/AddContactChannelValidator.cs
index af3e96b..c69f7e3 100644
--- a/src/Hexalith.Parties/Validation/AddContactChannelValidator.cs
+++ b/src/Hexalith.Parties/Validation/AddContactChannelValidator.cs
@@ -14,8 +14,11 @@ public sealed class AddContactChannelValidator : AbstractValidator<AddContactCha
             .WithMessage("PartyId must be a valid GUID.");
 
         RuleFor(x => x.ContactChannelId)
+            .Cascade(CascadeMode.Stop)
             .NotEmpty()
-            .WithMessage("ContactChannelId is required.");
+            .WithMessage("ContactChannelId is required.")
+            .Must(id => Guid.TryParse(id, out _))
+            .WithMessage("ContactChannelId must be a valid GUID.");
 
         RuleFor(x => x.Type)
             .IsInEnum()
diff --git a/src/Hexalith.Parties/Validation/RemoveContactChannelValidator.cs b/src/Hexalith.Parties/Validation/RemoveContactChannelValidator.cs
index 31a733e..cf4aa34 100644
--- a/src/Hexalith.Parties/Validation/RemoveContactChannelValidator.cs
+++ b/src/Hexalith.Parties/Validation/RemoveContactChannelValidator.cs
@@ -14,7 +14,10 @@ public sealed class RemoveContactChannelValidator : AbstractValidator<RemoveCont
             .WithMessage("PartyId must be a valid GUID.");
 
         RuleFor(x => x.ContactChannelId)
+            .Cascade(CascadeMode.Stop)
             .NotEmpty()
-            .WithMessage("ContactChannelId is required.");
+            .WithMessage("ContactChannelId is required.")
+            .Must(id => Guid.TryParse(id, out _))
+            .WithMessage("ContactChannelId must be a valid GUID.");
     }
 }
diff --git a/src/Hexalith.Parties/Validation/UpdateContactChannelValidator.cs b/src/Hexalith.Parties/Validation/UpdateContactChannelValidator.cs
index fd2d74f..1ce879e 100644
--- a/src/Hexalith.Parties/Validation/UpdateContactChannelValidator.cs
+++ b/src/Hexalith.Parties/Validation/UpdateContactChannelValidator.cs
@@ -14,7 +14,10 @@ public sealed class UpdateContactChannelValidator : AbstractValidator<UpdateCont
             .WithMessage("PartyId must be a valid GUID.");
 
         RuleFor(x => x.ContactChannelId)
+            .Cascade(CascadeMode.Stop)
             .NotEmpty()
-            .WithMessage("ContactChannelId is required.");
+            .WithMessage("ContactChannelId is required.")
+            .Must(id => Guid.TryParse(id, out _))
+            .WithMessage("ContactChannelId must be a valid GUID.");
     }
 }
diff --git a/tests/Hexalith.Parties.Tests/Validation/ContactChannelValidatorTests.cs b/tests/Hexalith.Parties.Tests/Validation/ContactChannelValidatorTests.cs
new file mode 100644
index 0000000..a28f710
--- /dev/null
+++ b/tests/Hexalith.Parties.Tests/Validation/ContactChannelValidatorTests.cs
@@ -0,0 +1,61 @@
+using Hexalith.Parties.Contracts.Commands;
+using Hexalith.Parties.Contracts.ValueObjects;
+using Hexalith.Parties.Validation;
+
+using Shouldly;
+
+namespace Hexalith.Parties.Tests.Validation;
+
+public sealed class ContactChannelValidatorTests
+{
+    [Fact]
+    public void AddContactChannel_InvalidContactChannelId_ReturnsValidationFailure()
+    {
+        AddContactChannel command = new()
+        {
+            PartyId = Guid.NewGuid().ToString("D"),
+            ContactChannelId = "not-a-guid",
+            Type = ContactChannelType.Email,
+            Value = "person@example.com",
+        };
+
+        var result = new AddContactChannelValidator().Validate(command);
+
+        result.IsValid.ShouldBeFalse();
+        result.Errors.Select(e => e.PropertyName).ShouldContain(nameof(AddContactChannel.ContactChannelId));
+        result.Errors.Select(e => e.ErrorMessage).ShouldContain("ContactChannelId must be a valid GUID.");
+    }
+
+    [Fact]
+    public void UpdateContactChannel_InvalidContactChannelId_ReturnsValidationFailure()
+    {
+        UpdateContactChannel command = new()
+        {
+            PartyId = Guid.NewGuid().ToString("D"),
+            ContactChannelId = "not-a-guid",
+            Value = "person@example.com",
+        };
+
+        var result = new UpdateContactChannelValidator().Validate(command);
+
+        result.IsValid.ShouldBeFalse();
+        result.Errors.Select(e => e.PropertyName).ShouldContain(nameof(UpdateContactChannel.ContactChannelId));
+        result.Errors.Select(e => e.ErrorMessage).ShouldContain("ContactChannelId must be a valid GUID.");
+    }
+
+    [Fact]
+    public void RemoveContactChannel_InvalidContactChannelId_ReturnsValidationFailure()
+    {
+        RemoveContactChannel command = new()
+        {
+            PartyId = Guid.NewGuid().ToString("D"),
+            ContactChannelId = "not-a-guid",
+        };
+
+        var result = new RemoveContactChannelValidator().Validate(command);
+
+        result.IsValid.ShouldBeFalse();
+        result.Errors.Select(e => e.PropertyName).ShouldContain(nameof(RemoveContactChannel.ContactChannelId));
+        result.Errors.Select(e => e.ErrorMessage).ShouldContain("ContactChannelId must be a valid GUID.");
+    }
+}

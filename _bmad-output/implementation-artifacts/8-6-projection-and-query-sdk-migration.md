---
story_key: 8-6-projection-and-query-sdk-migration
story_id: "8.6"
epic: "8"
created: 2026-07-08T18:23:46+02:00
status: in-progress
source_status: backlog
target_status: review
baseline_commit: 2c4a7af
review_loop_iteration: 1
eventstore_pin_at_creation: 0f428d0c914f2151aab15bb262f956a9630041dc
---

# Story 8.6: Projection and query SDK migration

Status: in-progress

<!-- Group 1+2+3+4 re-review patches applied 2026-08-03; Group 5 re-review 2026-08-03; prior open Group 2 logging/LastWrite items and Group 6 remain. -->

## Story

As a maintainer,
I want projection and query mechanics to use EventStore SDK abstractions,
so that Parties keeps only domain folds, query semantics, and tenant guardrails.

## Acceptance Criteria

1. Given the Story 8.3 matrix row "EventStore projection/query SDK" remains `needs-additive-api`, when this story starts, then source migration halts as blocked until the row records owner-approved additive parity or explicit already-available proof for G3 read-model erasure hooks, G10 index batching, G6 freshness mapping, duplicate/out-of-order replay, full-rebuild verification, cursor scope compatibility, and the current `references/Hexalith.EventStore` pin.
2. Given the prerequisite row is proven, when projection migration is implemented, then `PartyDetailProjectionHandler` and `PartyIndexProjectionHandler` run through EventStore SDK `IDomainProjectionHandler` implementations while preserving replay-from-zero, duplicate/out-of-order idempotency, erased-party behavior, payload redaction handling, and current detail/index read-model shapes.
3. Given SDK read-model writes replace local actor state writes, when detail and index projections persist state, then writes use `IReadModelStore` and `ReadModelWritePolicy`, preserve index batching or an approved equivalent, and exclude erased parties from the index.
4. Given query migration is implemented, when detail, index, search, export, processing-record, erasure-status, and erasure-certificate reads execute, then they run through EventStore SDK `IDomainQueryHandler` paths with the same tenant guardrails, payload validation, freshness metadata, GDPR semantics, and no-leak diagnostics as the current Dapr query actors.
5. Given pagination cursors exist before migration, when index/search pages are requested through the SDK query path, then `IQueryCursorCodec` preserves cursor purpose/scope compatibility or rejects only malformed/expired cursors with bounded existing error semantics.
6. Given full rebuild is available through the SDK path, when a rebuild is executed for detail and index, then rebuilt read models are verified against aggregate replay before any Dapr projection actor, companion sequence key, rebuild service, platform adapter, or query fallback is deleted.
7. Given stale or unavailable projection state (or a store-outage degraded read), when a read is requested, then the response returns last-known data when available and `ProjectionFreshnessMetadata` using the SDK-native statuses `Current`/`Stale`/`Unavailable` only; store-outage last-known reads report `Stale` with `Metadata.IsDegraded = true` and the store-unavailable warning code. Staleness never throws through the query contract. (`Rebuilding`/`Degraded`/`LocalOnly` are retained on the enum for wire compatibility but are not produced by the SDK query path after the 2026-08-03 AC7 renegotiation.)
8. Given the SDK path proves parity, when cleanup is performed, then Dapr projection actors, the Parties rebuild service, Epic 7 projection platform adapters, and projection/query `catch (NotImplementedException)` control flow are removed.
9. Given the host boundary from Story 8.5, when this migration completes, then the Parties host still exposes no public API, command/query ingress remains through EventStore gateway and DAPR `/process`, and the DAPR ACL remains deny-by-default, admitting only the `eventstore` appId on the exact internal SDK command/query/projection/rebuild/discovery routes documented in `accesscontrol.parties.yaml` — this is not a public/external ACL expansion (see the frozen spec's Design Notes).
10. Given Epic 8 is post-MVP maintenance, when this story is completed, then documentation and sprint status state this is platform cleanup only and no new PRD functional requirement coverage was delivered.

## Tasks / Subtasks

- [x] Establish the hard prerequisite gate before editing production source (AC: 1, 9, 10)
  - [x] Read `_bmad-output/implementation-artifacts/story-8-3-platform-api-prerequisite-matrix.md` and confirm the "EventStore projection/query SDK" row is no longer `needs-additive-api`.
  - [x] Record the current `references/Hexalith.EventStore` commit in the matrix before migration; story creation observed `0f428d0c914f2151aab15bb262f956a9630041dc`, which is newer than the Story 8.5 proof pin.
  - [x] If owner proof is not locally available, mark this story blocked in the Dev Agent Record and sprint status; do not edit production source.
  - [x] Preserve root-declared submodule discipline: do not run recursive submodule commands or initialize nested submodules.

- [x] Build the projection/query SDK verification harness before deleting rollback paths (AC: 2, 3, 4, 5, 6, 7)
  - [x] Add SDK-path tests for replay-from-zero, duplicate delivery, out-of-order delivery, stale/degraded fallback, erased-party exclusion, cursor compatibility, and processing-record reads.
  - [x] Include a full rebuild versus aggregate replay verification for both detail and index.
  - [x] Cover GDPR Art.20 export, Art.30 `ProcessingActivityRecord[]`, erasure status, erasure certificate, and no-PII diagnostics.
  - [x] Record all parity commands and results in `_bmad-output/implementation-artifacts/tests/test-summary.md`.

- [x] Rebind projection folds to EventStore SDK abstractions (AC: 2, 3, 6, 7)
  - [x] Update `src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs` and `PartyIndexProjectionHandler.cs` through `IDomainProjectionHandler` implementations or thin adapters that keep the existing pure fold behavior.
  - [x] Use `IReadModelStore` and `ReadModelWritePolicy` for detail and index writes; preserve set-based idempotency, single-key index semantics, batched index behavior or approved equivalent, and erased-party removal.
  - [x] Preserve typed protected/redacted payload behavior and fail-closed event-type handling; do not introduce `Type.GetType` or broad type activation.

- [x] Move query paths to EventStore SDK query handlers (AC: 4, 5, 7, 9)
  - [x] Replace `PartyDetailProjectionQueryActor` and `PartyIndexProjectionQueryActor` semantics with `IDomainQueryHandler` implementations for `PartyDetail`, `GetParty`, `ExportPartyData`, `GetProcessingRecords`, `GetErasureStatus`, `GetErasureCertificate`, `PartyIndex`, and `PartySearch`.
  - [x] Preserve tenant route validation, strict JSON payload validation, page/page-size guards, party-type allowlist, ISO timestamp offset requirements, and current malformed request outcomes.
  - [x] Route pagination through `IQueryCursorCodec` only after cursor purpose/scope compatibility and DAPR key-ring persistence are proven.
  - [x] Preserve optional Memories indexing/search behavior as best effort; do not make Memories required for local search.

- [x] Replace host/service registrations without widening ingress (AC: 8, 9)
  - [x] Update `src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs` to register SDK projection/query handlers, read-model store usage, write policy, and cursor codec.
  - [x] Keep existing projection actors, rebuild service, adapters, and health checks registered until parity and rebuild evidence are recorded.
  - [x] Verify `src/Hexalith.Parties/Program.cs` keeps the Story 8.5 SDK host shape and that the DAPR ACL remains deny-by-default, admitting only `eventstore` on the exact internal SDK POST routes in `accesscontrol.parties.yaml` (not a public/external ACL expansion; see AC9 and the frozen spec Design Notes).

- [x] Delete local mechanics only after parity is green (AC: 6, 8)
  - [x] Remove Dapr projection actors and actor interfaces only after the SDK path proves detail/index parity.
  - [x] Remove `ProjectionRebuildService`, rebuild checkpoint types, projection platform adapters, adapter mode, and local freshness adapter types only after SDK rebuild and rollback evidence is recorded.
  - [x] Remove projection/query `catch (NotImplementedException)` fallback flow from `PartyDetailProjectionActorExtensions` and `PartyIndexProjectionQueryActor`.
  - [x] Replace or remove `ProjectionActorsHealthCheck` only with equivalent SDK/read-model health evidence; projection degradation must remain non-readiness-blocking.

- [x] Validate and close evidence (AC: 6, 8, 10)
  - [x] Run focused and broad build/test lanes listed in Testing and Validation Guidance.
  - [x] Run `git diff --check` and `bash scripts/check-no-warning-override.sh`.
  - [x] Update `_bmad-output/implementation-artifacts/tests/test-summary.md` with parity, rebuild, and rollback evidence.
  - [x] Move sprint status through workflow states without rewriting unrelated comments or statuses.

### Review Findings

_Chunked adversarial review (bmad-code-review) against a diff split into 6 file groups (the story's own File List omitted several genuinely-8.6 files — expanded from ~53 to 77 relevant files after cross-checking against `git diff`). Groups 1-3 were reviewed in an earlier session (findings below, fixes applied and committed). Groups 4-6 were reviewed in a follow-up session. **2026-08-03: fresh Group 1–4 re-reviews**, then **Group 5 re-review** (test changes: SDK query/DI tests + 9 deleted legacy actor/rebuild/health suites) against `2c4a7af` including uncommitted WT patches on `PartySdkQueryHandlerTests` / `ProjectionPlatformAdapterTests` — findings below._

**Diagnostic-logging patch re-review (2026-08-05: code review of the operator-logging patch, 6 files) findings:**

_Adversarial review of the 2026-08-05 diagnostic-logging patch (the one that closed the Group 2 "All diagnostic logging... was deleted" and "genuinely corrupt live event is now indistinguishable..." findings above). Found 8 patch-worthy issues in that patch's own diff, all fixed in this pass. 4 additional findings from the same review were triaged as defer (not this-pass patches) and logged separately under `deferred-work.md`'s 2026-08-05 entries — not repeated here._

- [x] [Review][Patch] `PartyIndexSdkProjectionHandler.FoldCore`'s failure branch (`GetDeliveryFailureReason` short-circuit) returned before the logged `DeserializeNew` call ever ran, so the shared Party index projection never emitted `NonJsonEventDropped`/`UnknownEventTypeDropped`/`AmbiguousEventTypeDropped` for an unresolvable/ambiguous/non-JSON event across all three `FoldCore` callers (`ProjectAsync`'s pre-check, its persisting call, and `AccumulateAsync`) — exactly the case where operators most need visibility, since the delivery is stuck `Retryable` [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs] — Fixed 2026-08-05: `FoldCore`'s failure branch now re-walks once with `PartySdkProjectionFold.HasUnresolvedNewEvent(request.Events, lastSequence, logger)` when `failureReason` is `UnresolvedOrUnsupportedEventReason`, then returns immediately, so the main fold loop never re-walks the same events (no duplicate). `ProjectAsync`'s pre-check now also passes its logger (safe: it returns immediately on failure, so the persisting call's own logged invocation never runs for the same delivery). Added `IndexHandler_MixedKnownAndUnresolvedDeliveryLogsUnresolvedDiagnosticAsync` and `SharedIndexRebuild_UnresolvedEventLogsUnresolvedDiagnosticAsync`.
- [x] [Review][Patch] `DeserializeNew` silently dropped a resolved, non-redacted event whose payload deserialized without throwing but yielded `null` (e.g. a literal JSON `null` body): `advance` evaluates `false` and the `WholePayloadRedactedEventDropped` guard only covered the `advance == true` branch, so nothing logged at all, contradicting the method's own "every drop/skip emits a distinct diagnostic" doc comment [src/Hexalith.Parties.Projections/Handlers/PartySdkProjectionFold.cs] — Fixed 2026-08-05: added a new `NullPayloadEventDropped` Warning-level diagnostic for the `payload is null && !advance` case, distinct from the whole-payload-redacted (`advance == true`) case. Added `DeserializeNew_ResolvedTypeDecodesToNullWithoutRedaction_LogsNullPayloadEventDroppedWarning`.
- [x] [Review][Patch] Duplicate logging under optimistic-concurrency retry: `PartyIndexSdkProjectionHandler.ProjectAsync`'s `UpdateAsync` persisting callback and `PartyDetailSdkProjectionHandler.RecordUnresolvedProcessingAsync`'s retry callback both re-invoke the logged fold on every optimistic-concurrency retry attempt (`ReadModelWritePolicy.UpdateAsync` calls the callback once per attempt, up to `DefaultMaxAttempts`), so one delivery under writer contention could log the same drop multiple times — contradicting the "exactly once per delivery" claim in `PartyProcessingActivityFold.Fold`'s XML doc [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs; PartyDetailSdkProjectionHandler.cs] — Fixed 2026-08-05: both callbacks now pass the real logger only on the first attempt (`loggedThisDelivery` flag captured by the closure) and `null` on every retry, so a drop logs at most once per delivery regardless of retry count. Accepted trade-off, documented in code: a drop that only becomes visible on a later retry's differently-checkpointed snapshot could be missed rather than logged twice — preferred over repeat-spam under contention.
- [x] [Review][Patch] GDPR/PII risk: `Log.RedactedEventDropped`/`Log.PayloadDeserializationFailed` passed the raw `Exception` positionally to `ILogger`, but most logging sinks render `exception.ToString()` (including `.Message`) into the emitted log text, and direct testing confirmed a syntax-level `JsonException` (as opposed to a type-conversion `JsonException`) embeds a fragment of the offending raw JSON bytes/token in `.Message` (e.g. `"'n' is an invalid start of a property name..."` echoes a byte from the input) — a corrupt or adversarial event payload could leak a fragment of its raw bytes into an operator-visible log line, contradicting this same patch's own "AC7" no-payload-in-logs contract [src/Hexalith.Parties.Projections/Handlers/PartySdkProjectionFold.cs] — Fixed 2026-08-05: both methods no longer pass the `Exception` object to `ILogger`; only `exception.GetType().Name` travels as a structured `{ExceptionType}` field, matching the same audited pattern already used by `PartySdkQueryService.LogQueryFailed`. Extended the existing `ShouldNotContain("tenant-a")`/`ShouldNotContain("party-1")` privacy assertions to `DeserializeNew_CorruptLiveEvent_...` and `DeserializeNew_RedactedTailDecodeFailure_...`, and both now also assert `record.Exception.ShouldBeNull()`.
- [x] [Review][Patch] The new `AmbiguousEventTypeDropped` branch had zero test coverage — none of the original six `DeserializeNew_*` tests exercised it [tests/Hexalith.Parties.Projections.Tests/Handlers/PartySdkProjectionHandlerTests.cs] — Fixed 2026-08-05: confirmed by direct enumeration that the real `Hexalith.Parties.Contracts` assembly has 0 short-name collisions among its 44 event-payload types (matching `PartyEventTypeResolverTests`'s own prior dismissal of this as unreachable), so `DeserializeNew_AmbiguousShortEventTypeName_LogsAmbiguousEventTypeDroppedWarning` seeds `PartyEventTypeResolver`'s private resolution cache with a synthetic ambiguous outcome for a process-unique sentinel event-type name via reflection (`ConcurrentDictionary.GetOrAdd` returns an existing entry without invoking its factory), proving `DeserializeNew` calls `AmbiguousEventTypeDropped` rather than `UnknownEventTypeDropped` without adding any test-only type to the production Contracts assembly.
- [x] [Review][Patch] `PartyDetailSdkProjectionHandler.Fold`'s silent (no-logger) `DeserializeNew` call relies on an invariant — that `PartyProcessingActivityFold.Fold` always runs over the same events in the same call, so its logging covers both slots — documented only in `PartyProcessingActivityFold.Fold`'s XML doc, not at the `Fold` call sites that depend on it [src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs] — Fixed 2026-08-05: added an XML doc to `Fold` itself explaining the invariant and why it must not be broken, plus a one-line pointer comment at both call sites (`ProjectAsync`, `PrepareRebuildAsync`).
- [x] [Review][Patch] Misleading comment in `PartyIndexSdkProjectionHandler.ProjectAsync`'s idempotent-no-op branch: "the persisting FoldCore call below already reported any drops for it then" reads as if code later in the *same* method call still runs and covers the logging, when the idempotent branch returns immediately and the referenced `FoldCore` call is from a prior, separate dispatch attempt [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs] — Fixed 2026-08-05: reworded to state explicitly that the earlier logging happened on a prior, separate `ProjectAsync` dispatch, not later in this call.
- [x] [Review][Patch] `tests/Hexalith.Parties.Projections.Tests/Handlers/RecordingLogger.cs` was a near-verbatim copy of `tests/Hexalith.Parties.Tests/Gateway/RecordingLogger.cs`, duplicated rather than shared, even though both test projects already reference `Hexalith.Parties.Testing` — Fixed 2026-08-05: moved `RecordingLogger<T>` to `src/Hexalith.Parties.Testing/RecordingLogger.cs` (now `public`, the shared home), added `Microsoft.Extensions.Logging.Abstractions` as a `PackageReference` to `Hexalith.Parties.Testing.csproj` (the version is already centrally pinned at `10.0.10` in the imported `Hexalith.Builds` package-versions file, so no new `Directory.Packages.props` entry was needed), deleted both local copies, and updated the 3 consuming files (`PartySdkQueryHandlerTests.cs`, `PartyMemoryIndexEntrySearchIndexerTests.cs`, `PartySdkProjectionHandlerTests.cs`) to `using Hexalith.Parties.Testing;` instead.

_Verification: `Hexalith.Parties.Projections` and `Hexalith.Parties.Testing` build 0 warnings/0 errors; `Hexalith.Parties.Projections.Tests` builds 0 warnings/0 errors and passes 220/220 (up from 216/216 before this pass's 4 new tests). `Hexalith.Parties.Tests` builds 0 warnings/0 errors and passes 503/504 (the sole failure is the pre-existing, out-of-scope `PlatformApiPrerequisitesTests.Matrix_ValidationEvidenceCommandsAreReproducible` EventStore-pin-drift test). `Hexalith.Parties.Contracts.Tests`, `Hexalith.Parties.Security.Tests`, `Hexalith.Parties.Server.Tests`, and `Hexalith.Parties.Client.Tests` (all consumers of `Hexalith.Parties.Testing`) build 0 warnings/0 errors after the `RecordingLogger<T>` move. `git diff --check` and `bash scripts/check-no-warning-override.sh` both passed._

**Group 5 re-review (2026-08-03: test changes, 14 files) findings:**

_Prior open Group 5 cosmetic defer reconfirmed. Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor. Dismissed as noise for this chunk: Aging→Current freshness mapping (intentional AC7); rebuild coverage lives in `Hexalith.Parties.Projections.Tests` (out of this chunk); erasure `TrySaveAsync` payloads asserted in projection eraser tests; fenced-route/no-op test-double guard gaps; search/list erased filtering covered by the production providers and `BasicPartySearchProviderTests.BuildPagedList_*`; DI resolution covered by the retained host/integration tests; retry-helper cancellation/composite-key concerns mirror the production scheduler's validated identity contract. Cursor tamper/key-rotation mechanics are covered by EventStore's `QueryCursorCodecTests`; cursor v1 intentionally has no wall-clock expiry, while Parties retains handler-level paging and scope-binding coverage._

- [x] [Review][Decision] Spec Acceptance still requires dual-path actor-vs-SDK agreement after AC8 deleted the actor path — Resolved 2026-08-03: Administrator chose option 1 — renegotiate Spec Acceptance / story harness text to an SDK-only verification matrix and drop dual-path language.
- [x] [Review][Patch] Renegotiate Spec Acceptance and story harness to SDK-only verification (drop dual-path actor-vs-SDK) [spec-8-6-projection-and-query-sdk-migration.md; 8-6-projection-and-query-sdk-migration.md]
- [x] [Review][Patch] SDK query diagnostics violate the no-identifier/no-PII rule: warning/debug templates emit tenant, party, and correlation identifiers and warning paths retain the original exception, while all replacement tests inject `NullLogger` after the recording-logger suites were deleted [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:110-112,237-240,344-347,394-397,654-667; tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs:883-902]
- [x] [Review][Patch] Export `PersonalDataUnavailable` (blank DisplayName/SortName, non-erased) has no SDK handler test after actor suite deletion [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs; src/Hexalith.Parties/Queries/PartySdkQueryService.cs:81-94]
- [x] [Review][Patch] Accepted index filter contract is not exercised at `PartyIndexQueryHandler` boundary: non-null type/active/date filters and timestamps with explicit offsets remain helper-only coverage [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs:65-98,550-566; src/Hexalith.Parties/Queries/PartySdkQueryService.cs:244-276]
- [x] [Review][Patch] Accepted search contract is not exercised at `PartySearchQueryHandler` boundary: the happy path passes null type/active filters and never proves supported Lexical/DisplayName mode propagation [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs:583-605,632-700; src/Hexalith.Parties/Queries/PartySdkQueryService.cs:279-314]
- [x] [Review][Patch] Null erasure-certificate success + JSON-null payload unproven on SDK path [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs; PartySdkQueryService.GetErasureCertificateAsync]
- [x] [Review][Patch] Missing detail/index read model never asserts `ActorNotFoundInfrastructure` fail-closed outcome [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs; PartySdkQueryService.cs:69,333,383]
- [x] [Review][Patch] Search unknown-field rejection not covered on `PartySearchQueryHandler` (list path only) [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs]
- [x] [Review][Patch] `Unavailable` freshness status unproven on SDK query path after AC7 keeps it as a producer [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs; PartySdkQueryService.ToPartiesFreshness]
- [x] [Review][Patch] Direct `GetParty`/`PartyDetail` state matrix is incomplete: inactive inspectability and erased redacted-state/no-PII reads are unproven (export/erasure-status coverage is not equivalent) [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs]
- [x] [Review][Patch] Detail identity/tenant guardrail matrix is incomplete: AggregateId/EntityId mismatch and wrong-tenant-vs-absent observational indistinguishability are unproven after `TenantSafe*` deletion [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs:568-581,827-881; src/Hexalith.Parties/Queries/PartySdkQueryService.cs:637-643]
- [x] [Review][Patch] Index/search degraded last-known fallback has no cross-tenant isolation test; the retained cache eviction fact proves key separation but not that a tenant-B query can never receive tenant-A cached rows [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs:492-504; src/Hexalith.Parties/Queries/PartySdkQueryService.cs:351-388]
- [x] [Review][Patch] `PartyReadModelHealthCheck` timeout-specific branch remains untested: endpoint tests cover healthy and generic store failure, not cancellation-driven timeout returning non-readiness-blocking `Degraded` [src/Hexalith.Parties/HealthChecks/PartyReadModelHealthCheck.cs:24-54; tests/Hexalith.Parties.Tests/HealthChecks/HealthEndpointIntegrationTests.cs:39-79]
- [x] [Review][Patch] `ConfigurableCommandRouter` still keys status writes on `CorrelationId` while `DirectPartiesCommandRouter` uses `MessageId` [tests/Hexalith.Parties.Tests/Gateway/EventStoreGatewayRoutingTests.cs:978]
- [x] [Review][Patch] `InMemoryPartyKeyRetryScheduler` shares `EncryptionPipelineIntegrationTests.cs` (one-type-per-file) and no test asserts mark/clear/is-pending transitions [tests/Hexalith.Parties.IntegrationTests/Security/EncryptionPipelineIntegrationTests.cs:419-445]
- [x] [Review][Patch] Stale comment still cites deleted `PartyDetailProjectionQueryActorTests` / `PartyIndexProjectionQueryActorTests` for query-layer tenant isolation [tests/Hexalith.Parties.Tests/HealthChecks/ProjectionFreshnessAndDegradationTests.cs:109-113]

_Fixed 2026-08-03. Verification: `Hexalith.Parties.Tests` and `Hexalith.Parties.IntegrationTests` Debug builds passed with 0 warnings/errors; focused query, health, gateway, and retry-scheduler tests passed 53/53. Targeted `git diff --check` passed for every Group 5 patch file._
- [x] [Review][Defer] Prior Group 5 cosmetic defer remains open (stringly DI absence checks; health "all components" naming; partial Ada→Synthetic rename; undocumented MessageId status-key change) — deferred, pre-existing
- [x] [Review][Defer] `TestCursorCodec` private double instead of production DI codec; collapsed index invalid-payload theory; six indistinguishable `<factory-registered>` hosted-service exclusions — deferred, pre-existing — intentional test-isolation / factory-registration limits from the first Group 5 pass

**Group 4 re-review (2026-08-03: host wiring / ACL / registration + legacy deletion, 26 files) findings:**

_Prior open Group 4 items reconfirmed where still open (e2e assembly-scan wiring; cosmetic actor-shim naming). Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor. Dismissed as noise for this chunk: GDPR `ProjectionActorType` still works via `HandlerAwareQueryRouter` → `/query` (actor registration not required); health probe missing-key/`GetAsync` does not throw; write-path health probe was explicitly scoped to connectivity-only by the prior Administrator decision; LKG re-seed vs fail-closed eviction was decided in Group 3; `PlatformApiPrerequisitesTests` Story 8.5 AggregateActor assertions short-circuit because `spec-8-3` is `done`._

- [x] [Review][Decision] GDPR erasure still reports `Cleaned` for `aggregate-readable-state` and `snapshots` via no-op stubs after `AggregateActor` was unregistered [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:208-218] — Resolved 2026-08-03: Administrator chose option 1 — report `NotApplicable` (honest status; Parties no longer owns AggregateActor-backed stores after SDK cutover).
- [x] [Review][Patch] Report `NotApplicable` for `aggregate-readable-state` and `snapshots` erasure stubs [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:208-218] — Fixed 2026-08-03: both stubs return `NotApplicable`; erasure DI test asserts statuses + LKG eviction.
- [x] [Review][Patch] `DegradedResponseMiddleware` still keys off retired `projection-actors` while production emits `party-read-models` [src/Hexalith.Parties/Middleware/DegradedResponseMiddleware.cs:108-119; tests still inject the old key] — Fixed 2026-08-03: middleware + middleware/freshness tests adopt `party-read-models` only.
- [x] [Review][Patch] `PartyReadModelHealthCheck` rethrows `OperationCanceledException` on registration timeout instead of returning `Degraded`, and logs nothing on failure [src/Hexalith.Parties/HealthChecks/PartyReadModelHealthCheck.cs:37-47] — Fixed 2026-08-03: timeout → Degraded + structured warning logs.
- [x] [Review][Patch] ACL fitness guard omits `/replay-state` and `/project/rebuild/shared/v1` which the YAML already admits [tests/.../ArchitecturalFitnessTests.cs:317-337; accesscontrol.parties.yaml] — Fixed 2026-08-03: both routes pinned in fitness assertions.
- [x] [Review][Patch] Story task still claims DAPR ACL remains `/process` only; Rollback Plan still requires Story 8.5 `/process`-only ACL [8-6 story Tasks:65; Rollback Plan] — Fixed 2026-08-03: task + rollback wording aligned with AC9 exact-route allowlist.
- [x] [Review][Patch] Erasure DI cleanup test never seeds/asserts LKG cache eviction [tests/.../ProjectionPlatformAdapterTests.cs] — Fixed 2026-08-03: seeds cache, asserts eviction, asserts per-store statuses.
- [x] [Review][Patch] Register explicit `NoOp`/`PartyMemory` `IPartyIndexSearchIndexer` in DI and assert resolve when Memories is enabled [PartiesServiceCollectionExtensions.cs; tests] — Fixed 2026-08-03: NoOp when Memories off; resolve tests for both modes.
- [x] [Review][Patch] Operator docs still describe deleted projection actors/rebuild/`Parties:Projections`/`EventStore:CommandStatus` and omit `EventStore:Projections` SDK options [docs/development-guide.md; docs/architecture.md; docs/component-inventory.md] — Fixed 2026-08-03: docs updated to SDK projection/query path.
- [x] [Review][Patch] `LocalFuzzyPartySearchProvider` fuzzy-match gate changed in this host chunk with no paired tests [src/Hexalith.Parties/Search/LocalFuzzyPartySearchProvider.cs] — Fixed 2026-08-03: digit-token + multi-token full-phrase gate tests.
- [x] [Review][Patch] Remove unused `using Dapr.Actors` / `using Microsoft.Extensions.DependencyInjection.Extensions` if still unreferenced [PartiesServiceCollectionExtensions.cs] — Fixed 2026-08-03: unused usings removed.
- [x] [Review][Defer] Host `AddEventStoreDomainService(... PartyDetailProjectionHandler.Assembly)` remains source-text-only — deferred, pre-existing — same seeder-blocked e2e gap already open from the first Group 4 pass
- [x] [Review][Defer] ACL allow-list has no runtime Dapr enforcement check beyond YAML fitness — deferred, pre-existing — topology e2e gap; same class as the assembly-scan defer
- [x] [Review][Defer] Minor/cosmetic actor-shim naming / `EventStore:Projections` config-key reuse / undocumented csproj package rename — deferred, pre-existing — still open from the first Group 4 pass

**Group 3 re-review (2026-08-03: SDK query path, 14 files) findings:**

_Prior open Group 3 items reconfirmed where still open. Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor. Dismissed as noise for this chunk: DI/Evict wiring lives in Group 4 and is already present; actor `QueryAsync` deletion is intentional AC8 cleanup with constant/parser shims retained; `Success` wiping `WarningCodes` is currently a no-op because `ToQueryResponseMetadata` never sets them._

- [x] [Review][Decision] AC7 freshness mapping incomplete on the SDK query path — Resolved 2026-08-03: Administrator chose option 2 — renegotiate AC7/Spec Always to the three SDK-native statuses (`Current`/`Stale`/`Unavailable`); document store-outage as `Stale` + `Metadata.IsDegraded` + warning code; update `PartyPickerApiClient`/UI branches that still treat `Rebuilding`/`Degraded`/`LocalOnly` as live producers.
- [x] [Review][Patch] Renegotiate AC7 freshness to SDK-native statuses and align consumers [spec-8-6; 8-6 story AC7/Completion Notes; PartyPickerApiClient; UI/tests] — Fixed 2026-08-03: Spec Change Log + Always/AC7 wording; store-outage documented as `Stale`+`IsDegraded`; Picker collapses legacy `Rebuilding`/`Degraded`/`LocalOnly` into degraded UX; prior open freshness finding closed against the new contract.
- [x] [Review][Patch] Export fails closed when processing read throws without a last-known cache entry [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:70-104] — Fixed 2026-08-03: `ReadProcessingModelAsync` soft-fails to empty degraded records; export stays successful with empty `ProcessingRecords`.
- [x] [Review][Patch] Cursor scope uses unclamped payload `PageSize` when `Paging.PageSize` is null [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:222,261,482-512] — Fixed 2026-08-03: `ScopePageSize` clamps payload size the same way `TryResolvePaging` does when envelope page size is absent.
- [x] [Review][Patch] `TryValidateDetailEnvelope` only rejects `:` in partyId; address builder also rejects `|`, NUL, CR, LF [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:586-593; PartySdkReadModelAddresses] — Fixed 2026-08-03: reserved set aligned via `SearchValues`.
- [x] [Review][Patch] Null `query.Payload` NREs in `TryParseListPayload`/`TryParseSearchPayload` [src/Hexalith.Parties/Queries/PartyIndexProjectionQueryActor.cs:33,78] — Fixed 2026-08-03: null-or-empty guard returns false → `InvalidEnvelope`.
- [x] [Review][Patch] `GetErasureStatusAsync`/`GetErasureCertificateAsync` return bare `FromPayload` without `Success()` metadata [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:143-210] — Fixed 2026-08-03: both paths use `Success()`; detail-fallback status carries degraded/etag when applicable.
- [x] [Review][Patch] Query-path exceptions silently discarded [src/Hexalith.Parties/Queries/PartySdkQueryService.cs] — Fixed 2026-08-03: injected `ILogger<PartySdkQueryService>`; warning logs on query failures and processing soft-fail; debug log for cursor `failureReason`.
- [x] [Review][Patch] Null-coalesce `PartyIndexSdkReadModel.Entries` before `.Values` [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:231,270] — Fixed 2026-08-03: empty dictionary fallback.
- [x] [Review][Patch] Remaining Group 3 verification gaps [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs] — Fixed 2026-08-03: PartyDetail discriminator, search happy path, erased export shape, export/processing degraded soft-fail, cross-tenant last-known isolation, reserved party-id chars.
- [x] [Review][Defer] Cursor codec `failureReason` discarded via `out _` [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:TryResolvePaging] — deferred, pre-existing — client rejection already deterministic `InvalidCursor`; ties to logging patch above.
- [x] [Review][Defer] Out-of-range `PageSize` under `Paging` rejected as `InvalidCursor` even with no cursor [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:TryResolvePaging] — deferred, pre-existing — misdirects debugging only; prior list path used `InvalidEnvelope`.
- [x] [Review][Defer] Non-durable unbounded in-process last-known cache / no `ApplicationStopping` link / Actor-named constant bags / missing-detail vs empty-processing asymmetry [src/Hexalith.Parties/Queries/] — deferred, pre-existing — intentional shim/architecture trade-offs already flagged in the first Group 3 pass.

**Group 2 re-review (2026-08-03: SDK projection handlers & folds / search indexer, 6 files) findings:**

_Prior open Group 2 items reconfirmed and not re-listed: missing operator logging / `IsAmbiguousShortName` unused; corrupt-vs-redacted checkpoint-advance indistinguishability; rebuild `LastWrite` concurrency; unbounded processing-activity blob; cosmetic `LastModifiedAt` overwrite / sequential GetAsync / StoreName duplication; remaining fold/AlreadyCompleted/rebuild-vs-replay test gaps._

- [x] [Review][Patch] Same-id create after erasure: preserve the permanent-ID tombstone in detail and index [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs] — Superseded 2026-08-04 by the confirmed permanent Party-ID contract: a later `PartyCreated` for an erased identifier must not replace its tombstone.
- [x] [Review][Patch] Extend Memories search seam for erase + rebuild [IPartyIndexSearchIndexer; PartyIndexSdkProjectionHandler; PartySdkReadModelEraser; PartyMemoryIndexEntrySearchIndexer] — Fixed 2026-08-03: added `NotifyRemovedAsync`; index erase + eraser notify removal; `FinalizeAsync` best-effort reindexes rebuilt entries; adapter wraps cleanup/`IndexAsync` and never throws.
- [x] [Review][Patch] `erasedAtSequence` cleared on no-op payloads [PartyIndexSdkProjectionHandler] — Fixed 2026-08-03: only clear on successful Apply; drop checkpoint whenever entry stays null after an erasure in-batch. Added `IndexFold_NoOpAfterErasureStillDropsTerminalCheckpoint`.
- [x] [Review][Patch] Unresolved-only deliveries return Failed not AlreadyCompleted [PartyDetailSdkProjectionHandler; PartyIndexSdkProjectionHandler] — Fixed 2026-08-03: when watermarks unchanged but newer events exist, return `Failed("unresolved-or-unsupported-event")`. Added detail unresolved + duplicate AlreadyCompleted tests.
- [x] [Review][Patch] Index `ProjectAsync` AlreadyCompleted on duplicate delivery [PartyIndexSdkProjectionHandler] — Fixed 2026-08-03: compare prior/next aggregate entry+sequence before write. Added `IndexHandler_DuplicateDeliveryReturnsAlreadyCompletedAsync`.
- [x] [Review][Patch] Memories adapter never-throw + IndexAsync diagnostics [PartyMemoryIndexEntrySearchIndexer] — Fixed 2026-08-03: local try/catch, log soft failures/`FailureReason`, `NotifyRemovedAsync` via cleanup service. Added `PartyMemoryIndexEntrySearchIndexerTests` (4).
- [x] [Review][Patch] Art.30 records non-advancing events as Failed [PartyProcessingActivityFold] — Fixed 2026-08-03: dedupe-by-sequence Failed rows without advancing watermark. Added unresolved + append-across-deliveries tests.
- [x] [Review][Patch] Notify uses last mutating indexed event [PartyIndexSdkProjectionHandler] — Fixed 2026-08-03: `IndexFoldResult.LastIndexedEvent` drives notify metadata.
- [x] [Review][Patch] Broaden `DeserializeNew` deserialize failure catch [PartySdkProjectionFold] — Fixed 2026-08-03: catch `JsonException`/`ArgumentNullException`/`NotSupportedException`/`InvalidOperationException`.
- [x] [Review][Patch] Split `NoOpPartyIndexSearchIndexer` + detail-handler summary [Search/; PartyDetailSdkProjectionHandler] — Fixed 2026-08-03: NoOp in own file; summary mentions detail + processing slots.
- [x] [Review][Patch] New Group 2 verification gaps — Updated 2026-08-04: empty `ProjectAsync`, retryable search reconciliation, Memories adapter, AlreadyCompleted, unresolved/Art.30, and permanent post-erasure identifier coverage.
- [x] [Review][Defer] `s_caseIdMissingWarned` is an unbounded static `ConcurrentDictionary` [src/Hexalith.Parties/Search/PartyMemoryIndexEntrySearchIndexer.cs:23] — deferred, pre-existing pattern mirrored from the retired orchestrator; one entry per `(tenant, party)` for the process lifetime.
- [x] [Review][Defer] Index `ProjectionVersion` scheme (`global:N` / `{id}:{seq}` / keep-current) is undocumented on the Fold [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs:222-229] — deferred, pre-existing — documentation/remarks only; consumers already tolerate the string forms.
- [x] [Review][Defer] `GetOperationCategory` default returns a short event-type name rather than a stable category vocabulary [src/Hexalith.Parties.Projections/Handlers/PartyProcessingActivityFold.cs:598-609] — deferred, pre-existing — Art.30 category taxonomy design, not a regression from this chunk.

**Group 1 re-review (2026-08-03: models / options / eraser / resolver / csproj, 7 files) findings:**

- [x] [Review][Patch] Align eraser docs with empty-row semantics when no detail exists [src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs:20-24,50-53] — Resolved 2026-08-03 decision: Administrator chose option 2 — keep `Detail == null` when current/Detail is null (do not synthesize a tombstone). Authoritative erasure status remains `IPartyErasureRecordStore`; the detail read model only carries a tombstone when a prior detail row existed to redact. Fixed 2026-08-03: eraser comments and `Eraser_RedactDetail_NoExistingRowReturnsEmptyModel` now document empty-row semantics explicitly.
- [x] [Review][Patch] Detail erasure preserves a stale `LastSequenceNumber`, unlike processing/index checkpoints [src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs:50-53] — Confirmed by Blind Hunter and Acceptance Auditor. Fixed 2026-08-03: `RedactDetail` resets `LastSequenceNumber` to `long.MinValue` and clears `ProjectionVersion`. Extended eraser integration test and added `Eraser_RedactDetail_ExistingRowResetsSequenceWatermark`. `Hexalith.Parties.Projections.Tests` passes 176/176.
- [x] [Review][Patch] Eraser integration test never asserts `LastSequenceNumbers` cleanup [tests/Hexalith.Parties.Projections.Tests/Handlers/PartySdkProjectionHandlerTests.cs:382-383] — Confirmed by Verification Gap. Fixed 2026-08-03: asserts erased party absent from `LastSequenceNumbers` and unrelated party preserved.
- [x] [Review][Patch] `PartySdkReadModelAddresses.ValidateSegment` is weaker than EventStore `ProjectionKeySegments` and has no tests [src/Hexalith.Parties.Projections/Models/PartySdkReadModels.cs:69-76] — Confirmed by Blind Hunter and Verification Gap. Fixed 2026-08-03: reserved set now matches `ProjectionKeySegments` (`:`, `\0`, `|`, CR, LF) via `SearchValues`; added `PartySdkReadModelAddressesTests`.
- [x] [Review][Patch] Stale XML docs still say "projection actors" on `PartyEventTypeResolver` [src/Hexalith.Parties.Projections/Actors/PartyEventTypeResolver.cs:10-14] — Confirmed by Blind Hunter. Fixed 2026-08-03: summary now describes SDK projection folds / shared fail-closed registry.
- [x] [Review][Defer] Three independent `UpdateAsync` calls in `EraseAsync` can leave detail/processing/index mutually inconsistent on mid-flight failure [src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs:25-48] — deferred, pre-existing — no multi-key transactional write seam in the approved `ReadModelWritePolicy` API; same per-key optimistic pattern used by live projection handlers.
- [x] [Review][Defer] Optimistic concurrency retries re-run `ApplyErasure` and refresh `ErasedAt` [src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs:50-53; PartyDetailProjectionHandler.ApplyErasure] — deferred, pre-existing — `ApplyErasure` always stamps `UtcNow`; short-circuiting on `IsErased` would need a deliberate idempotency contract change.
- [x] [Review][Defer] Erasure copies through pre-erasure `ProjectedAt`/`ProjectionVersion` on detail/index [src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs:50-82] — deferred, pre-existing — stamping erasure-time freshness is a design choice entangled with the open AC7 freshness-mapping gap; index timestamps also cover unrelated remaining parties.

**Group 4 (host wiring/ACL/registration + deletion of retired legacy mechanics, 30 files) findings:**

- [x] [Review][Patch] Missing `POST /project/v2/reconcile` from the EventStore-only DAPR ACL allow-list [src/Hexalith.Parties.AppHost/DaprComponents/accesscontrol.parties.yaml] — Confirmed by two independent review layers. The EventStore SDK maps `/project/v2/reconcile` under the same `mapNamedProjectionEndpoint` gate as the already-admitted `/project/v2` (`references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/EventStoreDomainServiceExtensions.cs`). With exact-match route names and `defaultAction: deny`, a service-invocation call to this route from `eventstore` is currently rejected. The file's own header comment claims to cover "the exact command/query/projection/discovery operations," which is inaccurate while this route is missing. Fixed 2026-08-02: added the route to the ACL and to `ArchitecturalFitnessTests.PartiesAppHost_KeepsPartiesAppIdAndDedicatedDaprAccessControl`. `Hexalith.Parties.Tests` FitnessTests/HealthChecks/PlatformApiPrerequisitesTests classes pass 42/42.
- [x] [Review][Decision] `ProjectionActorsHealthCheck` was deleted with no equivalent SDK/read-model health signal added, contrary to the story's own task text ("Replace or remove `ProjectionActorsHealthCheck` only with equivalent SDK/read-model health evidence") [src/Hexalith.Parties/HealthChecks/PartiesHealthCheckExtensions.cs] — Confirmed by three independent review layers (Group 4 + two Group 5 layers) plus direct verification: only `dapr-sidecar`, `dapr-statestore`, `dapr-pubsub`, `tenants-integration`, and `memories-search` checks remain. Resolved 2026-08-02: the Administrator chose to add a best-effort SDK read-model-store connectivity health check now rather than accept the gap.
- [x] [Review][Patch] Add a best-effort SDK read-model health check to `AddPartiesDaprHealthChecks` [src/Hexalith.Parties/HealthChecks/PartiesHealthCheckExtensions.cs] — Per the decision above: probe `IReadModelStore`/`PartySdkReadModelEraser` or equivalent SDK read-model connectivity (a true `Rebuilding` signal isn't available from the SDK today per the already-deferred Group 2 finding, so this check is scoped to connectivity/reachability only, not full freshness-state fidelity). Must remain non-readiness-blocking per the story's own task text ("projection degradation must remain non-readiness-blocking"). Add a corresponding `HealthEndpointIntegrationTests.cs` case. Also fixes the stale doc-comment finding above once the new check exists. Fixed 2026-08-02: added `PartyReadModelHealthCheck` (probes `IReadModelStore.GetAsync` against a sentinel key, `Degraded` not `Unhealthy` on failure, no `ready` tag), registered as `party-read-models`. Added `HealthEndpoint_ReadModelStoreUnreachable_ReturnsDegradedWithoutBlockingReadinessAsync` plus a `party-read-models: Healthy` assertion in the happy-path test. `HealthEndpointIntegrationTests` passes 9/9.
- [x] [Review][Patch] Stale doc comments still describe the deleted projection-actor health check as active [src/Hexalith.Parties/Program.cs:31; src/Hexalith.Parties/HealthChecks/PartiesHealthCheckExtensions.cs:18-19] — Confirmed by two independent review layers. Both comments claim `/health` reports "projection actor responsiveness" / that "projection actor health still contribute[s] to `/health`," which is no longer true once the finding above is resolved either way. Fixed 2026-08-02: both comments now describe SDK read-model store reachability instead.
- [x] [Review][Patch] `PartyMemoryIndexingService.IndexAsync` has zero production callers after `PartyProjectionUpdateOrchestrator` was deleted — Memories search indexing is silently dead [src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs (deleted); src/Hexalith.Parties/Search/PartyMemoryIndexingService.cs] — Confirmed by direct repo-wide search: `.IndexAsync(` has zero production call sites; only unit tests call it directly. The service remains registered in DI and unit-tested but nothing in the new SDK projection handlers calls it. The story's own task list requires "Preserve optional Memories indexing/search behavior as best effort; do not make Memories required for local search" — this was missed, not intentionally dropped (no completion note claims otherwise). Party search results will grow silently stale indefinitely. Fixed 2026-08-02: `Hexalith.Parties.Projections` cannot reference `Hexalith.Parties` (the dependency runs the other way), so this needed a seam. Added `IPartyIndexSearchIndexer` (+ `NoOpPartyIndexSearchIndexer` default) in Projections; `PartyIndexSdkProjectionHandler.ProjectAsync` now notifies it best-effort with the latest folded entry and triggering event after a successful write. Implemented the real notifier (`PartyMemoryIndexEntrySearchIndexer`) in `Hexalith.Parties`, wrapping `PartyMemoryIndexingService` with the same Enabled/CaseId gating and one-shot warning the retired orchestrator had, registered in `AddParties` only when Memories search is enabled. Added `IndexHandler_NotifiesSearchIndexerWithLatestEntryAndEventAfterSuccessfulWriteAsync` and `IndexHandler_ErasureDoesNotNotifySearchIndexerAsync`. `Hexalith.Parties.Projections.Tests` passes 19/19.
- [x] [Review][Patch] `IIndexPartitionStrategy`/`SingleKeyPartitionStrategy` are orphaned dead code after their only consumer (`PartyIndexProjectionActor`) was deleted [src/Hexalith.Parties.Projections/Abstractions/IIndexPartitionStrategy.cs; src/Hexalith.Parties.Projections/Strategies/SingleKeyPartitionStrategy.cs] — Confirmed by direct repo-wide search: zero references beyond their own definitions. The DI registration for `SingleKeyPartitionStrategy` was removed but the files themselves were not. Not part of the enumerated 18-file rollback set, so the architectural fitness guardrail doesn't catch them either. Fixed 2026-08-02: both files deleted (their containing directories were otherwise empty). `Hexalith.Parties.Projections` builds 0 warnings/0 errors.
- [x] [Review][Patch] The DI-registered GDPR erasure-cleanup delegate list (`sdk-read-models` + `projection-cache` + `aggregate-readable-state` + `snapshots` + `memories-search`, 5 entries — the review's original "3" undercounted the list) is never resolved and exercised against a real `ServiceProvider` in any test [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:175-270] — Confirmed by two independent review layers. Existing tests either hand-build fake delegates (`ErasureVerificationServiceTests.cs`) or mock `IErasureVerificationService` entirely (`PartyDomainProcessorValidationTests.cs`); none resolve the real DI-wired list and assert it calls `PartySdkReadModelEraser.EraseAsync` with the correct `(tenantId, partyId)`. A swapped-argument or no-op mistake here would still report `Cleaned` and go undetected. Related and verified: the cleanup delegates run strictly sequentially (`ErasureVerificationService.VerifyErasureAsync` is a plain `for` loop with `await`), so cache eviction always follows canonical-store erasure within one verification call — but a concurrent request landing in that narrow gap could still read the not-yet-evicted cache via a degraded-read fallback path. This residual window is much narrower than before the Group 3 fix and shares its already-accepted architecture trade-off. Fixed 2026-08-02: added `AddParties_ErasureCleanupDelegatesInvokeRealEraserAndCacheEvictionWithCorrectKeysAsync`, which builds the real `AddParties` DI graph with a substitute `IReadModelStore`, resolves the real `IReadOnlyList<ErasureStoreCleanupDelegate>`, invokes all 5, and asserts the eraser's underlying `IReadModelStore.GetAsync` calls used the exact `(tenantId, partyId)`-derived keys. (`PartySdkLastKnownReadModelCache` is `sealed` and not mockable, so cache eviction itself is only indirectly exercised — proven not to throw — not directly asserted; the narrow race window above remains an accepted, not newly-tested, trade-off.) `ProjectionPlatformAdapterTests` passes 3/3.
- [ ] [Review][Defer] The single line of host wiring that makes the entire SDK projection migration functional is only checked as literal source text, never exercised end-to-end [src/Hexalith.Parties/Program.cs:18-20, `builder.AddEventStoreDomainService(typeof(PartyAggregate).Assembly, typeof(PartyDetailProjectionHandler).Assembly)`] — Confirmed by direct verification: `ArchitecturalFitnessTests`, `PlatformApiPrerequisitesTests`, and `RetiredLeafProjectFitnessTests` all assert `source.ShouldContain("typeof(PartyDetailProjectionHandler).Assembly")` against raw file text. `EventStoreGatewayE2ETests` submits a command and polls for `eventCount > 0` but never queries the projected read model afterward. If this assembly-scan argument were wrong or the SDK's handler discovery silently found zero handlers, every query would return stale/empty projections while every currently-passing test remained green. Investigated 2026-08-02, reclassified from Patch to Defer: the natural place to close this is `EventStoreGatewayE2ETests`, which already runs against `PartiesAspireTopologyFixture` and exposes both an `EventStoreClient` (public gateway) and a direct `PartiesClient`. But that fixture's own documented, deliberate limitation blocks it: `RequireSeededTenants()` unconditionally throws because Story 12.2 retired `TenantIntegrationTestSeeder`, and an authenticated query is exactly the scenario the fixture's own doc-comment calls out as needing it. The existing command test avoids this because command routing validates the tenant claim directly from the JWT, not against a seeded Tenants service. Closing this gap properly requires reinstating the retired seeder — real work, out of scope for a code-review patch pass, not a quick addition. Deferred pending explicit direction.
- [x] [Review][Patch] Story 8.6's own AC9 text is stale relative to the shipped ACL [`8-6-projection-and-query-sdk-migration.md:34`] — AC9 still reads "...the DAPR ACL remains deny-by-default with only `eventstore -> POST /process`," but `accesscontrol.parties.yaml` now allow-lists 12 operations for `appId: eventstore`. Not a violation — the frozen spec's Design Notes explicitly say this internal allowlist is not a public API expansion, and the Completion Notes already reconcile the wording elsewhere — but AC9's own sentence was never updated to match, so a literal read of the story's acceptance criterion fails against the file it names. Fixed 2026-08-02: AC9 now describes the exact-route allowlist and cross-references the Design Notes.
- [ ] [Review][Defer] Minor/cosmetic: `PartyDetailProjectionQueryActor`/`PartyIndexProjectionQueryActor` keep the "Actor" name with zero actor behavior after `IPartyProjectionQueryActor` was deleted (no shared interface remains linking the two static classes) — both are intentional temporary rollback shims per the story's Rollback Plan; `PartySdkReadModelOptions.ConfigurationSection` reuses the `"EventStore:Projections"` config key previously used by the now-removed `Hexalith.EventStore.Server.Configuration.ProjectionOptions`, which is functionally harmless today but could collide in meaning if EventStore.Server-style config is ever reintroduced; the new `Dapr.Actors.AspNetCore` package reference and the `$(HexalithCommonsHttpFromSource)` MSBuild property rename in `Hexalith.Parties.csproj` are undocumented but verified correct and consistent with sibling-project patterns.
- [x] [Review][Dismiss] ~~`accesscontrol.parties.yaml` route count grew from 1 to 12 operations for the `eventstore` appId, including 5 rebuild-lifecycle mutation routes~~ — False alarm: verified scoped to the single trusted `eventstore` appId with `defaultAction: deny` intact at both the top level and the appId level; no wildcard, no second appId. Covered by `ArchitecturalFitnessTests.PartiesAppHost_KeepsPartiesAppIdAndDedicatedDaprAccessControl`. Expected consequence of the SDK migration per the frozen spec's Design Notes, not a defect.

**Group 5 (test changes: modified + 9 fully-deleted legacy test files, 13 files) findings:**

_Several deleted actor-side test files never actually ran a dual-path actor-vs-SDK comparison (they only ever exercised the actor). Their deletion doesn't newly break anything by itself, but it permanently removes the only route to closing several already-open Group 2/3 findings via comparison, and confirms the story's own "dual-path harness" task requirement was, at best, only ever transiently true. This elevates the priority of the already-open, unresolved items below — no new bullets are added for them, only this pointer: true duplicate/out-of-order delivery idempotency (Group 2, `AlreadyCompleted()` short-circuit never exercised), self-referential rebuild-vs-replay parity tests (Group 2), untested `PartySearchQueryHandler` happy path / erased-party search exclusion (Group 3), untested `PartySdkLastKnownReadModelCache` cross-tenant isolation (Group 3, now with a concrete suggested test shape: populate the cache for tenant-A, force a degraded read as tenant-B, assert tenant-B never observes tenant-A's data), and untested `ExportPartyDataAsync`/`GetProcessingRecordsAsync` erased/redacted-shape assertions (Group 3, now with a concrete suggested test shape: construct an erased or PII-unavailable `PartyDetail`, assert `Party` is null and no PII substrings survive serialization). Also newly confirmed: `ProjectionFreshnessStatus.Rebuilding` (the already-deferred Group 2 unreachable-freshness-value finding) is producer-dead but still consumer-alive — UI code (`PartyPickerApiClient.cs`, ConsumerPortal pages, `DataFreshnessIndicatorTests.cs`) still actively branches on/renders it, sharpening that finding's "user-facing consequences" from theoretical to concrete. The health-check gap logged as `[Review][Decision]` under Group 4 is independently confirmed here too: `HealthEndpointIntegrationTests.cs`'s rewritten happy-path test now positively asserts the `projection-actors` key is *absent* rather than checking a replacement, and `ProjectionActorsHealthCheckTests.cs` (5 facts: healthy/degraded/rebuilding/timeout) was deleted with nothing added in its place._

- [x] [Review][Patch] Mid-read `OperationCanceledException` propagation has no test on the SDK query path [src/Hexalith.Parties/Queries/PartySdkQueryService.cs — all read methods share a `catch (OperationCanceledException) { throw; } catch (Exception) { return Failure(ActorException); }` pattern relying on catch order; tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs] — The only existing cancellation test pre-cancels the token before calling `ExecuteAsync`, so it never reaches the store call or the catch-order logic being relied on; no test makes the store itself throw `OperationCanceledException` mid-read. Reordering/collapsing the two catch clauses would silently convert a canceled request into a generic bounded failure with nothing to catch it. Fixed 2026-08-02: added `ExecuteAsync_StoreThrowsOperationCanceledMidReadPropagatesInsteadOfBoundedFailureAsync`, stubbing the store to throw mid-read and asserting the exception propagates.
- [x] [Review][Patch] `EncryptionPipelineIntegrationTests.cs`'s new `IPartyKeyRetryScheduler` substitute (added to isolate the fixture from the removed actor-proxy dependency) is fully stubbed — always succeeds, always reports not-pending — so the mark-pending/clear-pending/is-pending retry-scheduling path itself is no longer exercised by this integration test. Fixed 2026-08-02: replaced the NSubstitute stub with a real (non-mocked) `InMemoryPartyKeyRetryScheduler` that tracks actual pending state, so mark/clear/is-pending transitions are genuinely exercised rather than hard-coded. `EncryptionPipelineIntegrationTests` passes 6/6.
- [x] [Review][Patch] `EventStoreGatewayRoutingTests.cs`'s isolated-gateway test factory changed its hosted-service exclusion from an explicit, reviewed allowlist of 3 named incompatible services to "remove every `IHostedService` except `EventStoreAuthorizationStartupValidator`" — Confirmed by two independent review layers. Any hosted service added to the EventStore SDK in the future (already at least 2 more exist: `SafeDenialQueryRouteStartupLogger`, `DaprHealthHistoryCollector`) is now silently swept out of this test's DI graph with no conscious opt-in, inverting the original "prove new services are compatible or explicitly exclude them" intent. Fixed 2026-08-02: kept the denylist behavior (factory-registered hosted services genuinely have no `ImplementationType` to allowlist by name), but added a `KnownExcludedHostedServiceNames` snapshot assertion that fails loudly if the excluded set ever changes, restoring the "conscious review required" property without needing every hosted service to be name-addressable. `EventStoreGatewayRoutingTests` passes (76/76 combined with `PartySdkQueryHandlerTests` in the same run).
- [ ] [Review][Defer] Minor/cosmetic: the new DI test `AddParties_UsesSdkReadModelsAndCursorCodecWithoutLocalProjectionMechanics` asserts absence via a brittle `descriptor.ServiceType.FullName` string match rather than a type reference, weaker than the `ShouldBeOfType<T>()` checks it replaced; the renamed health-endpoint test `HealthEndpoint_AllComponentsHealthy_Returns200WithoutRetiredProjectionActorCheckAsync` keeps an "all components healthy" framing that now excludes a real component (SDK read models) from what "all" verifies; `EventStoreGatewayRoutingTests.cs` renamed the PII seed `"Ada"/"Lovelace"` to `"SyntheticPrivateFirstName8472"/"SyntheticPrivateLastName6391"` in only 2 of dozens of usages across the suite (7 other test files still use `"Ada"/"Lovelace"`) with no comment explaining the scope; `DirectPartiesCommandRouter`'s test double now writes a `CommandStatus.Received` record before processing and keys the completion write on `command.MessageId` instead of `command.CorrelationId` — traced against the EventStore SDK's `SubmitCommandHandler.cs` and confirmed this now correctly mirrors production behavior, but nothing in the diff explains why the test double changed.

**Group 6 (tracking/documentation files, 6 files) findings:**

- [x] [Review][Patch] **Currently failing, live in the working tree:** `Matrix_ValidationEvidenceCommandsAreReproducible` fails today [tests/Hexalith.Parties.Tests/FitnessTests/PlatformApiPrerequisitesTests.cs:18,604-623,1069-1090; story-8-3-platform-api-prerequisite-matrix.md, "Payload protection engine package" row] — Confirmed by direct re-run (`PlatformApiPrerequisitesTests` 11/12, this test red) and root-caused via `git log -p`: commit `03ab938` (this same review cycle's own Group 2/3 fix commit) carried a legitimate, unrelated forward submodule advance (from an earlier "Story 3.11" closure commit) that moved `references/Hexalith.EventStore` from `4bcf2484a09eb26490cb2d32ceb6df8949f90cc6` to `30810727cb91f5886cb2aa13601680a23b18bcc0` (confirmed ancestor, not corruption) without updating the hardcoded `PayloadProtectionEventStoreSha` test constant or the matrix row's "Validation command or inspection used" evidence text. Fix: update the constant and matrix evidence to the current SHA and its `git describe` output. Also note: the "EventStore projection/query SDK" row that Story 8.6 is actually about (citing `c590590b`) has no automated reproducibility check at all (`RequiredAvailableConsumptionIdentities` doesn't cover it) — only narrative text backs that identity claim. Fixed 2026-08-02: updated `PayloadProtectionEventStoreSha`/`PayloadProtectionEventStoreDescribe` and the matrix row's evidence text to the current submodule SHA `30810727cb91f5886cb2aa13601680a23b18bcc0` (`v3.89.0-8-g30810727`). `PlatformApiPrerequisitesTests` passes 12/12 again.
- [x] [Review][Decision] The EventStore-identity documentation trail is internally inconsistent and doesn't reflect the actual shipped state — Confirmed by all four review layers, multiple independent angles:
  - The frozen spec (`spec-8-6-projection-and-query-sdk-migration.md`) was never renegotiated: its Intent paragraph still names the abandoned `fa2d1c9910f8976553adb33dcdb1c9ff2ea75594` as "the approved identity," its frontmatter `context:` list still cites only the packet for that abandoned identity, its "Never: infer approval for descendants, tags, or packages" constraint is in tension with the ad hoc `v3.89.0` pivot, and a new "## Spec Change Log" heading was added but left with zero entries despite the frozen block's own header requiring "do not modify unless human renegotiates." The frozen spec also quietly dropped its specific ACL constraint ("do not widen the DAPR ACL beyond `eventstore -> POST /process`") in favor of a vaguer "Ask First" clause — loosened to match what was already built, not enforced against it.
  - The story's "452/452 all green" completion claim is not supported by its own Debug Log at the credited SHA: every entry that explicitly confirms checkout `c590590b` records 449/452 (3 failures); the only 452/452 result is logged under a different SHA (`8f004ecf8be271ec5e76f97f944c1fc1154391c7`) that appears exactly once and is never reconciled to `c590590b`/v3.89.0 anywhere else in the matrix, Completion Notes, Change Log, or test-summary.md — and that result is itself credited partly to concurrently-landing, unrelated Story 8.7 (G5) work, not disclosed as such at the summary level.
  - `sprint-status.yaml` asserts contradictory things about which identity Parties finally consumed within the same `last_updated: 2026-08-01` pass: the Gate 2 action-item comment says Parties selects `fa2d1c9` at both gitlink and checkout ("identity reconciliation is complete"), while the `8-6-projection-and-query-sdk-migration` and Epic-7 rollback-retention comments correctly say the shipped identity is `v3.89.0`/`c590590b`.
  - AC1's gate task is checked complete, and `8-6-ac1-llm-instructions.md`'s prescribed two-agent owner-proof gate procedure is presented as current, but that procedure only ever ran (and passed) for `fa2d1c99`, which then failed to compile and was abandoned; the identity actually shipped was authorized only by an ad hoc same-repo Administrator direction, formally backfilled after the fact by `sprint-change-proposal-2026-08-02-story-8-6-eventstore-identity-authorization-backfill.md`. (Correction, 2026-08-02: the original review pass found this file untracked via a stale `git status` snapshot taken before the day's earlier fix commit `03ab938` landed. `git ls-files` confirms it was in fact committed by `03ab938` and is part of the repository history — this specific sub-claim does not hold.)
  - The story-8-3 matrix's "EventStore projection/query SDK" and "EventStore DataProtection" rows are both marked `available` with no qualifier, even though each row's own prose admits the checked-out pin has since drifted and "Story 8.6's regression evidence is not yet re-confirmed" at the current pin.

  None of this is hidden — the caveats mostly exist *somewhere* in the diff — but they live as unenforced prose annotations rather than in the actual status fields, so a reader consulting only the status columns (or a downstream Story 8.10) would get a falsely clean picture. Resolved 2026-08-02: the Administrator chose to formally renegotiate the frozen spec now rather than leave it as-is with caveats.
- [x] [Review][Patch] Formally renegotiate the frozen spec and reconcile the identity documentation trail — Per the decision above: (1) add a `## Spec Change Log` entry to `spec-8-6-projection-and-query-sdk-migration.md` reconciling the `fa2d1c99` → `v3.89.0`/`c590590b` pivot and citing the backfill SCP; (2) update its Intent paragraph and frontmatter `context:` list to stop naming the abandoned identity/packet as current; (3) fix the self-contradicting Gate 2 comment in `sprint-status.yaml` (currently claims Parties selects `fa2d1c9` at both gitlink and checkout); (4) `git add` and commit the untracked `_bmad-output/planning-artifacts/sprint-change-proposal-2026-08-02-story-8-6-eventstore-identity-authorization-backfill.md` so the audit trail it's cited from actually exists in the reviewed changeset; (5) reconcile or annotate the "452/452 at `8f004ecf`" vs. "449/452 at `c590590b`" discrepancy in the Debug Log so the Completion Notes' all-green claim is traceable to a specific, correct SHA. Fixed 2026-08-02: (1)(2)(3)(5) done — see the frozen spec's renegotiated Intent/Spec Change Log, the fixed `sprint-status.yaml` Gate 2 comment, and the Debug Log/Completion Notes reconciliation notes above. (4) turned out to be unnecessary: `git ls-files` confirms `_bmad-output/planning-artifacts/sprint-change-proposal-2026-08-02-story-8-6-eventstore-identity-authorization-backfill.md` was already committed by `03ab938` earlier the same day — the "untracked" claim in the original finding was based on a stale pre-`03ab938` `git status` snapshot, not current state.
- [x] [Review][Patch] Completion Notes and the AC7 task checkbox claim freshness metadata is "preserved," contradicting this same file's own unresolved Group 2 finding that `Rebuilding`/`Degraded`/`LocalOnly` can never be produced by the new query layer — Confirmed by two independent review layers. Fixed 2026-08-02: Completion Notes now names the three reproduced statuses and the three that cannot be reproduced, cross-referencing the open Group 2 finding.
- [x] [Review][Patch] File List section has three inaccuracies [`8-6-projection-and-query-sdk-migration.md`, File List] — `8-6-ac1-llm-instructions.md` is a brand-new file per its diff header but isn't listed under "Added"; `spec-8-6-projection-and-query-sdk-migration.md` is extensively rewritten (Intent/Boundaries/Code Map/Tasks/Verification all replaced) but doesn't appear anywhere in the File List; the story file itself is `new file mode 100644` per its own diff header but self-lists under "Modified." Fixed 2026-08-02: both missing files added to the list, and a note explains the story-file classification choice. Also reconciled this session's own new files (health check, search indexer seam, two touched test files) into the same list while it was open for edit.
- [x] [Review][Patch] The story-8-3 matrix's "Payload protection engine package" row pins its validation command to a moving ref (`git ls-tree HEAD ...`) instead of an exact commit SHA, unlike the sibling domain-service-host row which pins an exact SHA — the row's own prescribed command can never be reproduced later since `HEAD` keeps moving; this is the same root cause as the currently-failing test above and should be fixed the same way (pin to an exact commit). Fixed 2026-08-02: the "EventStore projection/query SDK" and "EventStore DataProtection" rows' `git ls-tree HEAD` commands are now pinned to commit `03ab938c637aa15f7a0af402afc8664dfc54d1a4` (the last real commit as of this review), matching the sibling row's convention.
- [x] [Review][Patch] Several sprint-status.yaml action items outside Story 8.6's own scope (Epic 6 File List check, Epic 7 gitlink RC gate) were closed `done` in this same diff citing a file-list automation gate (`_bmad/scripts/check_file_list.py`) that this story's own Debug Log records as absent — contrary to the story's own task "Move sprint status through workflow states without rewriting unrelated comments or statuses." Reopen or caveat those items; confirmed the script is still absent from the repo today. Fixed 2026-08-02: added caveat comments to both items rather than unilaterally reopening them (out of Story 8.6's scope to fully re-investigate Epic 6/7 history).
- [ ] [Review][Defer] Minor/cosmetic: the Epic 7 rollback-retention action item is closed `done` citing an authorization SCP "approved 2026-08-02" for an action the same annotation dates to 2026-08-01 (approval postdating the act it authorizes by a day); `sprint-status.yaml`'s `last_updated: 2026-08-01` predates content in the same file citing "approved 2026-08-02" (will be corrected naturally when sprint-status is synced at the end of this review).

**Group 3 (SDK query path, expanded to 13 files) findings:**

- [x] [Review][Patch] GDPR erasure-status and erasure-certificate reads had no exception handling at all, unlike every sibling method [src/Hexalith.Parties/Queries/PartySdkQueryService.cs:143,177] — Fixed 2026-08-02: `GetErasureStatusAsync`/`GetErasureCertificateAsync` now wrap their bodies in the same `try { ... } catch (OperationCanceledException) { throw; } catch (Exception) { return Failure(ActorException); }` pattern as `ReadDetailAsync`/`ExportPartyDataAsync`/`GetProcessingRecordsAsync`/`ReadIndexAsync`. A transient store failure on these two GDPR-critical endpoints previously threw raw through the query contract, violating AC7. Added `ErasureStatusHandler_StoreFailureReturnsBoundedFailureInsteadOfThrowingAsync` and `ErasureCertificateHandler_StoreFailureReturnsBoundedFailureInsteadOfThrowingAsync`.
- [x] [Review][Patch] Last-known-good cache had no relationship to erasure — a degraded read during a narrow store-outage window after erasure could serve pre-erasure PII [src/Hexalith.Parties/Queries/PartySdkLastKnownReadModelCache.cs; src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs] — Confirmed by three independent review layers with a full root-cause trace: `ReadDetailModelAsync`/`ReadIndexAsync`/`ReadProcessingModelAsync` cache every successful read; `PartySdkReadModelEraser.EraseAsync` redacts the canonical store but never touches this cache; a store failure between erasure and the next successful read serves the stale pre-erasure snapshot as a "degraded" result, including through `ExportPartyDataAsync`. The retired actor kept its own equivalent in-process cache in sync on erasure (`s_lastKnownDetails[stateKey] = erased`) — this is a genuine regression, not a pre-existing risk. Fixed 2026-08-02: added `EvictDetail`/`EvictProcessing`/`EvictIndex` to the cache, and wired the previously-stub `"projection-cache"` erasure-cleanup delegate in `PartiesServiceCollectionExtensions.cs` (it existed but did no real work — always reported `Cleaned` unconditionally) to call them after `PartySdkReadModelEraser.EraseAsync`. Chose fail-closed eviction over reconstructing the redacted value in the cache: a degraded read for the narrow gap window now returns a bounded failure instead of either leaking PII or requiring more invasive plumbing. Added 3 unit tests for the new `Evict*` methods and `DetailHandler_DegradedReadAfterEvictionFailsBoundedInsteadOfReturningStalePreErasurePiiAsync` proving the fix end-to-end.
- [x] [Review][Patch] `ProjectionFreshnessStatus.Rebuilding`/`Degraded`/`LocalOnly` can never be produced by the new query layer [src/Hexalith.Parties/Queries/PartySdkQueryService.cs `ToPartiesFreshness`] — Closed 2026-08-03 via AC7 renegotiation (option 2): SDK-native `Current`/`Stale`/`Unavailable` are the approved producer set; store-outage uses `Stale`+`IsDegraded`; Picker collapses legacy enum values into degraded UX. Spec Always / Spec Change Log / AC7 updated.
- [x] [Review][Patch] Query-path exceptions are silently discarded, not logged [src/Hexalith.Parties/Queries/PartySdkQueryService.cs] — Fixed 2026-08-03: `ILogger` + `LoggerMessage` on query failures, processing soft-fail, and cursor `failureReason`. Group 2 fold-level operator logging remains open separately.
- [x] [Review][Defer] Cursor codec's log-safe failure reason discarded via `out _` [src/Hexalith.Parties/Queries/PartySdkQueryService.cs `TryResolvePaging`] — Fixed 2026-08-03 as part of query-path logging (`LogCursorRejected`).
- [x] [Review][Defer] Minor/cosmetic: `TryResolvePaging` returns `InvalidCursor` for an out-of-range `PageSize` even when no cursor was supplied; `TryValidateDetailEnvelope` AggregateId null out-partyId; duplicated `PartyDomain`; non-durable last-known cache — deferred intentional trade-offs from the first Group 3 pass.
- [x] [Review][Patch] Remaining test coverage gaps for this group [tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs] — Fixed 2026-08-03 in Group 3 re-review verification-gap patches.

- [x] [Review][Decision] EventStore identity used for this deletion never received formal owner/SCP authorization — Resolved 2026-08-02: the Administrator confirmed the pivot to `v3.89.0`/`c590590b` was their real, intentional direction. Backfilled by `sprint-change-proposal-2026-08-02-story-8-6-eventstore-identity-authorization-backfill.md` (approved 2026-08-02), which reconciled the two affected Story 8.3 matrix rows and the sprint-status Epic 7/8 annotations to record that identity as authorized, and opened a follow-up action to re-validate at the current pin (`4bcf2484`, drifted further via unrelated Story 8.7 work). Deletion was not reverted.
- [x] [Review][Patch] Erasure checkpoint handling in the shared tenant index [src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs:39] — Clarified 2026-08-04: Party identifiers are permanent. The erasure high-water mark is the authoritative guard and later same-ID creation cannot restore an entry; checkpoint cleanup must not weaken that tombstone.
- [x] [Review][Patch] Dead reference to a deleted class in the GDPR consent query path [src/Hexalith.Parties.Client/AdminPortal/HttpAdminPortalGdprClient.cs:318] — Fixed 2026-08-02: `GetPartyDetailAsync` now sends `"PartyDetailProjectionQueryActor"`, matching its sibling methods. Corrected the locked-in assertion in `AdminPortalGdprOperationContractTests.cs:333`. `Hexalith.Parties.Client.Tests` (35/35).
- [x] [Review][Patch] Same stale literal in a second, currently-unwired client option [tests/Hexalith.Parties.AdminPortal.Tests/Services/PartiesAdminPortalApiClientTests.cs:543] — Fixed 2026-08-02: both occurrences updated to `"PartyDetailProjectionQueryActor"`. `Hexalith.Parties.AdminPortal.Tests` (39/39).
- [x] [Review][Patch] Architectural fitness guardrail only covers 2 of the 18 deleted files [tests/Hexalith.Parties.Tests/FitnessTests/ArchitecturalFitnessTests.cs] — Fixed 2026-08-02: `PartiesProjectionRuntimeUsesOnlyEventStoreSdkHandlers` now asserts absence of all 18 rollback-only files. `ArchitecturalFitnessTests` (21/21).

**Group 2 (SDK projection handlers & folds) findings:**

- [x] [Review][Patch] Erasing a party never clears the GDPR Art.30 Processing read model's checkpoint at all [src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs:15] — Fixed 2026-08-02, combined with the tombstone decision below: `EraseAsync` now redacts the Detail row in place and resets the Processing row's `LastSequenceNumber` (preserving prior `Records` as Art.30 history) instead of never touching Processing at all. Added `Eraser_RedactsDetailInPlaceResetsProcessingCheckpointAndNeverErasesWholeSharedIndexAsync` plus null-model unit tests for both helpers.
- [x] [Review][Patch] Unguarded `.Max()` can crash an entire tenant's shared-index rebuild [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs:178] — Fixed 2026-08-02: `AccumulateAsync` now short-circuits on an empty `aggregateHistory.Events` before calling `Fold`, matching `ProjectAsync`'s existing guard. Added `SharedIndexRebuild_AccumulateWithEmptyAggregateHistoryDoesNotThrowAsync`.
- [x] [Review][Patch] GDPR Art.30 processing-activity log records `Outcome = "Succeeded"` for events that failed to deserialize [src/Hexalith.Parties.Projections/Handlers/PartyProcessingActivityFold.cs:34] — Fixed 2026-08-02: `Fold` now records `Outcome = "Failed"` for a genuine deserialization failure and `Outcome = "Redacted"` for a whole-payload-redacted event, reserving `"Succeeded"` for events that actually applied. Added `ProcessingActivityFold_CorruptLiveEventIsRecordedAsFailedNotSucceeded` and `ProcessingActivityFold_WholePayloadRedactedEventIsRecordedAsRedactedNotSucceeded`.
- [x] [Review][Patch] All diagnostic logging for dropped/unresolved/corrupt/ambiguous events was deleted, not migrated [src/Hexalith.Parties.Projections/Handlers/PartySdkProjectionFold.cs] — Fixed 2026-08-05: `DeserializeNew` now accepts an optional `ILogger?` and reports non-JSON, unknown-type, ambiguous-short-name, corrupt-live-deserialization, redacted-tail-deserialization, and whole-payload-redacted drops through six distinct message templates (no aggregate/party/tenant identifiers — only event type name and sequence number). `PartyProcessingActivityFold.Fold` is the sole carrier of the logger for the whole Detail-slot handler (covers `ProjectAsync`, `PrepareRebuildAsync`, and the unresolved-only audit write in one place, exactly once per delivery); `PartyIndexSdkProjectionHandler` passes its logger only from the actual persisting `FoldCore`/`AccumulateAsync` calls, not from the idempotent-no-op pre-check or reconciliation walk, to avoid duplicate log lines for the same drop. `[LoggerMessage]` source generation was not usable here (this project has no direct `Microsoft.Extensions.Logging.Abstractions` package reference, only a transitive one through the EventStore SDK ProjectReferences, and the generator does not activate across a P2P boundary without one — same class of gap as the pruning-driven CS8795 already documented in `Hexalith.Parties.csproj`); used plain `ILogger` extension-method calls instead, matching the pattern already used elsewhere in this codebase (e.g. `PartyMemoryIndexingService.cs`, `PartyKeyRetryActor.cs`). Added `RecordingLogger<T>` to `Hexalith.Parties.Projections.Tests` and 7 new tests asserting exact level/message/exception-presence for every drop path. `Hexalith.Parties.Projections.Tests` passes 216/216; `Hexalith.Parties.Tests` passes 503/504 (the one failure is the pre-existing, out-of-scope Story 8.7/G5 `PayloadProtectionEventStoreSha` matrix-drift test — see Group 6 finding above, unrelated to this patch).
- [x] [Review][Patch] A genuinely corrupt live event is now indistinguishable from an expected post-erasure redacted-tail read in the fold's checkpoint-advancement logging [src/Hexalith.Parties.Projections/Handlers/PartySdkProjectionFold.cs:48-58] — Fixed 2026-08-05, together with the logging item above: the corrupt-non-redacted path logs `PayloadDeserializationFailed` at Warning; the expected post-erasure redacted-tail path logs `RedactedEventDropped` at Information — distinct templates and levels so operators no longer have to guess which case produced a given drop. Both still skip-and-advance identically (unchanged, correct behavior); only the log signal was missing. Covered by `DeserializeNew_CorruptLiveEvent_LogsPayloadDeserializationFailedWarningDistinctFromRedacted` and `DeserializeNew_RedactedTailDecodeFailure_LogsRedactedEventDroppedInformationDistinctFromCorrupt`.
- [x] [Review][Decision] Full rebuild may resurrect a hard-deleted (erased) party's Detail row as a redacted tombstone [src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs:93,117-124] — Resolved 2026-08-02: Administrator chose "persisted tombstone" as canonical (matches the architecture doc's "PII-free tombstone semantics" invariant and what a full rebuild already produced). `PartySdkReadModelEraser.EraseAsync` no longer hard-deletes the Detail row; it now redacts it in place via `PartyDetailProjectionHandler.ApplyErasure`, so the eraser and the rebuild path agree.
- [ ] [Review][Patch] `PrepareRebuildAsync`/`FinalizeAsync` write with `ReadModelBatchConcurrency.LastWrite` (no ETag check) [src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs:99,103; PartyIndexSdkProjectionHandler.cs:102] — Still not fixed. Investigated 2026-08-05: a unilateral switch to `Match(etag)` would only be safe if the caller (the EventStore SDK's rebuild-plan executor) has a defined retry/abort contract for a rebuild-plan write conflict — that contract lives outside this repository, in `Hexalith.EventStore.DomainService`'s rebuild orchestration, not in the `IAsyncDomainProjectionRebuildHandler`/`IAsyncDomainSharedProjectionRebuildCompletionHandler` surface this story consumes. Changing the concurrency mode without knowing whether the SDK retries the whole rebuild, discards the plan, or surfaces an unhandled exception on conflict risks trading a silent-overwrite bug for a silent-total-rebuild-failure bug. Left open pending explicit SDK-level input, per the story's own prior note; not addressed in this session.
- [x] [Review][Dismiss] ~~`PartySdkReadModelOptions` has no validation on bind~~ — False positive: verified against `src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs` (Group 4, not visible to the reviewing subagent that raised this), which already registers `.Validate(options => options.FreshnessAgingSeconds >= 0 && options.FreshnessStaleSeconds >= options.FreshnessAgingSeconds, ...).ValidateOnStart()`. A misconfigured value already fails at startup, not at first query.
- [ ] [Review][Defer] `PartyProcessingSdkReadModel.Records` grows unbounded, one ever-growing JSON blob per party [src/Hexalith.Parties.Projections/Handlers/PartyProcessingActivityFold.cs] — every projection write re-serializes a long-lived party's entire processing history. Real scalability concern but a bigger design change (pagination/archival) than a quick patch — deferred.
- [ ] [Review][Defer] Minor/cosmetic: sequential (not parallel) `GetAsync` calls in `PartyDetailSdkProjectionHandler.ProjectAsync` (doubles state-store round-trip latency on the busiest projection path); `StoreName` null-check duplicated across classes instead of centralized; `PartyErased`'s `LastModifiedAt = erased.ErasedAt` is immediately overwritten by `NormalizeEventTimestamps` (harmless today since both timestamps are equal in current tests, but would silently diverge if they ever differ); `PartyIndexSdkProjectionHandler.Validate(ProjectionRequest,...)` reuses `PartySdkReadModelAddresses.Detail(...)` purely for its validation side effect, coupling Index validation to Detail's address-shape rules.
- [x] [Review][Patch] Remaining test coverage gaps for this group's branches [tests/Hexalith.Parties.Projections.Tests/Handlers/PartySdkProjectionHandlerTests.cs] — Closed 2026-08-05. Re-verified against current source before adding anything: `PartyProcessingActivityFold.Fold` called twice with a non-null `current` was already covered by `ProcessingActivityFold_AppendsAcrossDeliveries`, and the `AlreadyCompleted()` short-circuit was already covered by `DetailHandler_DuplicateDeliveryReturnsAlreadyCompletedAsync` / `IndexHandler_DuplicateDeliveryReturnsAlreadyCompletedAsync` — both landed in a prior session's commit and this finding's text was stale relative to the source it cited; left as-is otherwise. Added the two genuinely still-missing cases: `DeserializeNew_UnknownEventType_DoesNotAdvanceCheckpointAndYieldsNoPayload` (+ a non-JSON sibling) exercises `PartySdkProjectionFold.DeserializeNew` directly rather than only through delivery-failure-level handler tests, asserting `AdvanceCheckpoint == false` and `Payload == null`. `DetailHandler_IncrementalDispatchAcrossTwoDeliveriesMatchesSingleShotRebuildAsync` replaces the self-referential pattern: it runs two separate `ProjectAsync` calls against a new stateful `InMemoryReadModelBatchStore` test double (a real, non-mocked `IReadModelStore`/`IReadModelBatchStore` that persists across calls, unlike the existing NSubstitute stubs) to simulate genuine incremental multi-dispatch persistence, then compares the result to one `PrepareRebuildAsync` call over the full history — two independent code paths converging, not two calls to the same static `Fold` function. (A true ambiguous-short-event-type-name case remains untestable without fabricating two same-named types in the real contracts assembly — `PartyEventTypeResolverTests` already documents this as an unreachable branch in the current type set; only the unknown-type and non-JSON branches were directly testable at the fold level.) `Hexalith.Parties.Projections.Tests` passes 216/216.

**Group 2 chunk re-review (2026-08-04: SDK projection handlers, folds, and Memories adapter; 11 files) findings:**

_Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor. Focused current-source build passed with 0 warnings/errors; 56/56 projection-handler and event-type-resolver tests passed. Sixteen normalized claims were dismissed as intentional platform contracts, pre-existing findings already recorded above, unreachable branches, or unsupported worst-case extrapolations._

- [x] [Review][Decision] Resolve same-ID creation after erasure against the domain identity contract — Resolved 2026-08-04: Administrator chose option 1 — Party IDs remain permanent after erasure. Keep the current aggregate/fold tombstone behavior and correct the stale story language that still requires same-ID recreation.
- [x] [Review][Decision] Define optional Memories convergence after live-write failure and shared-index rebuild — Resolved 2026-08-04: Administrator chose option 2 — require an EventStore-supported durable post-commit reconciliation/outbox path covering failed live notifications and shared rebuild replacement/pruning while keeping Memories optional for local search.
- [x] [Review][Decision] Define Memories erasure routing across configuration changes — Resolved 2026-08-04: Administrator chose option 1 — persist the ingestion `CaseId` with every mapping and keep erasure cleanup registered and available even when Memories indexing is disabled. Preserve backward-compatible handling for legacy mappings that lack the new field.
- [x] [Review][Decision] Map the new `Redacted` processing outcome for consumer privacy UI — Resolved 2026-08-04: Administrator chose option 1 — map `Redacted` to the bounded consumer outcome `Limited`.
- [x] [Review][Patch] Return retryable—not terminal—for unresolved forward-schema events [src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs:187] — Fixed 2026-08-04: detail and index delivery failures preserve the retry ledger for consumer upgrades.
- [x] [Review][Patch] Notify Memories removal whenever `PartyErased` is observed, even when the canonical entry is already absent [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs:223] — Fixed 2026-08-04: the fold records erasure observation independently of prior entry presence.
- [x] [Review][Patch] Make the production detail handler persist the promised failed Art.30 record for non-advancing unresolved events [src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs:64] — Fixed 2026-08-04: an independent optimistic processing-record write retains the failed audit row without advancing the unresolved checkpoint.
- [x] [Review][Patch] Map producer category `PartyCommand` to consumer category `DataChanged` [src/Hexalith.Parties.UI/Services/ConsumerPrivacyProcessingClient.cs:84] — Fixed 2026-08-04.
- [x] [Review][Patch] Assert exact detail and name-history event timestamps instead of comparing two calls to the same fold [tests/Hexalith.Parties.Projections.Tests/Handlers/PartySdkProjectionHandlerTests.cs:78] — Fixed 2026-08-04.
- [x] [Review][Patch] Assert the coordinated processing-slot write and rebuild value, not only the detail operation [tests/Hexalith.Parties.Projections.Tests/Handlers/PartySdkProjectionHandlerTests.cs:57] — Fixed 2026-08-04.
- [x] [Review][Patch] Verify Memories adapter propagation of event type and timestamp into the generated unit [tests/Hexalith.Parties.Tests/Search/PartyMemoryIndexEntrySearchIndexerTests.cs:38] — Fixed 2026-08-04.
- [x] [Review][Patch] Verify the detail handler's two aggregate-owned canonical slot declarations [tests/Hexalith.Parties.Projections.Tests/Handlers/PartySdkProjectionHandlerTests.cs:724] — Fixed 2026-08-04.
- [x] [Review][Patch] Align Story 8.6 erasure guardrails and prior review notes with permanent post-erasure Party IDs [_bmad-output/implementation-artifacts/8-6-projection-and-query-sdk-migration.md:150] — Fixed 2026-08-04: stale recreate language now documents permanent tombstones.
- [x] [Review][Patch] Add durable post-commit Memories reconciliation for failed live notifications and shared-index rebuilds [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs:66] — Fixed 2026-08-04: live failures return retryable and replay notification after canonical no-op; EventStore now retains a bounded rebuild completion manifest and retries idempotent post-commit reconciliation.
- [x] [Review][Patch] Persist Memories mapping `CaseId` and make erasure cleanup independent of indexing enablement [src/Hexalith.Parties/Search/PartyMemoryUnitMappingStore.cs:19] — Fixed 2026-08-04: new mappings retain ingestion CaseId, legacy mappings use the configured fallback, and cleanup services remain registered while indexing is disabled.
- [x] [Review][Patch] Map producer outcome `Redacted` to consumer outcome `Limited` [src/Hexalith.Parties.UI/Services/ConsumerPrivacyProcessingClient.cs:98] — Fixed 2026-08-04.
- [x] [Review][Defer] Whole-payload `json-redacted` events still depend on a resolvable CLR type and can apply a default-valued event produced from `{}` [src/Hexalith.Parties.Projections/Handlers/PartySdkProjectionFold.cs:37] — deferred, pre-existing

**Fresh full-diff re-review (2026-08-04, bmad-code-review, independent pass): 26 files, 2266-line uncommitted diff, findings:**

_Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor (spec + story doc + epic-8-context + story-8-3 matrix + EventStore Story 1.20 packet + the 2026-08-02 identity-authorization-backfill SCP loaded as context). This pass reviewed the diff produced by the Group 2 chunk re-review (2026-08-04) patches above and found issues introduced by or surviving that patch round. Dismissed as noise: `PartyMemoryIndexEntrySearchIndexer.NotifyRemovedAsync` not short-circuiting on `!Enabled` (intentional per the 2026-08-04 Memories-erasure-routing Decision above); `DeliveryFailure` always returning `Retryable` with no operator-visible signal (= the still-open, already-deferred "missing operator logging" item from Group 2); unrelated `sprint_plan.py`/`epics.md` bundling (acknowledged, out of this review's scope by user direction)._

- [x] [Review][Decision] Production `PartyIndexSdkProjectionHandler` now implements `IAsyncDomainSharedProjectionRebuildCompletionHandler`, a type that exists only in the local EventStore submodule checkout's HEAD (`1d6e9321`) — one commit ahead of the recorded gitlink pointer (`7854f8e5`) and outside the Story 8.3-approved `v3.89.0`/`c590590b` identity, with no SCP or owner approval covering this commit. This also breaks the pre-existing, unmodified fitness test `PartiesProjectionRuntimeUsesOnlyEventStoreSdkHandlers` [tests/Hexalith.Parties.Tests/FitnessTests/ArchitecturalFitnessTests.cs:749], which still literal-matches the retired interface name — confirmed RED by direct build+test run. [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs:23] — Resolved 2026-08-04: the Administrator confirmed the pivot to EventStore `v3.91.0` (`1d6e9321acfc416768c1c78e9facf573c9c41f71`, tag `v3.91.0`) is their real, intentional direction — the same real-time-authorization pattern as the 2026-08-02 `v3.89.0` identity-authorization-backfill precedent (`sprint-change-proposal-2026-08-02-story-8-6-eventstore-identity-authorization-backfill.md`). `references/Hexalith.EventStore`'s gitlink already reflects `1d6e9321`/`v3.91.0` (uncommitted). A formal backfill SCP mirroring the 2026-08-02 one is a recommended follow-up but was not authored in this review session. Follow-up patch: update the fitness test to assert the current interface name.
- [x] [Review][Decision] `_bmad-output/implementation-artifacts/sprint-status.yaml` was wholesale-regenerated rather than incrementally edited: it silently drops the `blocked` story status (stories `8-7`/`8-8` flip to `backlog` with all rationale comments lost), truncates roughly 15 story keys mid-word at a fixed-width slug cap, and strips epic/action-item narrative comments — contradicting the epics.md/story-doc narrative that 8.7/8.8 remain gated. Root cause: `.agents/skills/bmad-sprint-planning/scripts/sprint_plan.py`'s `STORY_RANK`/`_slug()`, which never modeled `blocked` and caps key width at 60 chars. [_bmad-output/implementation-artifacts/sprint-status.yaml] — Resolved 2026-08-04: reverted to the prior committed content. Confirmed by diffing every changed key that no legitimate status/content delta was actually pending — Story 8-6's own `review` status was already identical in both versions, and every other changed key was either the `blocked`→`backlog` regression or a pure slug-truncation/re-slug artifact. The generator's `blocked`-status and slug-width bugs remain open in `sprint_plan.py` (not fixed in this session) — flag before the next sprint-planning regeneration of this file.
- [x] [Review][Patch] Erasure verification conflates optional Memories search-index convergence with canonical SDK read-model redaction: `PartySdkReadModelEraser.NotifySearchRemovalAsync` throws after the canonical detail/processing/index batch already committed, and the generic `catch (Exception)` in `AddParties`'s `"sdk-read-models"` cleanup delegate reports the whole store as `Failed` — contradicting the delegate's own adjacent comment ("SDK batch failures are operational... so D15 cannot mis-certify cleanup"). This also duplicates the separate, correctly-scoped `"memories-search"` cleanup delegate, which already reports search-notify status under its own store name. [src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs:198-208; src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:181-208,244-261] — Fixed 2026-08-04: `NotifySearchRemovalAsync` is best-effort again (discards, doesn't throw); `EraseAsync` only fails when the canonical batch itself fails.
- [x] [Review][Patch] Art.30 processing-activity checkpoint can race past a still-unresolved event within the same delivery batch: `PartyProcessingActivityFold.Fold` advances `lastSequence` to the latest advancing event's sequence number with no cap at the first unresolved event, so a batch containing e.g. unresolved-seq-5 + resolvable-seq-6 persists `LastSequenceNumber = 6` on the very first `RecordUnresolvedProcessingAsync` call. Every later retry then calls `DeserializeNew(events, 6)`, which skips seq 5 entirely — its "Failed" Art.30 record is never revisited even after a consumer upgrade resolves the type, undermining the "preserve the retry ledger for consumer upgrades" guarantee this same patch round added. [src/Hexalith.Parties.Projections/Handlers/PartyProcessingActivityFold.cs:67; src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs:194-206] — Fixed 2026-08-04: `Fold` now stops advancing `lastSequence`/`projectedAt` once the first unresolved event is seen in a batch, so a later-sequence resolvable event in the same batch can no longer race the checkpoint past it.
- [x] [Review][Patch] `RecordUnresolvedProcessingAsync` can throw an unhandled `InvalidOperationException` (from `ReadModelWritePolicy.UpdateAsync`'s retry-budget exhaustion after 3 optimistic-concurrency conflicts on the processing key) instead of returning the already-computed `DeliveryFailure` result — no try/catch guards the call. [src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs:72-76,194-206] — Fixed 2026-08-04: the call is now wrapped in a try/catch that swallows `InvalidOperationException` (cancellation still propagates) so retry-budget exhaustion on the audit write can't crash dispatch.
- [x] [Review][Patch] `PartyIndexSdkProjectionHandler.FinalizeAsync` computes the rebuild's "removed parties" set from a single pre-write snapshot of the current index, then writes the rebuilt candidate with unconditional `ReadModelBatchConcurrency.LastWrite` (no CAS). A party created by a live write during the rebuild-accumulation window is absent from the historical candidate but present in that stale snapshot, so `CompleteRebuildAsync` wrongly notifies the search indexer that a still-live party was removed, and the blind last-write can drop its canonical index entry — a new gap in this patch round's rebuild-completion manifest feature, not called out as a known limitation anywhere in this story. [src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs:190-262] — Fixed 2026-08-04 (partial): `CompleteRebuildAsync` now re-reads the index fresh and skips the `NotifyRemovedAsync` call for any party that is still present, closing the search-indexer-facing symptom. The underlying blind `LastWrite` overwrite in `FinalizeAsync` (which can still drop a canonical entry added mid-rebuild) is a deeper concurrency-model question left open — not addressed by this patch.
- [x] [Review][Patch] Story 8.6 Added/Modified File List omits files that are actually in the current diff: `_bmad-output/planning-artifacts/epics.md`, `.agents/skills/bmad-sprint-planning/scripts/sprint_plan.py`, and its test file — the exact gap the still-open Epic 6/7 "pre-review File List check" retro action item exists to catch. — Fixed 2026-08-04: File List updated; `sprint-status.yaml` also removed from the list since it was reverted to HEAD (Decision above) and is no longer part of the diff.
- [x] [Review][Patch] `ARCHITECTURE-SPINE.md`'s erasure bullet (~line 90-94) was not updated to reflect the 2026-08-04-resolved "permanent Party ID, no reuse after erasure" contract, even though this story doc cites the spine as the binding `[Source: ...]` for that exact statement — the two docs now disagree. [_bmad-output/planning-artifacts/architecture/epic-8-domain-focus-2026-07-06/ARCHITECTURE-SPINE.md] — Fixed 2026-08-04: I10 now states Party identifiers remain permanently unavailable for reuse after erasure.
- [x] [Review][Patch] Fitness test still literal-matches the retired `IAsyncDomainSharedProjectionRebuildHandler` interface name; update it to assert the current `IAsyncDomainSharedProjectionRebuildCompletionHandler`, consistent with the resolved EventStore v3.91.0 Decision above. [tests/Hexalith.Parties.Tests/FitnessTests/ArchitecturalFitnessTests.cs:749] — Fixed 2026-08-04.
- [x] [Review][Defer] `PartyIndexSdkProjectionHandler.BuildReconciliationFold` recomputes `LastIndexedEvent` via a separate code path from the canonical `FoldCore`, used only on the already-confirmed idempotent-no-op/reconciliation branch — could pick a different "last event" for search-reconciliation notification metadata on a multi-event no-op batch, but doesn't affect canonical read-model correctness — deferred, low-severity, needs a dedicated multi-event test
- [x] [Review][Defer] `PartyMemoryCleanupService`'s new "no persisted CaseId and no fallback configured" blocked branch has zero test coverage — deferred, add a `PartyMemoryCleanupServiceTests` case
- [x] [Review][Defer] `PartyIndexSdkProjectionHandler.CompleteRebuildAsync` would throw `NullReferenceException` (not a controlled result) if a persisted rebuild-completion manifest ever deserializes with null `Entries`/`RemovedPartyIds` — not reachable under the current producer (`FinalizeAsync` always serializes non-null arrays), hardening-only — deferred, pre-existing class of defensive-coding gap
- [x] [Review][Defer] Full-suite verification (run after applying the patches above) surfaced `PlatformApiPrerequisitesTests.Matrix_ValidationEvidenceCommandsAreReproducible` RED: it hard-pins the Story 8.3 matrix's "Payload protection engine package" (G5) row to EventStore `v3.89.0`/`7854f8e5`, but the working tree is now at `v3.91.0`/`1d6e9321` (this story's resolved EventStore identity Decision above). Pre-existing to this review session — the submodule was already at this commit in the working tree before this review started (confirmed against the session's initial `git status`) — not caused by any of this review's patches. Out of Story 8.6 scope per the frozen spec's "Never...pull Story 8.7 crypto extraction into scope" rule: the G5 payload-protection row is Story 8.7's territory and needs its own owner-reviewed identity-authorization update (mirroring the 2026-08-02 SCP pattern), not a Story 8.6 patch. [tests/Hexalith.Parties.Tests/FitnessTests/PlatformApiPrerequisitesTests.cs:18,1074-1090; _bmad-output/implementation-artifacts/story-8-3-platform-api-prerequisite-matrix.md]

**DI + query host sub-chunk re-review (2026-08-09: `PartiesServiceCollectionExtensions.cs` vs `2c4a7af`, File List scope) findings:**

_Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor. Acceptance Auditor: no AC violations for this chunk. Dismissed as noise: Program-owned `IDomainQueryHandler` discovery; optional eraser indexer default while `IPartyIndexSearchIndexer` is registered; tenant-wide `EvictIndex` (index key is tenant-scoped); empty-mapping `Cleaned` when Memories is off (P38 / always-on cleanup); null Endpoint / empty CaseId already bounded inside `PartyMemoryCleanupService`; Evict* dictionary ops not realistically throwing; `TryAddSingleton(configuration)` required by EventStore registration helpers; missing dedicated `IValidateOptions<PartySdkReadModelOptions>` type (inline `.Validate` is enough)._

- [x] [Review][Decision] Memories cleanup HttpClient freezes Endpoint/ApiToken from the boot `memorySearch` snapshot while CaseId is deliberately read live from `IOptionsMonitor` — Resolved 2026-08-09: Administrator chose option 1 — keep boot-freeze for Endpoint/ApiToken (matches typed HttpClient / MemoriesClient); document that those two require restart while CaseId remains live as legacy-mapping fallback only.
- [x] [Review][Patch] Document that Memories cleanup Endpoint/ApiToken are boot-frozen (restart required) while CaseId is live via `IOptionsMonitor` [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:293] — Fixed 2026-08-09: configure-callback comment documents boot-freeze vs live CaseId.
- [x] [Review][Patch] Register `PartySdkLastKnownReadModelCache` with injected `TimeProvider` so `TryAddSingleton(TimeProvider.System)` overrides reach LKG retention [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:170] — Fixed 2026-08-09: added `TimeProvider`-only constructor preferred by DI over the parameterless `TimeProvider.System` path.
- [x] [Review][Patch] Log exception type (not message/PII) when `sdk-read-models` cleanup maps failures to `Failed` [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:200] — Fixed 2026-08-09: Warning log with `{ExceptionType}` only.
- [x] [Review][Patch] Update stale `actor-host` comments after projection/query actors were removed from this composition [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:106] — Fixed 2026-08-09: comments now describe domain-service host / projection-side lookups.
- [x] [Review][Patch] Strengthen `AddParties` DI tests: resolve `PartySdkQueryService` and `IQueryCursorCodec` (with DataProtection), pin `PartySdkReadModelOptions` ValidateOnStart failures, and assert canceled `sdk-read-models` cleanup rethrows [tests/Hexalith.Parties.Tests/Projections/ProjectionPlatformAdapterTests.cs:25] — Fixed 2026-08-09: three new tests; descriptor assert also requires `PartySdkQueryService` / LKG cache. Focused class 7/7.
- [x] [Review][Patch] Refresh `AssertApprovedStory85DiffIsNarrow` expectations for the post-8.6 DI surface (no AggregateActor / projection hosted services / DaprDomainServiceInvoker) [tests/Hexalith.Parties.Tests/FitnessTests/PlatformApiPrerequisitesTests.cs:924] — Fixed 2026-08-09: asserts SDK read-model/cursor/DataProtection/query/`TimeProvider` registrations and absence of retired projection runtime types.
- [x] [Review][Patch] Bound unexpected non-cancel exceptions from the always-on `memories-search` erasure cleanup delegate to `Failed` [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:247] — Fixed 2026-08-09: try/catch mirrors sdk-read-models (type-only log + Failed).
- [x] [Review][Patch] Honor cancellation on the `projection-cache` eviction cleanup delegate before reporting `Cleaned` [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:213] — Fixed 2026-08-09: `cancellationToken.ThrowIfCancellationRequested()` before Evict*.
- [x] [Review][Defer] `tests/e2e/specs/story-7-4-projection-platform-compatibility.spec.ts` still expects deleted projection-adapter registrations and old test method names — deferred, pre-existing e2e drift outside this DI chunk
- [x] [Review][Defer] Erasure cleanup timestamps still use `DateTimeOffset.UtcNow` instead of the newly registered `TimeProvider` — deferred, pre-existing certificate timestamp pattern

### Story Classification and Gate

- Epic 8 is Class C post-MVP maintenance. It must not be reported as new PRD functional delivery. [Source: `_bmad-output/planning-artifacts/epics.md#Epic-8-Domain-Focus-Refactoring-and-Platform-Extraction-Class-C`]
- Story 8.6 follows completed stories 8.1 through 8.5 and precedes 8.7 through 8.10 in the deletion-heavy sequence. Each remaining story must satisfy the Epic 8 architecture spine readiness gate before a dev session. [Source: `_bmad-output/implementation-artifacts/sprint-status.yaml#Epic-8-Domain-Focus-Refactoring-and-Platform-Extraction-Class-C`]
- The Story 8.6 draft spec is explicitly `blocked-prerequisite` and says source migration must halt while the Story 8.3 projection/query SDK row remains `needs-additive-api`. [Source: `_bmad-output/implementation-artifacts/spec-8-6-projection-and-query-sdk-migration.md#Boundaries--Constraints`]
- The matrix row currently records source surfaces but still requires additive or approved parity proof for read-model erasure hooks, index batching, freshness mapping, duplicate/out-of-order replay, rebuild verification, and cursor scope compatibility. [Source: `_bmad-output/implementation-artifacts/story-8-3-platform-api-prerequisite-matrix.md#EventStore-projectionquery-SDK`]

### Required Source Discovery Results

- Loaded persistent project rules from `_bmad-output/project-context.md` and referenced submodule project-context files. Critical rules include .NET 10, C# 14, `.slnx` only, Central Package Management, warnings-as-errors, xUnit v3 with Microsoft Testing Platform, Dapr package pin discipline, and root-declared submodule discipline.
- Loaded Epic 8 story requirements from `_bmad-output/planning-artifacts/epics.md`, Story 8.6 draft spec, Epic 8 context, and the Epic 8 architecture spine.
- Loaded previous-story intelligence from Story 8.5. The host already uses `AddEventStoreDomainService` and `UseEventStoreDomainService`; `AddEventStoreProjectionRuntimeCompatibility` stayed because projection/query/rebuild migration is deferred to this story.
- Loaded current projection/query implementation files before story creation: projection handlers, Dapr projection actors, query actors, actor extensions, projection rebuild service, projection platform adapters, health checks, `Program.cs`, and service registrations.
- Inspected target EventStore SDK surfaces in the current submodule pin: `IDomainProjectionHandler`, `IDomainQueryHandler`, `IReadModelStore`, `ReadModelWritePolicy`, and `IQueryCursorCodec`.

### Architecture and Domain Guardrails

- Domain focus is binding: Parties owns party aggregate rules, party projection fold semantics, query shape, tenant guardrails, GDPR semantics, and user-visible freshness; generic projection/query mechanics belong to EventStore SDK after proof. [Source: `_bmad-output/planning-artifacts/architecture/epic-8-domain-focus-2026-07-06/ARCHITECTURE-SPINE.md#Invariants--Rules`]
- Gateway boundary is binding: public command/query ingress remains through EventStore gateway with `Domain="party"`; the Parties host is not a public API host. DAPR service invocation stays deny-by-default and scoped to `eventstore -> POST /process`. [Source: `_bmad-output/planning-artifacts/architecture/epic-8-domain-focus-2026-07-06/ARCHITECTURE-SPINE.md#Boundary-Decisions`]
- Rollback is binding: no local actor, adapter, rebuild, or fallback code is deleted until a replacement path is proven and the rollback set can be restored by DI/submodule pointer or commit revert. [Source: `_bmad-output/planning-artifacts/architecture/epic-8-domain-focus-2026-07-06/ARCHITECTURE-SPINE.md#Story-Readiness-Gate`]
- Read behavior is binding: stale, degraded, rebuilding, unavailable, and local-only freshness states must continue to produce last-known data and `ProjectionFreshnessMetadata`; staleness is not an exception path.
- Erasure behavior is binding: erased parties are removed from the index, detail reads expose only PII-free tombstone semantics, and Party identifiers remain permanently unavailable for reuse after erasure. Sequence/checkpoint state must preserve that tombstone under delayed or same-ID events.

### Current Implementation Map

- Projection folds to keep: `src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs` and `src/Hexalith.Parties.Projections/Handlers/PartyIndexProjectionHandler.cs`.
- Query semantics to keep: `src/Hexalith.Parties/Queries/PartyDetailProjectionQueryActor.cs`, `src/Hexalith.Parties/Queries/PartyIndexProjectionQueryActor.cs`, and `src/Hexalith.Parties/Queries/IPartyProjectionQueryActor.cs`.
- Actor mechanics to replace after parity: `src/Hexalith.Parties.Projections/Actors/PartyDetailProjectionActor.cs`, `src/Hexalith.Parties.Projections/Actors/PartyIndexProjectionActor.cs`, and their interfaces/event resolver.
- Rebuild mechanics to replace after parity: `src/Hexalith.Parties.Projections/Services/ProjectionRebuildService.cs`, `IProjectionRebuildService.cs`, rebuild scope/checkpoint types, and index manifest fallback behavior.
- Adapter mechanics to delete after parity: `IPartyProjectionPlatformAdapter`, `LocalPartyProjectionPlatformAdapter`, `EventStorePartyProjectionPlatformAdapter`, `PartyProjectionPlatformFreshness`, and `PartyProjectionPlatformAdapterMode`.
- Host wiring to update carefully: `PartiesServiceCollectionExtensions.cs`, `Program.cs`, erasure cleanup delegates, projection health checks, and `PartyProjectionUpdateOrchestrator`.

### SDK Surface Notes

- `IDomainProjectionHandler` is a stateless full-replay projection handler surface. If it cannot persist or merge multi-read-model state by itself, use the approved `IReadModelStore` and `ReadModelWritePolicy` path rather than inventing a local platform store. [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/IDomainProjectionHandler.cs`]
- `IDomainQueryHandler` is the target for query path execution. Keep `Domain="party"` and existing query type names. [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/IDomainQueryHandler.cs`]
- `ReadModelWritePolicy` relies on idempotent transforms under optimistic retry. Every transform in this story must be safe under duplicate replay. [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/ReadModelWritePolicy.cs`]
- `IQueryCursorCodec` uses an opaque DataProtection cursor with query type, scope, and position. Story 8.6 must prove cursor purpose stability and DAPR key-ring persistence before replacing cursor assumptions. [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Queries/IQueryCursorCodec.cs`]

### Previous Story Intelligence

- Story 8.5 established the SDK host shape but intentionally retained projection/rebuild compatibility services. Do not remove those services at the start of 8.6; remove them only after this story's parity evidence is green. [Source: `_bmad-output/implementation-artifacts/spec-8-5-eventstore-domain-service-sdk-host-cutover.md#Review-Fix-Plan`]
- `PartyDomainProcessor` was registered for multiple `party` casing variants because the SDK keyed lookup was exact-match. Keep domain casing behavior covered by regression tests when routing projections/queries through SDK handlers.
- SDK `/query`, `/project`, `/replay-state`, and metadata endpoints are in-process host capabilities after 8.5; they are not permission to widen public DAPR service invocation. [Source: `src/Hexalith.Parties/Program.cs`]
- The projection rollback action item remains open until Story 8.6 records parity, processing-record reads, rebuild-vs-replay, and rollback evidence. [Source: `_bmad-output/implementation-artifacts/sprint-status.yaml#open-actions`]

### Technical Stack and Current External Facts

- Local project rules win over external versions: use .NET SDK `10.0.302`, `net10.0`, C# 14, Dapr packages pinned by the repo, Fluent UI Blazor pinned by the repo, xUnit v3, Shouldly, NSubstitute, and Microsoft Testing Platform.
- Official .NET 10 release notes list SDK `10.0.302` as the July 2026 servicing SDK and .NET 10 LTS support through November 14, 2028; do not upgrade as part of this story. [Source: https://github.com/dotnet/core/blob/main/release-notes/10.0/README.md]
- Dapr official support policy supports the current and previous two minor versions; Dapr 1.18 is current-era, but this repo's Dapr package pin remains authoritative and must not be independently bumped. [Source: https://docs.dapr.io/operations/support/support-release-policy/]
- xUnit v3 supports Microsoft Testing Platform and direct test executable execution; use the repo's direct EXE approach for focused `-class` runs because `dotnet test --filter` can silently run zero tests in this workspace. [Source: https://xunit.net/docs/getting-started/v3/microsoft-testing-platform]

### Project Structure Notes

- Keep source in existing projects; do not create a new platform project in Parties for generic projection/query infrastructure.
- Keep public contracts in contracts/client projects unchanged unless the parity harness proves a compatible additive change is required.
- Do not introduce package `Version=` attributes in `.csproj` files; package versions belong in `Directory.Packages.props`.
- Do not introduce a classic `.sln`; this repo uses `.slnx`.
- Do not edit submodules unless the story explicitly records owner approval and a root gitlink update as story work.

### Testing and Validation Guidance

Use direct xUnit v3 assembly execution for focused tests after building the test projects. Example commands to adapt during implementation:

```bash
git -C references/Hexalith.EventStore rev-parse HEAD
rg -n -F "EventStore projection/query SDK" _bmad-output/implementation-artifacts/story-8-3-platform-api-prerequisite-matrix.md
dotnet build tests/Hexalith.Parties.Tests/Hexalith.Parties.Tests.csproj -c Debug -p:UseHexalithProjectReferences=true -p:UseNuGetDeps=false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0 --verbosity minimal
dotnet build tests/Hexalith.Parties.Projections.Tests/Hexalith.Parties.Projections.Tests.csproj -c Debug -p:UseHexalithProjectReferences=true -p:UseNuGetDeps=false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0 --verbosity minimal
dotnet ./tests/Hexalith.Parties.Projections.Tests/bin/Debug/net10.0/Hexalith.Parties.Projections.Tests.dll
dotnet ./tests/Hexalith.Parties.Tests/bin/Debug/net10.0/Hexalith.Parties.Tests.dll -class Hexalith.Parties.Tests.Gateway.PartyDetailProjectionQueryActorTests -class Hexalith.Parties.Tests.Gateway.PartyIndexProjectionQueryActorTests -class Hexalith.Parties.Tests.Projections.ProjectionRebuildServiceTests -class Hexalith.Parties.Tests.Projections.ProjectionPlatformAdapterTests -class Hexalith.Parties.Tests.Gateway.TenantSafeProjectionReadGuardrailsTests
pwsh scripts/test.ps1 -Lane unit
pwsh scripts/test.ps1 -Lane topology
bash scripts/check-no-warning-override.sh
git diff --check
```

Add direct `-class` runs for the new SDK parity harness. If topology or Docker-backed checks skip, record the skip as environment-limited evidence, not as a passing release gate.

### Rollback Plan

- Before deletion: rollback is the retained actor/query/rebuild/adapter registration set. If SDK parity fails, keep local projection/query mechanics and leave this story blocked.
- After deletion: rollback is a targeted revert of the migration/deletion commit plus a root `references/Hexalith.EventStore` pointer rollback if the SDK pin caused the issue.
- Data rollback is not allowed. Rebuild verification must prove persisted detail and index read models can be regenerated from aggregate replay without losing erased-party exclusion, processing records, or freshness semantics.
- Public routing rollback must preserve EventStore gateway ingress and the deny-default EventStore-only exact-route DAPR ACL (including `/process` plus the Story 8.6 SDK query/projection/rebuild/discovery routes), not a return to a public Parties API.

### References

- [Source: `_bmad-output/planning-artifacts/epics.md#Story-8.6-Projection-and-query-SDK-migration`]
- [Source: `_bmad-output/implementation-artifacts/spec-8-6-projection-and-query-sdk-migration.md`]
- [Source: `_bmad-output/implementation-artifacts/epic-8-context.md`]
- [Source: `_bmad-output/implementation-artifacts/story-8-3-platform-api-prerequisite-matrix.md#EventStore-projectionquery-SDK`]
- [Source: `_bmad-output/planning-artifacts/architecture/epic-8-domain-focus-2026-07-06/ARCHITECTURE-SPINE.md#Story-Readiness-Gate`]
- [Source: `_bmad-output/implementation-artifacts/spec-8-5-eventstore-domain-service-sdk-host-cutover.md`]
- [Source: `_bmad-output/project-context.md`]
- [Source: `src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs`]
- [Source: `src/Hexalith.Parties.Projections/Handlers/PartyIndexProjectionHandler.cs`]
- [Source: `src/Hexalith.Parties/Queries/PartyDetailProjectionQueryActor.cs`]
- [Source: `src/Hexalith.Parties/Queries/PartyIndexProjectionQueryActor.cs`]
- [Source: `src/Hexalith.Parties.Projections/Services/ProjectionRebuildService.cs`]
- [Source: `src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs`]
- [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/IDomainProjectionHandler.cs`]
- [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/IDomainQueryHandler.cs`]
- [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/IReadModelStore.cs`]
- [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/ReadModelWritePolicy.cs`]
- [Source: `references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Queries/IQueryCursorCodec.cs`]

## Validation Summary

- Source discovery loaded the Epic 8 planning artifacts, Story 8.6 draft spec, Story 8.3 matrix, Epic 8 architecture spine, previous Story 8.5 evidence, current projection/query source files, current sprint status, project-context rules, recent git history, and current EventStore submodule pin.
- Checklist fixes applied before finalizing: made the `needs-additive-api` gate explicit, required matrix proof before source changes, preserved rollback-only projection/query files until parity and rebuild evidence are recorded, required cursor compatibility proof, required full rebuild versus aggregate replay, and scoped the story away from crypto, client/MCP/AppHost/deploy, and UI work.
- Latest technical review found no dependency upgrade requirement. Current official .NET, Dapr, and xUnit/MTP information was checked only to confirm that repo pins and direct xUnit executable guidance remain appropriate.

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Implementation Plan

1. Add red architectural fitness assertions for the EventStore operational-index metadata discovery route in both `Program.cs` documentation and the deny-default DAPR ACL.
2. Add the one exact EventStore-only POST ACL operation required by the frozen Story 8.6 spec while preserving unchanged public/gateway behavior and rejecting wildcard or peer ingress.
3. Run focused architecture tests, canonical package-mode unit/CI/topology lanes, the Release solution build, and static guardrails; keep the story in progress if any full regression gate remains non-green.
4. Isolate the encryption fixture from the production DAPR-backed key-retry scheduler removed from its former actor-proxy test seam, then rerun the focused fixture and every canonical lane.
5. Advance to review only after all 15 projects, the Release solution build, static guardrails, and the manual File List reconciliation are green.

### Debug Log References

- 2026-07-09T13:25:25+02:00 - Loaded sprint status and selected requested story `8-6-projection-and-query-sdk-migration` from `_bmad-output/implementation-artifacts/sprint-status.yaml`.
- 2026-07-09T13:25:25+02:00 - Read `_bmad-output/implementation-artifacts/story-8-3-platform-api-prerequisite-matrix.md`; the `EventStore projection/query SDK` row still has status `needs-additive-api`.
- 2026-07-09T13:25:25+02:00 - Ran `git -C references/Hexalith.EventStore rev-parse HEAD`; current pin is `0f428d0c914f2151aab15bb262f956a9630041dc`, matching `eventstore_pin_at_creation`.
- 2026-07-09T13:25:25+02:00 - Halted before production source edits per AC1 and the story block-if rule. No submodule update/init command was run.
- 2026-07-16T01:04:55+02:00 - Re-read the complete prerequisite matrix; the `EventStore projection/query SDK` row remains `needs-additive-api` and explicitly records that no Story 1.20 owner-approved `available` decision exists.
- 2026-07-16T01:04:55+02:00 - Re-read the EventStore sprint status and Story 1.20: Story 1.19 remains `review`, Story 1.20 remains `ready-for-dev`, and the required `1-20-owner-approved-parity-closure-proof-packet.md` is absent.
- 2026-07-16T01:04:55+02:00 - Verified the root EventStore gitlink is `82ed167c1c78d4ff50d3f8eab43850bb6abd0fe7` while the pre-existing checkout is `97c335cc5685928166914e6b7725502b8017de8b`; the mismatch is not approved consumption identity proof. Halted without production source edits, tests, or submodule commands.
- 2026-08-01T11:27:47+02:00 - Re-read the complete prerequisite matrix; the `EventStore projection/query SDK` row is now `available` and authorizes only exact source SHA `fa2d1c9910f8976553adb33dcdb1c9ff2ea75594`.
- 2026-08-01T11:27:47+02:00 - The starting root gitlink and clean EventStore checkout were both `c590590bc581a3f72ef6e67148eda988ba4b8fe6`. Created dependency-only checkpoint `a377c28b51d17690068ff0236744152a481d0172` at the approved SHA after the identity check failed red.
- 2026-08-01T11:27:47+02:00 - Ran the owner packet's full A/B/C verifier and source-consumer procedure in one shell using evidence commit A `b695ad3215cd873c41561635e4eb4d7ff29d56a2`, pointer commit B `ed48057e9bf9cb5e5e8667fec84f7c70e4534eea`, and authorization commit C `1b219d39cfa8f0349175c356001ba539bfb4aa92`. Verification failed closed before the consumer procedure: `raw_evidence_bundle_retention_until` is `2033-08-01T00:00:00Z`, which is less than seven years from the fresh verification time.
- 2026-08-01T11:27:47+02:00 - Restored the pre-story EventStore identity `c590590bc581a3f72ef6e67148eda988ba4b8fe6` with Conventional Commit `f5058f7` after the source-authorization gate failed, leaving no net gitlink drift.
- 2026-08-01T11:27:47+02:00 - Halted before production source edits, tests, or Aspire startup. The EventStore owner must refresh the immutable-evidence retention proof and A/B/C authorization chain before the exact source receipt can pass.
- 2026-08-01T12:37:42+02:00 - Extended the exact raw-evidence blob's locked WORM retention from `2033-08-01T00:00:00Z` to `2036-08-02T00:00:00Z` without changing blob URL, version `2026-07-26T10:36:02.8785061Z`, or SHA-256 `76d9d02e9d75017f5d2b952d36c76e243968f037739a56c3ed18e34be3bf68ec`; published refreshed provider proof SHA-256 `1d1c12c45aef2e77305e26d2315c715be9cae47372ab312aabb583bf475bc8c4`.
- 2026-08-01T12:37:42+02:00 - Published and pushed the history-preserving refreshed owner chain: A `21997d1974c4bc7022c77a5065edd9d327435c97`, B `55471ad752e49686c7d0a47159f25455fda24003`, C `dbf81916ac56ceebf8cda313089be86e40d96c98`, merged to EventStore `main` as `77d6f47743453d542d96dbe088d5eef7cd05284b`. The owner verifier and 13 focused proof-integrity tests passed.
- 2026-08-01T12:37:42+02:00 - Created dependency-only checkpoint `e65e8b5e9a1d202f240bb641490e7747a84a2da1` at exact authorized source `fa2d1c9910f8976553adb33dcdb1c9ff2ea75594`; the refreshed A/B/C verifier and source-consumer procedure passed in the same shell with `verified_source_consumer_handoff=passed`.
- 2026-08-01T12:37:42+02:00 - Restored the focused projection test project in source mode, then its build failed with 11 CS0246 errors: the authorized source does not define `IAsyncDomainSharedProjectionRebuildHandler`, `DomainSharedProjectionRebuildIdentity`, or `DomainSharedProjectionRebuildCandidate`, which the current tenant-shared Party index SDK handler requires for accumulation, complete replacement, and stale-entry pruning. Those surfaces first appear in unauthorized descendant `e92ae66866d68842c3551b9709df5e81eb05b08c`.
- 2026-08-01T12:37:42+02:00 - Restored the pre-story EventStore identity `c590590bc581a3f72ef6e67148eda988ba4b8fe6` with rollback commit `64af3bc1ec7a39a83883401b19c5d0578530ca7f`, leaving the dependency checkpoints net-zero. Halted before new production edits, parity/deletion work, or Aspire startup; all local rollback paths remain intact.
- 2026-08-01T14:20:00+02:00 - Resumed under the user's explicit direction to use the latest EventStore release. Verified root gitlink, checkout, and exact tag `v3.89.0` all select `c590590bc581a3f72ef6e67148eda988ba4b8fe6`; the formerly missing tenant-shared rebuild surfaces are present.
- 2026-08-01T14:20:00+02:00 - Rebound canonical detail, processing-record, and tenant-shared index projections to SDK read-model/rebuild paths; migrated all eight query discriminators to SDK handlers with protected cursor scope and tenant-scoped last-known degraded reads; removed the retired projection/query actors, rebuild service, platform adapters, and actor health check.
- 2026-08-01T14:20:00+02:00 - Latest source-mode projection consumer build passed with 0 warnings and 0 errors; direct projection execution passed 150/150. Focused query/health/composition/architecture execution passed 48/48.
- 2026-08-01T14:20:00+02:00 - Broad Parties execution reported 452 total, 449 passed, and only the three pre-existing payload-protection matrix failures. The integration project compiled with 0 warnings/errors after its EventStore server fixture dependency became explicit; focused execution stopped during host construction on missing mixed-graph `Hexalith.Commons.Http, Version=2.29.0.0` and received no pass credit.
- 2026-08-01T14:20:00+02:00 - `git diff --check` and `scripts/check-no-warning-override.sh` passed. Production searches found no `NotImplementedException`, retired projection actor/rebuild/adapter runtime types, or production EventStore.Server reference.
- 2026-08-01T15:55:59+02:00 - Reconciled the older `/process`-only wording with the later frozen Story 8.6 spec: gateway/public behavior remains unchanged, while deny-default DAPR service invocation admits only EventStore and exact internal POST SDK routes. EventStore's operational-index hosted service requires `/admin/operational-index-metadata` to discover Party query and named-projection handlers.
- 2026-08-01T15:55:59+02:00 - Added red architectural fitness assertions for metadata-route documentation and ACL admission (2/2 failed), then documented the route in `Program.cs`, added its exact EventStore-only POST ACL operation, and reran the two tests plus the full `ArchitecturalFitnessTests` class green (2/2 and 21/21).
- 2026-08-01T15:55:59+02:00 - Canonical package-mode unit lane passed 1660/1660; CI lane passed 31/31; the Release solution build passed with 0 warnings and 0 errors; Story 8.6-scoped `git diff --check` and `scripts/check-no-warning-override.sh` passed.
- 2026-08-01T15:55:59+02:00 - Full regression completion remains blocked: the Parties assembly remains 449/452 on the three pre-existing G5 prerequisite-matrix failures, and the topology lane reported 26 passed, 6 explicitly skipped, and 5 encryption-fixture failures because DAPR actor calls to `localhost:3500` were refused. Source-mode unit validation additionally built/passed 8 projects while three mixed-version/source-graph projects failed outside Story 8.6. The unchecked task and story status were preserved per the dev-story completion gate.
- 2026-08-01T15:55:59+02:00 - The approved file-list gate could not run: `python3 _bmad/scripts/check_file_list.py --story _bmad-output/implementation-artifacts/8-6-projection-and-query-sdk-migration.md --require-file-list` exited 2 because `_bmad/scripts/check_file_list.py` is absent. Manually added the three files changed in this continuation to the story File List; unrelated concurrent worktree edits remain excluded.
- 2026-08-01T16:14:29+02:00 - Resumed from clean `main` at `31a2f37`. The Release `Hexalith.Parties.Tests` build passed with 0 warnings/errors, and direct execution of `ArchitecturalFitnessTests` passed 21/21, confirming the Story 8.5 SDK host shape and exact deny-default EventStore-only POST ACL routes remain intact.
- 2026-08-01T16:14:29+02:00 - Canonical package-mode `pwsh scripts/test.ps1 -Lane unit -ContinueOnFailure -Properties NuGetAudit=false,MinVerVersionOverride=1.0.0` passed all 11 projects and 1660/1660 tests.
- 2026-08-01T16:14:29+02:00 - `pwsh scripts/test.ps1 -Lane integration -ContinueOnFailure -Properties NuGetAudit=false,MinVerVersionOverride=1.0.0` remained red: Sample passed 58/58, while Parties passed 449/452. The same three `PlatformApiPrerequisitesTests` failed on the Story 8.7/G5 payload-protection evidence (`Matrix_ValidationEvidenceCommandsAreReproducible`, `Matrix_EvidencePathsExistAndMatchDeclaredOwner`, and `Matrix_ValidationEvidenceNamesExpectedSymbols`). Stopped at the regression gate without changing production source, marking the remaining subtask complete, or moving the story from `in-progress`.
- 2026-08-01T17:38:55+02:00 - Revalidated the host/ACL focus at the current EventStore root gitlink and checkout `8f004ecf8be271ec5e76f97f944c1fc1154391c7`: the Release test build completed with 0 warnings/errors and `ArchitecturalFitnessTests` passed 21/21.
- 2026-08-01T17:38:55+02:00 - The first full all-lanes pass reached 14/15 projects: concurrently refreshed G5 matrix evidence made `Hexalith.Parties.Tests` green at 452/452, while five encryption-fixture tests exposed a Story 8.6 regression. Removing the retired projection actor proxy setup had also removed the fixture's only DAPR isolation, so the production `ActorBackedPartyKeyRetryScheduler` attempted `localhost:3500`.
  - **Reconciliation note (added by code review, 2026-08-02):** the 449/452 → 452/452 change between the prior entry and this one is not Story 8.6 work. Two things happened concurrently, between them: (1) the EventStore root gitlink independently drifted from `c590590bc581a3f72ef6e67148eda988ba4b8fe6` to `8f004ecf8be271ec5e76f97f944c1fc1154391c7` (and has since drifted further, to `30810727cb91f5886cb2aa13601680a23b18bcc0` as of this review); (2) concurrent, unrelated Story 8.7 work refreshed the G5 payload-protection matrix evidence that the three previously-failing `PlatformApiPrerequisitesTests` checked. Story 8.6's own consumer-parity work was validated at `c590590b` (449/452, the three failures being the pre-existing G5 gap, not a Story 8.6 regression); the subsequent 452/452 all-green claim in the Completion Notes reflects the tree state at whatever EventStore pin was current at that later timestamp, not a re-run specifically at `c590590b`.
- 2026-08-01T17:38:55+02:00 - Registered a substitute `IPartyKeyRetryScheduler` in `EncryptionTestFactory` without restoring retired projection actors. The focused Release build completed with 0 warnings/errors and all six `EncryptionPipelineIntegrationTests` passed.
- 2026-08-01T17:38:55+02:00 - Final `pwsh scripts/test.ps1 -Lane all -ContinueOnFailure -Properties NuGetAudit=false,MinVerVersionOverride=1.0.0` exited 0: all 15 projects passed; Parties passed 452/452, Sample passed 58/58, Integration passed 31 with 6 explicit deferred-health skips, and CI passed 31/31.
- 2026-08-01T17:38:55+02:00 - Final Release solution build passed with 0 warnings and 0 errors. `git diff --check`, `bash scripts/check-no-warning-override.sh`, and production searches for `NotImplementedException` and retired projection/query runtime types passed.
- 2026-08-01T17:38:55+02:00 - `_bmad/scripts/check_file_list.py` remains absent, so the automated File List gate could not run. Manual reconciliation confirmed the Story 8.6 continuation files are listed; concurrent G5 matrix/test edits, FrontComposer gitlink drift, and the untracked CI prerequisite spec remain excluded.
- 2026-08-04 - Coordinated EventStore-source validation passed, but the canonical package-mode projection build is intentionally not green until the additive EventStore API is released and consumed: `dotnet build src/Hexalith.Parties.Projections/Hexalith.Parties.Projections.csproj --no-restore -nr:false -p:UseHexalithProjectReferences=false -p:HexalithEventStoreFromSource=false` fails with CS0246 for `IAsyncDomainSharedProjectionRebuildCompletionHandler`. Do not advance the story or package-mode gate until that owner handoff occurs.
- 2026-08-04T23:00:08+02:00 - **Package-handoff blocker resolved (recorded by `sprint-change-proposal-2026-08-04.md`):** the additive EventStore API landed at `v3.91.0` (`1d6e9321acfc416768c1c78e9facf573c9c41f71`), matching Builds `824d7ef100455423aabbcd399c8364074000b2e0` (`HexalithEventStoreVersion=3.91.0`). A current package-mode restore and Release build of the narrow projection project completed with zero warnings and zero errors:
  ```text
  dotnet restore src/Hexalith.Parties.Projections/Hexalith.Parties.Projections.csproj -p:UseHexalithProjectReferences=false -p:HexalithEventStoreFromSource=false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0
  dotnet build src/Hexalith.Parties.Projections/Hexalith.Parties.Projections.csproj --configuration Release --no-restore -nr:false -m:1 -p:UseHexalithProjectReferences=false -p:HexalithEventStoreFromSource=false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0
  ```
  This closes the package-handoff sub-block only. Story 8.6 status is corrected from `done` back to `in-progress` because the earlier unresolved diagnostic-logging, corrupt-vs-redacted observability, rebuild/live-write concurrency, and remaining parity-test findings noted below were never part of the approved twelve-patch set and remain open. This entry does not authorize or imply anything about Story 8.7 or the G5 shared payload-protection provider, which remain `blocked` / `needs-additive-api`.
- 2026-08-05 - Re-read the full story file and the frozen spec per the outstanding Group 2 diagnostic-logging, corrupt-vs-redacted, rebuild-concurrency, and test-coverage findings above. Verified current submodule state first: root gitlink and checkout both `b140a57699a619a69bd01b1fd4fee50338153f7d` (`v3.91.0-1-gb140a576`), working tree otherwise clean.
- 2026-08-05 - Re-verified two of the four items under "Remaining test coverage gaps" against current source before writing anything: `ProcessingActivityFold_AppendsAcrossDeliveries` and `DetailHandler_DuplicateDeliveryReturnsAlreadyCompletedAsync`/`IndexHandler_DuplicateDeliveryReturnsAlreadyCompletedAsync` already exist and already exercise the two claims the finding said were "still missing" — both landed in a prior session's commit after the finding text was written and the checkbox was never reconciled. Left the existing tests untouched; only added the two genuinely-still-missing cases.
- 2026-08-05 - Threaded an optional `ILogger?` through `PartySdkProjectionFold.DeserializeNew` and its two dependent folds (`PartyDetailSdkProjectionHandler.Fold` stays silent by design; `PartyProcessingActivityFold.Fold` carries the logger for the whole Detail-slot handler; `PartyIndexSdkProjectionHandler.FoldCore`/`AccumulateAsync` carry it only from their real persisting call sites). First attempt used `[LoggerMessage]`-generated partial methods (matching `PartySdkQueryService.cs`'s pattern) and failed CS8795 — `Hexalith.Parties.Projections.csproj` has no direct package reference to `Microsoft.Extensions.Logging.Abstractions` (only a transitive one through the EventStore SDK `ProjectReference`s), and the source generator does not activate across that P2P boundary. Switched to plain `ILogger` extension-method calls (`LogWarning`/`LogInformation`), matching the precedent already used elsewhere in this codebase (`PartyMemoryIndexingService.cs`, `PartyKeyRetryActor.cs`, `TenantAccessService.cs`) rather than modifying the shared root `Directory.Packages.props` to add a new centrally-pinned package purely for this generator.
- 2026-08-05 - Added `tests/Hexalith.Parties.Projections.Tests/Handlers/RecordingLogger.cs` (mirrors `Hexalith.Parties.Tests.Gateway.RecordingLogger<T>`) and 7 new logging-assertion tests, 2 new direct `DeserializeNew`-level fold tests, and 1 new `DetailHandler_IncrementalDispatchAcrossTwoDeliveriesMatchesSingleShotRebuildAsync` test backed by a new stateful `InMemoryReadModelBatchStore` test double (a real, non-mocked `IReadModelStore`/`IReadModelBatchStore` implementation that persists across calls) to close the self-referential rebuild-vs-replay gap.
- 2026-08-05 - `dotnet build src/Hexalith.Parties.Projections/Hexalith.Parties.Projections.csproj -c Debug -p:UseHexalithProjectReferences=true -p:UseNuGetDeps=false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0 -nr:false -m:1` passed 0 warnings/0 errors. `dotnet build tests/Hexalith.Parties.Projections.Tests/Hexalith.Parties.Projections.Tests.csproj` (same flags) passed 0 warnings/0 errors; direct execution passed 216/216 (up from 204/204 before this session's 12 new tests — some intervening growth from other work already on `main`).
- 2026-08-05 - `dotnet build tests/Hexalith.Parties.Tests/Hexalith.Parties.Tests.csproj` (same flags) passed 0 warnings/0 errors; direct execution reported 503/504, the sole failure being `PlatformApiPrerequisitesTests.Matrix_ValidationEvidenceCommandsAreReproducible` — confirmed pre-existing and out of Story 8.6 scope: it hard-pins the Story 8.3 matrix's Story 8.7/G5 "Payload protection engine package" row to EventStore `7854f8e5`, while the current checkout has since drifted to `b140a576` (one commit past `1d6e9321`/`v3.91.0`, which the Group 6 finding above already documents as the same drift class). `dotnet build tests/Hexalith.Parties.IntegrationTests/Hexalith.Parties.IntegrationTests.csproj` (same flags) passed 0 warnings/0 errors (not executed — needs Docker/Dapr sidecars unavailable in this sandbox, consistent with prior sessions' environment-limited evidence).
- 2026-08-05 - `git diff --check` and `bash scripts/check-no-warning-override.sh` both passed.
- 2026-08-05 - Investigated the remaining open `PrepareRebuildAsync`/`FinalizeAsync` `ReadModelBatchConcurrency.LastWrite` finding and left it open: `Hexalith.EventStore.DomainService`'s rebuild-plan executor (outside this story's approved surface) is the party that would need to define what happens on a rebuild-plan write conflict before switching to `Match(etag)` here is safe; a unilateral change risks trading a silent-overwrite bug for a silent-total-rebuild-failure bug. Not fixed this session; flagged for explicit SDK-level input as the story's own prior note already required.
- 2026-08-05 - Code review of the diagnostic-logging patch above returned 8 patch-worthy issues (dead-code Index diagnostics, a silent null-payload drop path, duplicate logging under optimistic-concurrency retry, a raw-exception PII/GDPR risk, missing ambiguous-branch test coverage, an undocumented silent-Fold invariant, a misleading comment, and duplicated `RecordingLogger<T>`). Fixed all 8 — see the new "Diagnostic-logging patch re-review" findings block above for each one's root cause and fix. 4 additional findings from the same review pass were triaged as defer (not same-pass patches: the `[LoggerMessage]` vs. plain-`ILogger` house-style question, per-drop log-category attribution, rebuild-time log-volume strategy, and a pre-existing `FormatException`/`OverflowException` catch-filter gap) and logged to `deferred-work.md` — not touched here, per explicit direction.
- 2026-08-05 - Root-caused the `PartyIndexSdkProjectionHandler.FoldCore` dead-code bug precisely: `GetDeliveryFailureReason` (silent, no logger parameter) internally calls `HasUnresolvedNewEvent`, which is the same function whose logged variant lives later in `FoldCore`'s success path — but the failure branch returns before ever reaching it. Fix threads an optional `ILogger?` onto `HasUnresolvedNewEvent` only (not `GetDeliveryFailureReason`, to avoid changing `PartyDetailSdkProjectionHandler`'s two existing silent call sites) and re-walks once, guarded by `failureReason == UnresolvedOrUnsupportedEventReason`, immediately before the early return — so it fires exactly once and never overlaps with the main fold loop (which never runs in the failure branch).
- 2026-08-05 - Empirically verified the PII-risk finding with a throwaway console script (`JsonSerializer.Deserialize` against several malformed/mismatched payloads) before deciding the fix: a syntax-level `JsonException` (raised by `Utf8JsonReader` before any type conversion starts) echoes a fragment of the raw offending bytes/token in `.Message` (e.g. `'n' is an invalid start of a property name...` for a truncated `"{ not valid json"` payload), while a type-conversion `JsonException` (e.g. an unrecognized enum string) does not — only path/line/byte metadata. Since a genuinely corrupt live event trips the syntax-error path, and the two cases are not reliably distinguishable at the catch site, both `PayloadDeserializationFailed` and `RedactedEventDropped` now log only `exception.GetType().Name`, never the exception object itself.
- 2026-08-05 - Empirically verified before writing a test that `AmbiguousEventTypeDropped` has no true-positive path through the real resolver: a temporary diagnostic test enumerating `Hexalith.Parties.Contracts`'s `IEventPayload` types found 44 total, 0 short-name collisions. Removed the diagnostic test after confirming this, then closed the coverage gap via a reflection-based cache-seed test instead (see the findings block above) rather than adding fake production event types or leaving the branch untested.
- 2026-08-05 - `dotnet build src/Hexalith.Parties.Projections/Hexalith.Parties.Projections.csproj -c Debug -p:UseHexalithProjectReferences=true -p:UseNuGetDeps=false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0 -nr:false -m:1` passed 0 warnings/0 errors. `dotnet build src/Hexalith.Parties.Testing/Hexalith.Parties.Testing.csproj` (same flags) passed 0 warnings/0 errors. `dotnet build tests/Hexalith.Parties.Projections.Tests/Hexalith.Parties.Projections.Tests.csproj` (same flags) passed 0 warnings/0 errors; direct execution passed 220/220 (up from 216/216 before this pass's 4 new tests).
- 2026-08-05 - `dotnet build tests/Hexalith.Parties.Tests/Hexalith.Parties.Tests.csproj` (same flags) passed 0 warnings/0 errors; direct execution reported 503/504 — same sole pre-existing, out-of-scope failure as before (`PlatformApiPrerequisitesTests.Matrix_ValidationEvidenceCommandsAreReproducible`), confirming the `RecordingLogger<T>` relocation introduced no regression. `Hexalith.Parties.Contracts.Tests`, `Hexalith.Parties.Security.Tests`, `Hexalith.Parties.Server.Tests`, and `Hexalith.Parties.Client.Tests` (the other consumers of `Hexalith.Parties.Testing`) all built 0 warnings/0 errors after the shared-package-reference change.
- 2026-08-05 - `git diff --check` and `bash scripts/check-no-warning-override.sh` both passed.

### Completion Notes List

- 2026-08-04 adversarial re-review patches are applied. The coordinated source-mode builds completed with zero warnings/errors; Party projections passed 204/204, focused Memories/cleanup/platform tests passed 37/37, consumer privacy mapping passed 18/18, and EventStore shared-rebuild dispatcher tests passed 4/4. Story status remains `in-progress` because the earlier unresolved diagnostic-logging, corrupt-vs-redacted observability, rebuild/live concurrency, and remaining parity-test findings above were not part of the approved twelve-patch set.
- 2026-08-04T23:00:08+02:00 - The additive EventStore package handoff is no longer pending: package-mode restore/build is green at EventStore `v3.91.0` (see Debug Log). Status is corrected from a since-reverted `done` back to `in-progress` (`sprint-change-proposal-2026-08-04.md`) — the package-handoff sub-block is resolved, but the diagnostic-logging, corrupt-vs-redacted observability, rebuild/live-write concurrency, and remaining parity-test findings above remain open and unresolved.
- The former SDK compatibility block is resolved by explicitly selecting latest stable EventStore `v3.89.0` (`c590590bc581a3f72ef6e67148eda988ba4b8fe6`), which contains the tenant-shared rebuild accumulation, replacement, and stale-entry pruning surface absent from `fa2d1c99`.
- Detail, index, and PII-free Art.30 processing-record projections now persist through SDK read-model batches and rebuild plans. Duplicate/out-of-order delivery no longer advances projection timestamps, and shared-index rebuild verification covers complete replacement and erased-party exclusion.
- All eight Party query discriminators run through SDK `IDomainQueryHandler` implementations. Strict tenant/payload validation, DataProtection-backed cursor scope, GDPR export/status/certificate semantics, and tenant-scoped last-known degraded data are preserved. Freshness metadata uses the renegotiated SDK-native set `Current`/`Stale`/`Unavailable`; store-outage last-known reads report `Stale` with `Metadata.IsDegraded` and the store-unavailable warning. Legacy `Rebuilding`/`Degraded`/`LocalOnly` enum values are not produced; Picker collapses them into degraded UX if an old payload appears (AC7 renegotiation 2026-08-03).
- Dapr projection/query actors, local rebuild services/checkpoints, projection platform adapters, actor fallbacks, and the actor health check were removed after focused parity passed. EventStore server utilities remain only as an explicit integration-test fixture dependency, not a production dependency.
- Focused validation is green: latest SDK source build 0 warnings/errors, projections 150/150, query/health/composition/architecture 48/48, architecture fitness 21/21, and the corrected encryption fixture 6/6.
- The ingress wording is reconciled by the later frozen spec: gateway/public behavior remains unchanged, and deny-default DAPR service invocation allows only EventStore on exact POST command/query/projection/rebuild/discovery routes. The previously omitted operational-index metadata discovery route is now documented, admitted, and fitness-tested without a wildcard or peer expansion.
- The final all-lanes regression is green across all 15 projects. Parties passes 452/452; the Integration assembly passes 31 tests with 6 explicit deferred-health skips; and the Release solution build has 0 warnings and 0 errors. (Reconciliation note, 2026-08-02: this 452/452 result was recorded at EventStore checkout `8f004ecf`, not `c590590b`, and the three previously-failing G5 tests were fixed by concurrent Story 8.7 work rather than by this story — see the Debug Log reconciliation note at 2026-08-01T17:38:55+02:00. Story 8.6's own consumer-parity evidence at `c590590b` is 449/452 with the three pre-existing G5 failures.)
- Story 8.6 is ready for review. This Class C platform cleanup delivers no new PRD functional coverage.
- 2026-08-05 - Closed three of the four remaining Group 2 review findings: operator-facing diagnostic logging for dropped/unresolved/corrupt/ambiguous/redacted projection events is restored (via plain `ILogger` extension calls, not `[LoggerMessage]` — see Debug Log for why), the corrupt-live-vs-redacted-tail log signal is now distinct, and the two genuinely-missing fold/rebuild-parity test gaps are closed (two of the four originally-listed gaps were already covered by prior-session tests and only needed the finding text reconciled). The `PrepareRebuildAsync`/`FinalizeAsync` `LastWrite`-concurrency finding remains open pending explicit EventStore SDK-level input on the rebuild-plan conflict contract — this is the one remaining known risk from this story's own open findings list. Story status remains `in-progress` for that reason; not advanced to `review`/`done` in this session.
- 2026-08-05 - Human directed: defer the remaining 7 open `[Review]` tasks (the `LastWrite`-concurrency Patch plus 6 pre-existing Defer items) rather than force them through this build session, and proceed to code review. Logged as `## Deferred from: code review of 8-6-projection-and-query-sdk-migration.md (2026-08-05)` in `deferred-work.md`, which also closes out the still-open 2026-08-04 ledger action item to record the `FinalizeAsync` concurrency defect there.
- 2026-08-05 - A follow-up adversarial code review of the diagnostic-logging patch itself (the one that closed the earlier Group 2 logging findings) found 8 patch-worthy issues in that patch's own diff — none PRD-functional, all mechanical correctness/observability/GDPR-hygiene defects in the newly-added logging code and its tests. All 8 fixed: the shared Party index projection's unresolved/ambiguous/non-JSON diagnostics were dead code (the failure branch returned before the logged walk ever ran) and now fire correctly at every `FoldCore` call site without duplicating; a resolved-but-null-payload event now logs distinctly instead of silently; optimistic-concurrency retries no longer repeat the same diagnostic per attempt; the raw `Exception` object is no longer passed to `ILogger` after direct testing confirmed a syntax-level `JsonException` can embed a fragment of raw payload bytes in `.Message`; the `AmbiguousEventTypeDropped` branch now has coverage via a reflection-seeded resolver cache (confirmed empirically unreachable through the real 44-type Contracts assembly, so no production type was added); the silent-`Fold` invariant and a misleading no-op-branch comment are now documented in place; and `RecordingLogger<T>` moved from two duplicated per-project copies into the shared `Hexalith.Parties.Testing` project. `Hexalith.Parties.Projections.Tests` passes 220/220; `Hexalith.Parties.Tests` and the other three `Hexalith.Parties.Testing` consumers show no regression.

### File List

Pre-existing user-owned `references/Hexalith.Builds` dirt and
`references/Hexalith.FrontComposer` gitlink drift were preserved and are excluded.

Story-file self-listing note (2026-08-02 review): this story file is `new file mode 100644`
relative to the `2c4a7af` baseline diff, so a strict reading would list it under Added rather
than Modified; kept under Modified since it was materially drafted before the baseline commit
and only accumulated content since.

**Added**
- `references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/IAsyncDomainSharedProjectionRebuildCompletionHandler.cs`
- `src/Hexalith.Parties.Projections/Handlers/PartyProcessingActivityFold.cs`
- `src/Hexalith.Parties/Queries/PartySdkLastKnownReadModelCache.cs`
- `src/Hexalith.Parties/HealthChecks/PartyReadModelHealthCheck.cs`
- `src/Hexalith.Parties.Projections/Search/IPartyIndexSearchIndexer.cs`
- `src/Hexalith.Parties.Projections/Search/NoOpPartyIndexSearchIndexer.cs`
- `src/Hexalith.Parties/Search/PartyMemoryIndexEntrySearchIndexer.cs`
- `_bmad-output/implementation-artifacts/8-6-ac1-llm-instructions.md`
- `tests/Hexalith.Parties.Projections.Tests/Models/PartySdkReadModelAddressesTests.cs`
- `src/Hexalith.Parties.Testing/RecordingLogger.cs`

**Modified**
- `_bmad-output/implementation-artifacts/deferred-work.md`
- `_bmad-output/implementation-artifacts/8-6-projection-and-query-sdk-migration.md`
- `_bmad-output/planning-artifacts/epics.md`
- `.agents/skills/bmad-sprint-planning/scripts/sprint_plan.py`
- `.agents/skills/bmad-sprint-planning/scripts/tests/test_sprint_plan.py`
- `_bmad-output/implementation-artifacts/story-8-3-platform-api-prerequisite-matrix.md`
- `_bmad-output/implementation-artifacts/spec-8-6-projection-and-query-sdk-migration.md`
- `_bmad-output/implementation-artifacts/tests/test-summary.md`
- `src/Hexalith.Parties.Projections/Actors/PartyEventTypeResolver.cs`
- `src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs`
- `src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs`
- `src/Hexalith.Parties.Projections/Handlers/PartySdkProjectionFold.cs`
- `src/Hexalith.Parties.Projections/Services/PartySdkReadModelEraser.cs`
- `src/Hexalith.Parties.Projections/Hexalith.Parties.Projections.csproj`
- `src/Hexalith.Parties.Projections/Models/PartySdkReadModels.cs`
- `src/Hexalith.Parties.Testing/Hexalith.Parties.Testing.csproj`
- `src/Hexalith.Parties.AppHost/DaprComponents/accesscontrol.parties.yaml`
- `src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs`
- `src/Hexalith.Parties/HealthChecks/MemoriesSearchHealthCheck.cs`
- `src/Hexalith.Parties/Search/PartyMemoryCleanupService.cs`
- `src/Hexalith.Parties/Search/PartyMemoryIndexingService.cs`
- `src/Hexalith.Parties/Search/PartyMemoryUnitMappingStore.cs`
- `src/Hexalith.Parties.UI/Services/ConsumerPrivacyProcessingClient.cs`
- `src/Hexalith.Parties/HealthChecks/PartiesHealthCheckExtensions.cs`
- `src/Hexalith.Parties/Hexalith.Parties.csproj`
- `src/Hexalith.Parties/Program.cs`
- `src/Hexalith.Parties/Queries/PartyDetailProjectionQueryActor.cs`
- `src/Hexalith.Parties/Queries/PartyIndexProjectionQueryActor.cs`
- `src/Hexalith.Parties/Queries/PartySdkQueryService.cs`
- `tests/Hexalith.Parties.IntegrationTests/Hexalith.Parties.IntegrationTests.csproj`
- `tests/Hexalith.Parties.IntegrationTests/Security/EncryptionPipelineIntegrationTests.cs`
- `tests/Hexalith.Parties.Projections.Tests/Handlers/PartySdkProjectionHandlerTests.cs`
- `tests/Hexalith.Parties.Tests/HealthChecks/MemoriesSearchHealthCheckTests.cs`
- `tests/Hexalith.Parties.Tests/Search/PartyMemoryCleanupServiceTests.cs`
- `tests/Hexalith.Parties.Tests/Search/PartyMemoryIndexEntrySearchIndexerTests.cs`
- `tests/Hexalith.Parties.Tests/Search/PartyMemorySearchOptionsValidatorTests.cs`
- `tests/Hexalith.Parties.Tests/Search/PartyMemoryUnitMapperTests.cs`
- `tests/Hexalith.Parties.UI.Tests/ConsumerPrivacyProcessingClientTests.cs`
- `references/Hexalith.EventStore/docs/guides/named-projection-authoring.md`
- `references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/DomainProjectionRebuildPlan.cs`
- `references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/DomainSharedProjectionRebuildDispatcher.cs`
- `references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/DomainSharedProjectionRebuildSessionState.cs`
- `references/Hexalith.EventStore/tests/Hexalith.EventStore.DomainService.Tests/DomainSharedProjectionRebuildDispatcherTests.cs`
- `tests/Hexalith.Parties.Tests/FitnessTests/ArchitecturalFitnessTests.cs`
- `tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs`
- `tests/Hexalith.Parties.Tests/HealthChecks/HealthEndpointIntegrationTests.cs`
- `tests/Hexalith.Parties.Tests/Projections/ProjectionPlatformAdapterTests.cs`
- `tests/Hexalith.Parties.Tests/Gateway/EventStoreGatewayRoutingTests.cs`
- `tests/Hexalith.Parties.Tests/FitnessTests/PlatformApiPrerequisitesTests.cs`

**Deleted**
- `src/Hexalith.Parties.Projections/Abstractions/IPartyDetailProjectionActor.cs`
- `src/Hexalith.Parties.Projections/Abstractions/IPartyIndexProjectionActor.cs`
- `src/Hexalith.Parties.Projections/Actors/PartyDetailProjectionActor.cs`
- `src/Hexalith.Parties.Projections/Actors/PartyIndexProjectionActor.cs`
- `src/Hexalith.Parties.Projections/Configuration/PartyProjectionPlatformAdapterMode.cs`
- `src/Hexalith.Parties.Projections/Configuration/ProjectionOptions.cs`
- `src/Hexalith.Parties.Projections/Services/IPartyProjectionPlatformAdapter.cs`
- `src/Hexalith.Parties.Projections/Services/IProjectionRebuildService.cs`
- `src/Hexalith.Parties.Projections/Services/LocalPartyProjectionPlatformAdapter.cs`
- `src/Hexalith.Parties.Projections/Services/PartyProjectionPlatformFreshness.cs`
- `src/Hexalith.Parties.Projections/Services/PartyProjectionRebuildCheckpoint.cs`
- `src/Hexalith.Parties.Projections/Services/PartyProjectionRebuildScope.cs`
- `src/Hexalith.Parties.Projections/Services/ProjectionRebuildService.cs`
- `src/Hexalith.Parties/Domain/EventStorePartyProjectionPlatformAdapter.cs`
- `src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs`
- `src/Hexalith.Parties/Extensions/PartyDetailProjectionActorExtensions.cs`
- `src/Hexalith.Parties/HealthChecks/ProjectionActorsHealthCheck.cs`
- `src/Hexalith.Parties/Queries/IPartyProjectionQueryActor.cs`
- `tests/Hexalith.Parties.Tests/Gateway/RecordingLogger.cs`
- `tests/Hexalith.Parties.Tests/Gateway/PartyDetailProjectionQueryActorTests.cs`
- `tests/Hexalith.Parties.Tests/Gateway/PartyIndexProjectionQueryActorTests.cs`
- `tests/Hexalith.Parties.Tests/Gateway/TenantSafeProjectionReadGuardrailsTests.cs`
- `tests/Hexalith.Parties.Tests/HealthChecks/ProjectionActorsHealthCheckTests.cs`
- `tests/Hexalith.Parties.Tests/Projections/PartyDetailProjectionActorCorruptionTests.cs`
- `tests/Hexalith.Parties.Tests/Projections/PartyDetailProjectionActorExtensionsTests.cs`
- `tests/Hexalith.Parties.Tests/Projections/PartyIndexProjectionActorCorruptionTests.cs`
- `tests/Hexalith.Parties.Tests/Projections/ProjectionRebuildAndHealthHardeningTests.cs`
- `tests/Hexalith.Parties.Tests/Projections/ProjectionRebuildServiceTests.cs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-09 | 0.1 | Blocked Story 8.6 at the prerequisite gate because the Story 8.3 `EventStore projection/query SDK` row remains `needs-additive-api`; recorded the current EventStore pin and preserved all production source rollback paths. | GPT-5 Codex (dev-story) |
| 2026-07-16 | 0.2 | Revalidated the prerequisite gate; Story 1.20 remains unstarted with no owner-approved closure packet, and the EventStore checkout does not match the root gitlink. Preserved all production source rollback paths. | GPT-5 Codex (dev-story) |
| 2026-08-01 | 0.3 | Revalidated the now-available owner proof at its exact approved source SHA; blocked before migration because the mandatory A/B/C verifier's fresh seven-year WORM-retention check fails, then restored the pre-story dependency identity. | GPT-5 Codex (dev-story) |
| 2026-08-01 | 0.4 | Extended WORM retention, published and verified a refreshed owner A/B/C chain, passed the exact source-consumer handoff, then blocked safely when the authorized SHA failed to compile the required tenant-shared rebuild surface. | GPT-5 Codex (dev-story) |
| 2026-08-01 | 0.5 | Used latest stable EventStore v3.89.0 under explicit user direction, completed the SDK projection/query migration and local-mechanics retirement, and recorded focused-green plus broad/environment-limited validation. | GPT-5 Codex (dev-story) |
| 2026-08-01 | 0.6 | Reconciled public versus internal ingress from the frozen spec, admitted and tested the missing EventStore-only operational-index metadata route, and retained `in-progress` because full regression gates remain non-green outside Story 8.6. | GPT-5 Codex (dev-story) |
| 2026-08-01 | 0.7 | Revalidated the host/ACL and full unit gates, then halted on the unchanged Story 8.7/G5 prerequisite-matrix regression failures without modifying production source or advancing status. | GPT-5 Codex (dev-story) |
| 2026-08-01 | 0.8 | Isolated the encryption fixture from the production DAPR retry scheduler, passed all 15 regression projects and the warning-free Release build, and advanced Story 8.6 to review. | GPT-5 Codex (dev-story) |
| 2026-08-04 | 0.9 | Applied all twelve approved adversarial-review patches, including durable post-commit Memories convergence, ingestion-CaseId cleanup routing, retryable forward-schema delivery, Art.30 audit persistence, bounded UI mappings, and focused verification. Retained `in-progress` for earlier unresolved findings outside this patch set. | GPT-5 Codex (code-review) |
| 2026-08-04 | 0.10 | Corrected the `Status:` field from a since-reverted `done` back to `in-progress`, matching this table's own 0.9 entry, and recorded the resolved package-mode build at EventStore `v3.91.0` per approved `sprint-change-proposal-2026-08-04.md`. Material open review findings remain unresolved; no Story 8.7/G5 change is authorized or implied. | Claude Sonnet 5 (correct-course recovery) |
| 2026-08-05 | 0.11 | Restored operator-facing diagnostic logging for dropped/unresolved/corrupt/ambiguous/redacted projection events (plain `ILogger` calls, not `[LoggerMessage]`, due to a source-generator/ProjectReference gap), made the corrupt-vs-redacted log signal distinct, and closed the two genuinely-missing fold/rebuild-parity test gaps (reconciling two already-fixed ones the finding text had not caught up with). `PrepareRebuildAsync`/`FinalizeAsync` `LastWrite`-concurrency remains the one open finding, pending SDK-level input; status stays `in-progress`. | Claude Sonnet 5 (dev-story) |
| 2026-08-05 | 0.12 | Human-directed: the remaining 7 open `[Review]` findings (the `LastWrite`-concurrency Patch item plus 6 pre-existing Defer items) are logged under a new dated section in `deferred-work.md` and this build session proceeds to review without forcing them through. This also satisfies the still-open 2026-08-04 ledger action item to log the `FinalizeAsync` concurrency defect. Tasks remain unchecked in this story to reflect their deferred (not done) status. | Claude Sonnet 5 (dev-story) |
| 2026-08-05 | 0.13 | Fixed all 8 patch-worthy issues from a code review of the diagnostic-logging patch itself: dead-code Index unresolved/ambiguous/non-JSON diagnostics, a silently-dropped resolved-but-null-payload case, duplicate logging under optimistic-concurrency retry, a raw-exception PII/GDPR leak risk (confirmed empirically), missing `AmbiguousEventTypeDropped` test coverage (via a reflection-seeded resolver cache), an undocumented silent-`Fold` invariant, a misleading comment, and a duplicated `RecordingLogger<T>` (moved to shared `Hexalith.Parties.Testing`). `Hexalith.Parties.Projections`/`Hexalith.Parties.Testing` build 0 warnings/0 errors; `Hexalith.Parties.Projections.Tests` passes 220/220; `Hexalith.Parties.Tests` and other `Hexalith.Parties.Testing` consumers show no regression. | Claude Sonnet 5 (dev-story) |
| 2026-08-09 | 0.14 | bmad-build review pass: patched missing `TimeProvider` DI, asymmetric detail/processing checkpoint gap false-positives, empty-event `Max` throw, null rebuild-manifest lists, boot-time CaseId capture on erasure cleanup, missing-CaseId search stall, and global LKG generation aborting unrelated reads. Deferred Incomplete-erasure resume, memories-disabled erasure proof, actor-era failure vocabulary, Actors-folder packaging, deploy backfill, mapping-store collision, and party-id allowlist rigor to `deferred-work.md`. Focused query/DI/indexer tests 61/61; projection handler tests 64/64. | Cursor Grok 4.5 (bmad-build) |
| 2026-08-09 | 0.15 | bmad-code-review DI+query-host sub-chunk: applied 8 patches (LKG TimeProvider ctor, erasure type-only logging, memories Failed bound, cache CT, boot-freeze docs, stale actor-host comments, DI/fitness tests). Deferred e2e story-7-4 drift and UtcNow timestamps. Remaining Suggested Review Order chunks still pending review; status `in-progress`. | Cursor Grok 4.5 (bmad-code-review) |

## Suggested Review Order

**DI and query host**

- Register `TimeProvider.System` so `PartySdkQueryService` resolves without host defaults.
  [`PartiesServiceCollectionExtensions.cs:47`](../../src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs#L47)

- Read live `CaseId` on GDPR memories-search erasure cleanup.
  [`PartiesServiceCollectionExtensions.cs:255`](../../src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs#L255)

**Projection folds**

- Use the present slot watermark when the other detail/processing slot is missing.
  [`PartyDetailSdkProjectionHandler.cs:66`](../../src/Hexalith.Parties.Projections/Handlers/PartyDetailSdkProjectionHandler.cs#L66)

- Guard empty deliveries before `Events.Max` on the shared index fold.
  [`PartyIndexSdkProjectionHandler.cs:402`](../../src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs#L402)

- Null-safe rebuild search manifest lists.
  [`PartyIndexSdkProjectionHandler.cs:279`](../../src/Hexalith.Parties.Projections/Handlers/PartyIndexSdkProjectionHandler.cs#L279)

**Last-known cache and Memories**

- Scope LKG invalidation generations per cache key.
  [`PartySdkLastKnownReadModelCache.cs:54`](../../src/Hexalith.Parties/Queries/PartySdkLastKnownReadModelCache.cs#L54)

- Pass key-scoped generations from query reads.
  [`PartySdkQueryService.cs:418`](../../src/Hexalith.Parties/Queries/PartySdkQueryService.cs#L418)

- Treat missing Memories CaseId as converged skip, not permanent retry.
  [`PartyMemoryIndexEntrySearchIndexer.cs:45`](../../src/Hexalith.Parties/Search/PartyMemoryIndexEntrySearchIndexer.cs#L45)

**Tests**

- Assert `TimeProvider` registration and unrelated-key LKG isolation.
  [`ProjectionPlatformAdapterTests.cs:29`](../../tests/Hexalith.Parties.Tests/Projections/ProjectionPlatformAdapterTests.cs#L29)
  [`PartySdkQueryHandlerTests.cs:829`](../../tests/Hexalith.Parties.Tests/Gateway/PartySdkQueryHandlerTests.cs#L829)
  [`PartyMemoryIndexEntrySearchIndexerTests.cs:38`](../../tests/Hexalith.Parties.Tests/Search/PartyMemoryIndexEntrySearchIndexerTests.cs#L38)

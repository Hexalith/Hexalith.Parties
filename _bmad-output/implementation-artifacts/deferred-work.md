# Deferred Work

Items raised during code review that are real but not actionable in the current story. Pick up in a follow-up story or hardening sprint.

## Deferred from: code review of 9-6-hexalith-memories-backed-party-search — second pass (2026-05-03)

- Replay-from-zero re-applies non-`PartyCreated` events on every command — root cause for the AC1-reindex burst flagged in this pass. Pre-existing structural issue not introduced by 9-6 acceptance criteria. Bundle with the projection-determinism hardening story. [src/Hexalith.Parties.CommandApi/Domain/PartyProjectionUpdateOrchestrator.cs DeliverProjectionAsync]
- `Aspire.Hosting.Keycloak` / `Aspire.Hosting.Kubernetes` pinned to `13.2.2-preview.1.26207.2` while core Aspire is `13.2.2` and `Aspire.Hosting.Testing` is `13.2.1`. Pre-existing dependency skew. Bundle into a single Aspire-bump PR. [Directory.Packages.props:26-27]
- `NameHistory.Where(...).OrderBy(ChangedAt).LastOrDefault()` is O(n log n) per request and re-sorts a naturally chronological list. Perf only — re-evaluate when temporal-name endpoints become a profiled hotspot. [src/Hexalith.Parties.CommandApi/Controllers/PartiesController.cs:401; src/Hexalith.Parties.CommandApi/Mcp/GetPartyNameAtMcpTool.cs:1588-1591]
- `LocalFuzzyPartySearchProvider` rejects fuzzy match on tokens containing digits — intentional tradeoff to suppress cross-id false positives, but legitimate fuzzy hits on identifier typos (e.g., passport-number transposition) are lost. Revisit if identifier-typo recovery becomes a use case. [src/Hexalith.Parties.CommandApi/Search/LocalFuzzyPartySearchProvider.cs:1672-1677]
- Three independent string tables map search mode → axis (`MemoriesPartySearchService.MapModeToAxis`, `PartiesController.ParseSearchMode`, `FindPartiesMcpTool` mode parsing). Style/maintenance only — factor when the next axis is added. [Multiple call sites]
- `PartiesServiceCollectionExtensions` performs boot-time validation that duplicates `PartyMemorySearchOptionsValidator`. Two enforcement paths can drift; collapse to validator-only on next DI refactor. [src/Hexalith.Parties.CommandApi/Extensions/PartiesServiceCollectionExtensions.cs:1133-1137]
- `EnabledAxes` validator does not deduplicate the array; `IsAxisEnabled` linear-scan tolerates duplicates but wastes work. Cosmetic. [src/Hexalith.Parties.CommandApi/Search/PartyMemorySearchOptions.cs:2447]
- `PartyEventTypeResolver` `s_resolvedCache` has no eviction; bounded by event-type string space (closed-world). Adversarial expansion via `HandleSerializedEventAsync` requires authenticated ingress, so not a practical DoS vector. [src/Hexalith.Parties.Projections/Actors/PartyEventTypeResolver.cs:3173]
- `PartyMemoryUnitMapper` does not bound `context.Timestamp` against `DateTimeOffset.MinValue`/`MaxValue` — rare and the test fixture does not currently produce it. [src/Hexalith.Parties.CommandApi/Search/PartyMemoryUnitMapper.cs:2649]
- `MemoriesPartySearchService.SearchAsync` materializes `entries` via `[..]` enumeration; if the underlying yield-iterator throws partway, the request 500s instead of degrading. Rare and probably indicates state-store corruption that should not be masked. [src/Hexalith.Parties.CommandApi/Search/MemoriesPartySearchService.cs:1796]
- `IPartySearchService` and the `PartySearchRequest`/`PartySearchResponse` DTOs are declared `public` in CommandApi. The Authority-Boundary constraint focuses on Memories types not leaking into Parties.Contracts (which holds), but exposing CommandApi-internal contracts as `public` couples future sibling integration projects. Tighten to `internal` when next reviewing module boundaries.
- `ProbingMemoriesClient` test fixture inherits the real `MemoriesClient` rather than mocking an interface. If the SDK becomes `sealed` upstream every test breaks. Switch to a mockable abstraction when the SDK forces it. [tests/Hexalith.Parties.CommandApi.Tests/Search/MemoriesPartySearchServiceTests.cs ProbingMemoriesClient]

## Deferred from: code review of 9-6-hexalith-memories-backed-party-search (2026-05-03)

- Static `s_lastKnownDetails` ConcurrentDictionary on `PartyDetailProjectionActor` — pre-existing pattern (not introduced by 9-6). Static actor state is an anti-pattern that compounds under the new synchronous-projection load path. Hardening belongs in a separate Tier-3 story (e.g., move the cache to per-actor instance state with a bounded eviction policy, or remove it once the actor service can satisfy reads from the projection store). [src/Hexalith.Parties.Projections/Actors/PartyDetailProjectionActor.cs]
- AppHost does not wire `Parties__MemoriesSearch__ApiToken` env var — deployment-config concern. Follow up when the cleanup-auth patch lands so dev/staging/prod all have a coherent token-injection path. [src/Hexalith.Parties.AppHost/Program.cs]
- `ContractsArchitectureFitnessTests` resolves the contracts csproj via hardcoded `..\..\..\..\..\src\Hexalith.Parties.Contracts\...` — works in current CI structure but breaks if the test runner cwd or output layout changes. Refactor to `[CallerFilePath]` or an MSBuild-injected source-project path when next touching this test. [tests/Hexalith.Parties.CommandApi.Tests/FitnessTests/ContractsArchitectureFitnessTests.cs]

## Deferred from: code review of 9-5-temporal-name-queries (2026-05-03)

- `ApplyErasure` wipes `ConsentRecords` along with PII — GDPR Art.7(1) requires retaining the lawful-basis audit trail; revisit with the consent-management owners (story 9.4). [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:822-830]
- `HandleConsentRevoked` returns `null` when the consent id is unknown — out-of-order or replayed revocations are silently dropped, leaving the read model showing the consent as still active. (story 9.4) [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:912-919]
- `UpdatePersonDetails` typed-shape JSON branch does not fall back to the route id when the body omits `partyId`, inconsistent with the flat-shape branch a few lines below. Composite-command area (epic 4). [src/Hexalith.Parties.CommandApi/Controllers/PartiesController.cs:312-331]
- `GetPartyDetailAsync` JSON-string emptiness check trims to `"{}"` only, while the byte-array path uses `JsonNode.Parse` — `"{ }"` slips through the string path and produces a non-null `PartyDetail` with default fields. Refactor to a single `IsEmptyJsonPayload` helper. [src/Hexalith.Parties.CommandApi/Controllers/PartiesController.cs:428-440]
- `ParseSearchMode` returns `Hybrid` for any unknown value — caller typos silently switch mode instead of returning 400. Search area (story 9.6). [src/Hexalith.Parties.CommandApi/Controllers/PartiesController.cs:557-564]
- `SetSearchMetadataHeaders` strips only CR/LF from `DegradedReason`; other control chars (NUL, VT, FF, DEL) still break Kestrel header serialization. Search area (story 9.6). [src/Hexalith.Parties.CommandApi/Controllers/PartiesController.cs:545-555]
- `MessageId = Guid.NewGuid()` is minted per-request inside the controller, defeating command-level idempotency on client retries. Should come from a client-supplied correlation id. CQRS-pattern multi-story concern. [src/Hexalith.Parties.CommandApi/Controllers/PartiesController.cs:257, 274]
- `TryUpdateProjectionAsync` swallows non-cancellation exceptions and still returns 202 Accepted — clients depending on read-after-write get a false-positive accept and a stale read with no degraded-header signal. Search/projection area (story 9.6). [src/Hexalith.Parties.CommandApi/Controllers/PartiesController.cs:287-308]
- New `ConsentRecorded`, `ConsentRevoked`, `ProcessingRestricted`, `RestrictionLifted` handlers ship without any unit-test coverage in the 9.5 diff. Consent/restriction area (story 9.4). [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:894-932]
- Diff range `c870bcb^..HEAD` includes substantial work outside 9.5 acceptance criteria (search modes, consent records, restriction events, command-DTO reshape, MessageId injection) — expected because the original combined story 9.5 was split after the work was already merged. Flagged so future reviewers know not to chase the same items again.
- `NameHistoryEntry.ChangedAt` is stamped from `DateTimeOffset.UtcNow` at apply time, not the original event timestamp — on projection rebuild every entry collapses to the rebuild wall-clock, weakening AC5 ("rebuilt deterministically") and turning historical `?at=...` queries into 404 / current-name. Equal-millisecond ties also make `LastOrDefault(e => e.ChangedAt <= at)` non-deterministic. Fix needs to thread the envelope timestamp through `Apply(string, IEventPayload, PartyDetail?)` (handler signature + `ProjectionRebuildService` plumbing) or stamp `ChangedAt` on the event payload at command time — both touch the parallel `PartyIndexProjectionHandler`. Split into a dedicated projection-determinism hardening story. [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:91, 119, 141]
- `PartyCreated` arriving for a party whose detail state is non-null is silently no-op'd — the existing test pins this behavior intentionally for replay idempotency, but if state was seeded by an out-of-order non-creation event first, `NameHistory` stays empty forever and the temporal endpoint returns 404 indefinitely. Needs a design discussion on projection-rebuild semantics before flipping the guard. [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:15]
- `NameHistory` grows unbounded inside `PartyDetail` actor state and `GET /name-history` has no pagination or size guard — once the per-key state payload exceeds the Dapr state-store limit the projection wedges. Needs a GDPR retention policy + pagination protocol designed with the consent/legal owners. [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:130-146; src/Hexalith.Parties.CommandApi/Controllers/PartiesController.cs:354-382]

# Deferred Work

Items raised during code review that are real but not actionable in the current story. Pick up in a follow-up story or hardening sprint.

## Deferred from: story 12-1 AppHost recomposition validation (2026-05-09)

Residual full-solution test failures observed after Story 12.1 focused AppHost topology tests, deploy-validation tests, AppHost build, and solution build passed. These failures are outside the Story 12.1 AppHost/DAPR component footprint and should be handled by the owning follow-up stories or hardening items.

- **`ClientArchitecturalFitnessTests.ClientCsproj_HasNoForbiddenProjectReferences`** and **`ClientArchitecturalFitnessTests.ClientAssembly_HasNoReferencesToServerProjectionsOrPartiesService`** — same over-broad client architecture guard class as the already logged `ArchitecturalFitnessTests.ClientProject_HasNoReferencesToServerProjectionsOrPartiesService` failure. The tests flag legitimate `Hexalith.Parties.Contracts` / substring matches rather than a Story 12.1 AppHost dependency. [tests/Hexalith.Parties.Client.Tests/FitnessTests/ClientArchitecturalFitnessTests.cs:36,112]
- **`PartyPayloadProtectionServiceTests.ProtectEventPayload_PersonCreated_EncryptsPersonalDataFields`** — random encrypted payload text contained the literal sentinel `Ada`, causing a probabilistic false positive in a security serialization test. Story 12.1 does not touch payload protection or encryption code. [tests/Hexalith.Parties.Security.Tests/PartyPayloadProtectionServiceTests.cs:95]
- **`PartyApiRoundTripIntegrationTests.ReadyEndpoint_ReturnsSuccessAsync`** — `/ready` returned `503 ServiceUnavailable` in full-solution run. Story 12.1 changes AppHost composition only; the test hosts the service directly and remains outside the AppHost topology change. [tests/Hexalith.Parties.IntegrationTests/PartyApiRoundTripIntegrationTests.cs:93]
- **`AdminEndpointE2ETests.RebuildEndpoint_WithAdminToken_Returns202AcceptedAsync`** — admin rebuild endpoint returned `403 Forbidden` in full-solution run. Story 12.1 does not change admin controller authorization or integration-test token setup. [tests/Hexalith.Parties.IntegrationTests/Admin/AdminEndpointE2ETests.cs:90]

## Deferred from: story 12-0 EventStore-to-Parties feasibility spike (2026-05-09)

Three pre-existing test failures observed when running the full `Hexalith.Parties.Tests` suite against `main` after the spike commit (`59c448c`). The spike commit only added BMAD artifacts plus `tests/Hexalith.Parties.Tests/FitnessTests/EventStorePartiesInvocationSpikeTests.cs` (4 passing) — none of these failures are caused by story 12-0 work. Verified via `git log` that all three test files were last modified in commit `db0bf14`, well before any 12-0 activity. Marked deferred per Jérôme's close-out decision so story 12-0 can transition to `review`.

- **`ArchitecturalFitnessTests.ClientProject_HasNoReferencesToServerProjectionsOrPartiesService`** — test-logic bug. The `forbiddenReferences` list contains `"Hexalith.Parties"` and the check uses `reference.Contains(forbidden, StringComparison.OrdinalIgnoreCase)`, which over-matches the legitimate `Hexalith.Parties.Contracts` reference (the only `ProjectReference` in `Hexalith.Parties.Client.csproj`). No real architectural violation; the test predicate needs an exact-match or trailing-segment guard. Likely candidate for the Epic 12 fitness-test rewrite (Story 12-10) or a small architecture-fitness hardening fix. [tests/Hexalith.Parties.Tests/FitnessTests/ArchitecturalFitnessTests.cs:246-251]
- **`HealthEndpointIntegrationTests.QueryEndpoint_PubSubDegraded_AddsDegradationHeadersAsync`** — production health-degradation header behavior (Epic 8 surface). Asserts `X-Service-Degraded=true` + `X-Stale-Data-Age` headers on `/api/v1/parties/{id}` when DAPR pub/sub is degraded. Currently failing; out of spike scope. Epic 12 server pivot (12-1..12-4) will revisit health-endpoint contract anyway. [tests/Hexalith.Parties.Tests/HealthChecks/HealthEndpointIntegrationTests.cs:163-179]
- **`TemporalNameEndpointTests.GetPartyNameAt_ErasedParty_Returns410Gone`** — GDPR temporal-name endpoint returning `404 NotFound` instead of `410 Gone` for an erased party (Epic 9.5 surface). Out of spike scope; investigate as a separate Epic 9 hardening item or fold into Epic 12 query-path rewrite. [tests/Hexalith.Parties.Tests/Controllers/TemporalNameEndpointTests.cs:162]

## Deferred from: code review of 12-0-eventstore-parties-actor-invocation-feasibility-spike (2026-05-09)

- Wildcard `*|party|v1` registration assumes the resolver wildcard key format is `*|domain|version` with case-passthrough on `domain` (resolver lowercases version but not domain). The spike documents this as the preferred registration but does not call out the case-sensitivity asymmetry vs the convention path which also passes case through. Defer until Story 12.1 actually wires the static registration. [docs/spikes/2026-05-09-eventstore-parties-actor-invocation.md:57,63]
- Reproduction commands in the "Commands run" block are labeled `powershell` but use `rg -n "^" path` (POSIX-style `rg` invocation that effectively `cat -n`s the file). On stock Windows PowerShell without ripgrep installed the commands fail with `rg : The term 'rg' is not recognized`. Convert to PowerShell-native `Get-Content -Path ... | Select-String -Pattern '.*'` or document the `rg` install prerequisite. Cosmetic — reproduction is unblocked once `rg` is on PATH. [docs/spikes/2026-05-09-eventstore-parties-actor-invocation.md:153-167]
- Feasibility-table classification "Event persistence: unknown — dependent on command routing" omits state-store configuration dependencies. Even if command routing succeeds, persistence can still fail on `actorStateStore=true`, `keyPrefix=none`, or shared-state-store key collisions between EventStore and Parties. Story 12.1's AppHost recomposition will surface this when the topology is wired; defer the doc clarification until the runtime proof attempt actually exercises persistence. [docs/spikes/2026-05-09-eventstore-parties-actor-invocation.md:139,141]
- Domain/app-id matrix risks misreading: row 1 (`Domain=party`) recommends "Yes, map to `parties/process`" via explicit registration, while row 2 implies convention routing was the natural plan. The narrative structure makes it easy to misread "convention works for `party`" before realizing all three rows actually require explicit static registration. Tied to the verdict-integrity decision (D1 in story Review Findings); rewrite the matrix prose alongside whatever verdict resolution is chosen. [docs/spikes/2026-05-09-eventstore-parties-actor-invocation.md domain matrix]

## Deferred from: code review of 10-1-1-admin-portal-frontcomposer-and-tenants-integration (2026-05-09)

- `IsTenantProblem` heuristic relies on a header (`X-Tenant-Required`) that the backend does not emit — `StatusKind.TenantRequired` UX path is unreachable today. Story 10-1-1 is PAUSED (superseded by Epic 12 admin portal rebuild on FrontComposer + EventStore queries); deferred until backend contract exists (header or RFC 7807 problem+json `type` discriminator). Local `!HasTenantContext` cannot substitute because the heuristic is intended for server-side tenant-validation failures (suspended/missing tenant) where the client still has a tenant context. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalApiClient.cs:1696]
- `AuthenticationStateChanged` subscription leak risk on circuit-aborted teardown — Dispose unsubscribes, but the lambda captures `this`; if Dispose never runs the singleton provider holds a stale reference. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor]
- `GetRichSearchCapabilityAsync` swallows `AdminPortalQueryException` — captive-portal HTML on `/health` becomes `Degraded` instead of `AuthenticationRequired`. Low priority because `/health` is typically anonymous. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalApiClient.cs:1457]
- `AdminPortalQueryMetadata` constructor accepts unbounded header values — `BoundHeaderValue` was moved to the call sites. Test still passes the bounded path, but a future caller constructing the type directly bypasses the invariant. [src/Hexalith.Parties.AdminPortal/Services/AdminPortalQueryMetadata.cs]
- `SwapListCts` race: between `Interlocked.Exchange(ref _listCts, newCts)` and `token = newCts.Token`, a concurrent swap could `SafeCancel(newCts)`. Mitigated downstream in `AdminPortalPartyQueryService` but not at the component. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor:768,778]
- Test helpers use sync-over-async (`GetAwaiter().GetResult()` on Bunit dispatcher / `_tenantStore.SaveAsync(state).GetAwaiter().GetResult()`). Test-only, may deadlock on alternate runners. [tests/Hexalith.Parties.AdminPortal.Tests/Components/PartiesAdminPortalComponentTests.cs:2280,2291]
- `TryReadProblemDetail` reads JSON unbounded — `JsonDocument.Parse` of arbitrarily large response. Defensive only; backend is trusted. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalApiClient.cs:1733]
- Two unauthenticated users share `<missing>` cache key in `AdminPortalAuthorizationState.ContextSignature` — mitigated because `IsAdmin=false` returns early. Pattern is fragile if downstream code adds branches. [src/Hexalith.Parties.AdminPortal/Services/AdminPortalAuthorizationService.cs:48-49]
- `AdminPortalQueryException.RetryAfter` populated from `Retry-After` header but never consumed by UI. [src/Hexalith.Parties.AdminPortal/Services/AdminPortalQueryException.cs]
- `FluentTextInput.Element` may be null on first render after a state transition — focus is silently lost; `_pendingFocus` already cleared, no retry path. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor:722]
- MediaType comparison rejects legitimate non-standard subtypes (e.g., `application/x-ndjson`) and a captive-portal `text/html` is reported as Unknown failure rather than redirect-to-sign-in. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalApiClient.cs:265-269]
- `Items.Where(x => x.Party is not null).Select(x => x.Party!)` — backend should enforce `required` on `Party.Id`; null-Id slipping through would NRE the click handler. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor:506,569]
- File List drift: `_bmad-output/implementation-artifacts/sprint-status.yaml` is in the story's File List but the captured diff (9b6d180..HEAD) contains zero hunks for that path; sprint-tracking edit was made out-of-band or is missing. [_bmad-output/implementation-artifacts/sprint-status.yaml]
- Pagination subtask wording: Tasks line 36 reads `[ ] Replace pagination buttons with FrontComposer paging component`. The diff DOES replace pagination with `FluentButton`; the unchecked subtask refers to a dedicated FrontComposer paging component which the Completion Notes acknowledge does not exist. Tighten the subtask wording. [_bmad-output/implementation-artifacts/10-1-1-admin-portal-frontcomposer-and-tenants-integration.md:36]

## Deferred from: code review of 11-1-apphost-and-package-integration (2026-05-07)

- AppHost env-vars on `parties` duplicate `appsettings.json` Tenants values; env-var precedence makes JSON values dead under AppHost. Future maintenance trap; pre-existing pattern. [src/Hexalith.Parties.AppHost/Program.cs:48-52]
- `ValidateOnStart` eagerness for `TenantIntegrationOptions` is not asserted by any test. Likely fires correctly under modern .NET host, but if validation falls back to lazy on `IOptions.Value`, missing config surfaces as a 500 from `/ready` rather than a startup crash. Add a startup-fail integration test in a hardening pass. [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:79-82]
- `WaitFor(tenantsResources.CommandApi)` waits on the project resource, not the Tenants DAPR sidecar. First N readiness probes after startup may race app-id resolution, briefly returning `/ready=503`. Pre-existing Aspire pattern across sibling stories. [src/Hexalith.Parties.AppHost/Program.cs:45-46]
- `Uri.EscapeDataString(serviceName)` does not validate that the service name is a syntactically valid DAPR app id. Typos like `tenants/` or `tenants:dev` percent-encode and surface as opaque "not ready" instead of a clear configuration error. Low impact. [src/Hexalith.Parties/HealthChecks/TenantsIntegrationHealthCheck.cs:25-26]
- Integration test fake `HealthyTenantsReadinessProbe` uses `RemoveAll().Add()`, which is a no-op if a future regression drops the production `ITenantsReadinessProbe` registration. Add a guard test asserting the production registration exists. [tests/Hexalith.Parties.Tests/HealthChecks/HealthEndpointIntegrationTests.cs:336-348]

## Carried from pass-1 deferral chain — Story 10-1.1 follow-up (2026-05-07)

These three items were marked "Deferred to follow-up story 10-1.1" in the pass-1 Review Findings of `10-1-admin-portal-browse-search-and-inspect.md` but were not previously surfaced in the project-level deferred-work tracking. Pass-2 surfaces them here for visibility.

- **D2b — Hexalith.Tenants integration** for `IsAuthenticated`/`HasTenantContext`/`IsAdmin` parameters. Component currently accepts host-supplied booleans (default `false`, fail-closed). Story 10-1.1 should consume `AuthenticationStateProvider`, claims access, and `Hexalith.Tenants` membership/role services directly so the portal does not depend on the host wiring auth correctly. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor]
- **D3 — Rich-search capability detection probe**. AC3 requires "deterministic detection that the backend capability is available and healthy"; `RichSearchAvailable` is currently a passive `[Parameter] bool`. Story 10-1.1 should add a capability probe (e.g., `GET /api/v1/parties/search/capabilities` once Story 9.6 stabilizes the contract) and toggle the email/identifier search modes based on the live signal. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor RichSearchAvailable parameter]
- **D4 — FrontComposer Fluent UI reuse**. AC8 requires "uses the existing Hexalith.FrontComposer Blazor/Fluent UI shell and composable components". Current implementation uses raw `<table>`, `<select>`, `<input>`, `<button>` elements; only `Hexalith.FrontComposer.Contracts.Registration.IFrontComposerRegistry.RegisterDomain` is consumed. Story 10-1.1 should replace markup with `FluentDataGrid<T>`, `FluentTextInput`, `FluentSelect`, `FluentButton`, and wire FrontComposer transports/ETag caching/auth redirect. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor]

## Deferred from: code review of 10-1-admin-portal-browse-search-and-inspect — pass 2 (2026-05-07)

- `PageSize` negative coerced silently to default — `BoundPageSize(-1)` returns 20 with no caller warning. Defensive only; current `<select>` only emits valid values. [src/Hexalith.Parties.AdminPortal/Services/AdminPortalQueryBounds.cs]
- Zero-width unicode chars in query (`U+200B` etc.) routed to `SearchPartiesAsync` instead of LIST endpoint because `string.IsNullOrWhiteSpace` does not recognize them; backend's `IsEffectivelyEmptyQuery` does and returns LocalOnly empty. UI then reports "Display-name search only" when the user typed nothing visible. Defensive normalization at the component. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor]
- Long scoped party ids may overflow URL length — no client-side guard. Pre-existing pattern; URL-length limits are typically enforced at proxy layer. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalApiClient.cs GetPartyAsync]
- `PartiesAdminPortalOptions.ApiBaseAddress` setter publicly mutable — a misconfigured caller could change the base address mid-app lifecycle. No concrete defect today. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalOptions.cs]
- `@key="row.Id"` lacks defensive uniquification on duplicate ids — backend filters by dictionary key, but a malformed projection could deliver duplicates and Blazor would throw. Pre-existing pattern. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor]
- `DateTimeOffset.ToString("g", CurrentCulture)` Blazor Server vs browser culture mismatch — server-side rendering uses server culture; user's browser culture is not honored. Full localization deferred to FrontComposer integration in Story 10-1.1. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor FormatDate]
- `PartiesAdminListCoordinator.State` lacks memory barrier — moot because the type is dead code (D5 in pass-2 review); revisit if D5 wires the type. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminListCoordinator.cs]
- `AddDate` produces verbose URL-encoded dates (~250 chars when all four set) — defensive only; not a bug. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalApiClient.cs AddDate]
- `Search` request with empty/whitespace `Query` reachable only via the public API surface — UI routes whitespace-only queries to LIST; the search-with-empty-q path is unreachable from the component but not enforced at API client level. Defensive. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalApiClient.cs SearchPartiesAsync]

## Deferred from: code review of 10-1-admin-portal-browse-search-and-inspect (2026-05-07)

- `AdminPortalQueryBounds.BoundPage` has no upper limit; `_page` could overflow with `int.MaxValue` clicks. Defensive only; not realistic at human click rate. [src/Hexalith.Parties.AdminPortal/Services/AdminPortalQueryBounds.cs]
- `AdminPortalQueryException.StatusCode` is publicly exposed — a future caller logging `ex.StatusCode` would mildly leak server status. No current consumer uses it. [src/Hexalith.Parties.AdminPortal/Services/AdminPortalQueryException.cs]
- Header parsing edge cases: multiple values for the same header (`X-Service-Degraded`), non-bool synonyms ("yes"/"1"), very long query strings (>2KB → 414), inverted date ranges (`createdBefore < createdAfter`). All defensive; backend currently emits canonical bool/single-value headers. [src/Hexalith.Parties.AdminPortal/Services/PartiesAdminPortalApiClient.cs]
- `PortalFilters.Active` does not trim whitespace before `bool.TryParse` — minor robustness only. [src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor]
- `AdminPortalQueryFailureKind` switches in `ApplyFailure`/`SelectPartyAsync` are non-exhaustive — no new failure kinds planned for this story. [src/Hexalith.Parties.AdminPortal/Services/AdminPortalQueryFailureKind.cs]
- Adopt `IAsyncDisposable` to await any in-flight task before final disposal — the immediate `ObjectDisposedException` race after `Dispose` is covered by P11 (`_disposed` flag check); the broader async-dispose contract is a hardening improvement, not a fix.

## Deferred from: code review of 11-4-tenants-integration-tests-deployment-validation-and-documentation — pass 2 (2026-05-06)

- `tenants-integration.yaml` and `subscription-tenants.yaml` hardcode `parties` without env-var indirection (`{env:COMMAND_API_APP_ID}`) — diverges from rest of deploy folder pattern; deferred because changing this could break operator-side env wiring without a coordinated config-template refresh. [deploy/dapr/tenants-integration.yaml, deploy/dapr/subscription-tenants.yaml]
- `Test-TenantsIntegration` does not cross-validate manifest topic/pubsubName against subscription file values — design enhancement; current behavior produces two unrelated Fails when files disagree, which is informative enough. Revisit if operators report confusion. [deploy/validate-deployment.ps1 Test-TenantsIntegration]
- `subscription-tenants.yaml` advisory annotations (`hexalith.io/resiliency-policy`, `hexalith.io/max-deliveries`) are not validator-checked against `resiliency.yaml` — annotations can drift indefinitely without any test catching it. Defer extending the validator to probe the named policy in a deployment-validation hardening pass. [deploy/dapr/subscription-tenants.yaml, deploy/validate-deployment.ps1]
- `McpSessionScope` AsyncLocal nested-scope contract — current restoration is correct for documented usage but a future test pattern using await chains outside the `using` block could leak parent-flow tenant id to parallel tests. Add an xUnit collection guard if/when those patterns appear. [tests/Hexalith.Parties.Tests/Mcp/McpSessionScope.cs]
- `JsonSerializer.Parse` / `FileStream` lifetime in `ResolveSigningKey` — file handle leak on malformed JSON; pre-existing pattern. Broader test-config refactor preferred over targeted fix. [tests/Hexalith.Parties.IntegrationTests/Tenants/TenantIntegrationTestSeeder.cs ResolveSigningKey]
- Multi-document YAML `...` end-of-doc separator handling in `Split-YamlDocuments` — rarely seen in Helm-generated DAPR manifests. Add when a real config exhibits it. [deploy/validate-deployment.ps1 Split-YamlDocuments]
- Project-wide `xUnit1051` suppression in `Hexalith.Parties.Tests.csproj` — pass-1 deferred and pass-2 reaffirms; threading `TestContext.Current.CancellationToken` through new tests is straightforward but project-wide suppression should be tightened to file/method scope in a hardening sprint. [tests/Hexalith.Parties.Tests/Hexalith.Parties.Tests.csproj]

## Deferred from: code review of 11-4-tenants-integration-tests-deployment-validation-and-documentation (2026-05-05)

- 7 residual full-topology integration test failures — `party/process` returns 500 → surfaces as 422 in create-path E2E tests. Pre-existing; dev notes (line 244) acknowledge the failures are unrelated to Tenants authorization. Investigate as a separate hardening item.
- `EventStore.Contracts.Results.DomainResult` coupling pulled into Parties tests via `Hexalith.Tenants.Testing` — the Tenants helper APIs return this type. Acceptable as the Tenants public API surface; defer to architecture-fitness follow-up if test-project boundaries are tightened.
- `PrivateAssets="all"` on the `Hexalith.Tenants.Testing` reference does not enforce a build-time prohibition against a future `src/` project taking the same dependency. Add an architecture-fitness test that forbids `Hexalith.Tenants.Testing` references under `src/`.
- `[Collection("DeployValidation")]` shared across `DeploymentValidationTests` and `TenantsDeploymentValidationTests` with overlapping `Path.GetTempPath()` patterns — low flake risk in practice; defer dedicated fixture isolation work.
- `Path.GetTempPath` Guid-prefix collision risk in `TenantsDeploymentValidationTests` — Edge Case Hunter himself flagged the probability as negligible.
- `PollAsync` first iteration wastes one delay cycle when convergence is immediate — Edge Case Hunter flagged as harmless boundary condition.
- `tenant-state-stale` recovery transition (until-recovers branch) not covered by integration test — denial path is sufficient for AC; defer recovery test to a separate hardening story.
- `ProjectFromTenantsAsync` silently drops unknown event types in `InMemoryTenantProjection.ApplyEvents` — projection-conformance test belongs in the upstream Hexalith.Tenants test suite, not in Parties.
- `validate-deployment.ps1` has no real Parties-source bypass detection — only the synthetic `bypassTenantsAuthorization` YAML flag is checked. Defer broader detection (missing `MapTenantEventSubscription()`, missing `ITenantAccessService` registration, etc.) to a deployment-validation hardening pass.


## Deferred from: code review of 11-3-rest-and-mcp-tenant-authorization-enforcement (2026-05-05)

- Multiple `eventstore:tenant` claims silently collapse to the first via `FindAll(...).FirstOrDefault(...)` in PartiesController, AdminController, and the MCP `RunSessionHandler` — pre-existing extraction behavior, no per-request tenant selector. Out of Story 11.3 scope; revisit if multi-tenant tokens become a supported issuance pattern. [src/Hexalith.Parties/Controllers/PartiesController.cs ExtractTenant; AdminController.cs:46-49; Extensions/PartiesServiceCollectionExtensions.cs:311-313]
- MCP authorization failures are surfaced as `InvalidOperationException` with the reason code embedded in the free-text message rather than a structured error envelope. The story spec accepts a string contract; clients parse `ShouldContain("missing-tenant")`. Refactor to a typed `McpException` with `reasonCode` field is larger; revisit if MCP clients need machine-readable error parsing. [src/Hexalith.Parties/Mcp/McpTenantAuthorization.cs:13-30]
- Long-running `Task.Run` background operations on `AdminController` (projection rebuild, key rotation/destruction, multi-phase erasure) authorize once on the request thread and continue with `CancellationToken.None`. A tenant disabled mid-operation will still see all subsequent side-effects complete. Pre-existing fire-and-forget pattern; spec scope is request-boundary fail-closed. Revisit if periodic re-authorization of long jobs is desired. [src/Hexalith.Parties/Controllers/AdminController.cs:110-146, 335-535]
- `RebuildProjections` accepts `request.PartyId` and forwards it to `rebuildService.RebuildDetailProjectionAsync(tenantId, partyId, ...)` without validating that `partyId` is a valid GUID or that it scopes within the supplied tenant. Rebuild-service responsibility, out of authorization-enforcement scope for Story 11.3. [src/Hexalith.Parties/Controllers/AdminController.cs:121]
- `TenantLocalState.Members` and `InMemoryTenantProjectionStore` use `StringComparer.Ordinal`. JWT `sub` and `eventstore:tenant` claim casing can vary across IdPs; benign casing differences fail closed with `UnknownTenant`/`MissingMember`. Hexalith.Tenants library concern, raise upstream. (Already noted in 11-2 deferred list; reaffirmed by 11-3 review.)
- Tenant id logged at `Warning` on every authorization denial via `logger.LogWarning("Tenant access denied. ... Tenant={Tenant}, ...")`. Anonymous attackers can flood Warning logs with arbitrary tenant claims; consider `Information` for client-faced denials or per-tenant rate-limit. Operational tuning, not a correctness bug. [src/Hexalith.Parties/Controllers/PartiesController.cs:771-777; AdminController.cs:67-71]


## Deferred from: code review of 11-2-tenants-event-consumption-and-local-access-projection (2026-05-05)

- Whitespace not trimmed from `tenantId`/`userId` in `TenantAccessService` — values like `"user-1\r"` survive `IsNullOrWhiteSpace` and miss in lookups, returning `UnknownTenant`/`MissingMember` instead of failing fast. Normalization belongs in Story 11-3's REST/MCP request pipeline / claims transformation. [src/Hexalith.Parties/Authorization/TenantAccessService.cs]
- Case sensitivity of `userId` lookups — `Members` dict uses `StringComparer.Ordinal`; JWT `sub` casing varies by IdP. Requires alignment with Tenants client convention; not Parties-owned. [Hexalith.Tenants/src/Hexalith.Tenants.Client/Projections/TenantLocalState.cs:26-27]
- `TenantRole.TenantOwner = 0` is the default enum value — a malformed `UserAddedToTenant` payload with missing `Role` field silently grants `TenantOwner`. Tenants client contract concern; raise upstream for a non-zero default or explicit-required serialization. [Hexalith.Tenants/src/Hexalith.Tenants.Contracts/Enums/TenantRole.cs]
- `TenantStatus.Active = 0` is the default enum value — a malformed event without `Status` defaults to `Active` (fail-open). Tenants client contract concern; raise upstream. [Hexalith.Tenants/src/Hexalith.Tenants.Contracts/Enums/TenantStatus.cs]
- DAPR retry / dead-letter not configured for `FailedInvalidPayload` — the Tenants subscription endpoint returns 500 on invalid payloads, which DAPR will redeliver indefinitely. Configure resilience policy + DLQ at the AppHost / DAPR component level. Naturally Story 11-4 (deployment validation) scope. [src/Hexalith.Parties/appsettings.json]
- Cold-start denial trap not explicit in docs — after a process restart, the in-memory projection is empty so every access decision is `UnknownTenant` until events are republished. `tenant-access-projection.md` covers the in-memory limitation but does not call out the cold-start denial behavior explicitly. (Resolved by review patch — added paragraph in `docs/tenant-access-projection.md`.)
- `/tenants/events` endpoint authorization posture — endpoint is mapped after `UseAuthorization` but does not call `RequireAuthorization()` and the Tenants client does not enforce DAPR app-id verification. Story 11-1 (currently `blocked`) is supposed to own DAPR access-control YAML; deferred to **Story 11-4 deployment validation** to add a probe that fails closed if the access-control YAML does not restrict the Tenants topic to the expected publisher app id. [src/Hexalith.Parties/Program.cs:49]
- Replace bespoke `WebApplication.CreateBuilder()` subscription test with `WebApplicationFactory<Program>`-based composition test — current test asserts route metadata but bypasses the full Program.cs middleware pipeline. Deferred to **Story 11-4** integration suite where the WebApplicationFactory infrastructure is naturally exercised. CI flake hardening already applied via `WebApplicationOptions` content-root pin. [tests/Hexalith.Parties.Tests/Tenants/TenantEventInfrastructureTests.cs]


## Deferred from: code review of 9-6-hexalith-memories-backed-party-search — fifth pass / Chunk B-1 search subsystem (2026-05-04)

- **AC6 deployment validation does not check cleanup capability** — `MemoriesSearchHealthCheck` only probes `HybridSearchAsync`; no per-unit DELETE-route probe, no `IPartyMemoryUnitMappingStore` round-trip, no smoke of `PartyMemoryCleanupService`. The named test `OperationalValidationReportsMemoriesEndpointAuthHealthProvisioningAndCleanup` promises "AndCleanup" but the assertions don't cover it. Outside chunk B-1 file scope (HealthChecks/* not in this diff); carry to a follow-up review of HealthChecks/* before story 9-6 moves to `done`. AC5 is a GDPR requirement; AC6 explicitly demands deployment-time visibility into cleanup readiness. [src/Hexalith.Parties/HealthChecks/MemoriesSearchHealthCheck.cs]
- **Per-party mapping list grows unbounded over time** — `RecordMappingAsync` deduplicates by `MemoryUnitId`, but if Memories' stable-id property does not hold (Memories restart, contract change), the list grows with ghost ids. Eventually exceeds Dapr state-store value-size limit. Depends on resolved decision around `MemoryUnitId` vs `workflowInstanceId` semantic mismatch (D1 in the fifth-pass review); defer until D1 resolved, then revisit cap / TTL / dedup-by-SourceUri. [src/Hexalith.Parties/Search/PartyMemoryUnitMappingStore.cs:50]
- **`LocalFuzzyPartySearchProvider` materializes unbounded `entries` IEnumerable** — pre-existing perf concern not introduced by 9-6; an actor-state of millions of parties materializes wholesale in memory. OOM in worst case; latency in average case. Add a count guard with logged warning above a threshold, or stream + paginate, when local-search hardening becomes a profiled hotspot. [src/Hexalith.Parties/Search/LocalFuzzyPartySearchProvider.cs:46]
- **`MaxMemoriesTopK = 200` magic constant should be options-bound** — hygiene; tracked as part of the broader pagination patch P1. Operator can't increase candidate pool for tenants with high filter rates without a redeploy. Bind from `PartyMemorySearchOptions` when revisiting pagination strategy. [src/Hexalith.Parties/Search/MemoriesPartySearchService.cs:28]

## Deferred from: code review of 9-6-hexalith-memories-backed-party-search — fourth pass / Chunk B (2026-05-04)

- Authority-Boundary leak — `PartyProjectionUpdateOrchestrator` imports the `Hexalith.Parties.Search` namespace (`PartyMemoryIndexingService`, `PartyMemoryUnitMappingContext`, `PartyMemorySearchOptions`). The orchestrator is now bound to the Memories-search subsystem and cannot be unit-tested without it. Introduce an `IPartyMemoryIndexer` abstraction in projection abstractions and DI-wire the concrete implementation when the next layering pass occurs. [src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs:7]
- Synchronous AC1 indexing in command pipeline couples command latency to Memories availability — every command pays inline indexing latency. AC1 does not require synchronous indexing; an event-driven hand-off (background reminder, outbox) would honor slice-1 intent without the coupling. Architecture-level decision; revisit when AC1 reindex-on-every-event burst becomes a profiled hotspot. [src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs]
- `GetEventsAsync(0)` reads ALL events from origin every command — quadratic delivery cost for long-lived aggregates. Per-event idempotency check inside the projection only prevents duplicate APPLY, not duplicate FETCH/DECRYPT. Pre-existing structural issue (spec line 209). Revisit when `AggregateActor` exposes a `GetEventsAsync(fromSequence)` overload. [src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs:50,219-220]
- `WaitAsync(cancellationToken)` cancels the wait, not the underlying actor RPC — Dapr proxy limitation; cannot be fixed without proxy-level support for cancellation propagation. Document the leak in the orchestrator XML doc. [src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs:49-52]
- Projection update non-transactional across detail+index actors — sequential awaits with no compensation; partial failures leave detail and index out of sync until the next replay-from-zero. Outside the Dapr actor model; needs broader two-phase or outbox design. [src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs:112-127]
- Index actor `_lastProcessedSequencePerParty` map grows unbounded — every distinct partyId in a tenant adds a key; never evicted. Long-running actor instance for tenants with millions of parties leaks memory. Add an LRU cap or TTL when actor lifecycle work resumes. [src/Hexalith.Parties.Projections/Actors/PartyIndexProjectionActor.cs:34,154,178-179]
- Static `s_lastKnownDetails` ConcurrentDictionary on `PartyDetailProjectionActor` shared across all actor instances — pre-existing pattern (not introduced by 9-6); spec line 265 already deferred. Carry-forward. [src/Hexalith.Parties.Projections/Actors/PartyDetailProjectionActor.cs]
- `PartyEventTypeResolver.s_resolvedCache` unbounded — bounded by event-type string space (closed-world) once the `Type.GetType` patch lands; spec line 348 already deferred. Carry-forward. [src/Hexalith.Parties.Projections/Actors/PartyEventTypeResolver.cs]
- `DateTimeOffset.UtcNow` used inside `Apply` handlers (`LastModifiedAt` etc.) — replay-from-zero re-derives timestamps to "now", making projection rebuilds non-deterministic. Cross-story projection-determinism concern (matches the 9-5 deferred-work entry). Bundle into a single projection-determinism hardening story. [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:111-112,143-144]
- Replay-from-zero re-applies non-`PartyCreated` events on every command — pre-existing structural issue not introduced by 9-6 (spec line 209 already deferred). Carry-forward.
- `PartyState` rejection-Apply reflection-ordering AOT/trimming risk — fitness test mitigates source-side regressions; long-term fix is rehydrator-side full-name match in `Hexalith.EventStore.Client.Handlers.DomainProcessorStateRehydrator`, a cross-package change. Spec line 327 already noted; carry-forward and add an AOT/trimming compatibility test when the EventStore client next opens for change. [src/Hexalith.Parties.Contracts/State/PartyState.cs:47-93]
- GDPR cleanup precondition for plaintext-PII decision (resolved decision #1) — Chunk B redaction-and-continue path assumes AC5 cleanup works, but Chunk A audit (spec line 388) shows the per-unit mapping rewrite still pending. Track as the open Chunk A patch item, not a Chunk B regression. [src/Hexalith.Parties/Search/PartyMemoryCleanupService.cs]
- `CaseId` whitespace/control-char/bidi-mark sanitization at orchestrator — Chunk A controller already validates `caseId` (length + CR/LF + control chars); orchestrator-level check is hardening only. Revisit when controller validation contract is consolidated into a shared `CaseIdValidator`. [src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs:175]

## Deferred from: code review of 9-6-hexalith-memories-backed-party-search — third pass / Chunk A (2026-05-04)

- Concurrent indexing of the same party from two near-simultaneous projection events races on Memories storage — no idempotency key beyond `sourceUri` is passed to `IngestAsync`, and the SDK call site silences `HXL001` for the obsolete API. Partially mitigated when the AC5 per-unit mapping lands (resolved decision #2), but the underlying race against the Memories backend remains. Revisit when Memories exposes a non-deprecated indexing path or an explicit idempotency token. [src/Hexalith.Parties/Search/PartyMemoryIndexingService.cs:30-50]
- `unit.Content` is built unbounded from `SearchableContactChannels` and `SearchableIdentifiers`; a legacy migration with thousands of identifiers per party balloons the body, risking OOM client-side or 413 server-side. Cap content size or chunk identifiers across multiple memory units once a real load profile materializes. [src/Hexalith.Parties/Search/PartyMemoryUnitMapper.cs BuildContent]
- `LocalFuzzyPartySearchProvider` rejects fuzzy match on tokens containing any digit — affects legitimate display names with digits (`"3M Company"`, `"Audi Q5"`). Already deferred in spec line 344 as an intentional cross-id false-positive tradeoff. Carry forward; revisit if identifier-typo recovery becomes a use case. [src/Hexalith.Parties/Search/LocalFuzzyPartySearchProvider.cs]
- `TraverseAsync` is hard-coded to `depth: 2`; deeper relationships are silently truncated. Config concern, no acute bug. Make depth tunable via options when graph search is exercised against real datasets. [src/Hexalith.Parties/Search/MemoriesPartySearchService.cs:117-122]
- `ResolveGraphStartNodeIdAsync` uses a syntactic-axis search with the URN as free-text query to find the memory unit for a given source URI; the canonical match may not be in the top 5 syntactic hits and graph mode silently degrades to local. Depends on Memories SDK adding a URI-keyed lookup endpoint. [src/Hexalith.Parties/Search/MemoriesPartySearchService.cs:181-188]
- `Uri.EscapeDataString(memoryUnitId)` produces `%2F` for embedded `/`, which IIS (`AllowDoubleEscaping=false`) and some Kestrel configurations reject as a 400. Server-config concern — document required server configuration in operations runbook rather than rewriting the cleanup contract. [src/Hexalith.Parties/Search/PartyMemoryCleanupService.cs:40, 684]
- `IPartySearchService` and the `PartySearchRequest`/`PartySearchResponse`/`PartySearchScoreMetadata`/`PartySearchSourceMetadata` DTOs are declared `public` in `Hexalith.Parties.Search` while only used internally. The Authority-Boundary constraint focuses on Memories types not leaking into Parties.Contracts (which holds), but exporting CommandApi-internal contracts as `public` couples future sibling integration projects. Already deferred in spec line 351 — carry forward. [src/Hexalith.Parties/Search/PartySearchBoundary.cs]
- `PartyMemorySearchOptionsValidator` does not deduplicate `EnabledAxes`; `IsAxisEnabled` linear scan tolerates duplicates but wastes work and obscures operator intent. Already deferred in spec line 347 — carry forward. [src/Hexalith.Parties/Search/PartyMemorySearchOptions.cs]
- `PartyMemoryCleanupService.CreateBlockedResult` is a static factory exposed on the service class but conceptually belongs on `PartyMemoryCleanupResult`. Architectural-cleanup only; no behavior change. [src/Hexalith.Parties/Search/PartyMemoryCleanupService.cs:792-802]

## Deferred from: code review of 9-6-hexalith-memories-backed-party-search — second pass (2026-05-03)

- Replay-from-zero re-applies non-`PartyCreated` events on every command — root cause for the AC1-reindex burst flagged in this pass. Pre-existing structural issue not introduced by 9-6 acceptance criteria. Bundle with the projection-determinism hardening story. [src/Hexalith.Parties/Domain/PartyProjectionUpdateOrchestrator.cs DeliverProjectionAsync]
- `Aspire.Hosting.Keycloak` / `Aspire.Hosting.Kubernetes` pinned to `13.2.2-preview.1.26207.2` while core Aspire is `13.2.2` and `Aspire.Hosting.Testing` is `13.2.1`. Pre-existing dependency skew. Bundle into a single Aspire-bump PR. [Directory.Packages.props:26-27]
- `NameHistory.Where(...).OrderBy(ChangedAt).LastOrDefault()` is O(n log n) per request and re-sorts a naturally chronological list. Perf only — re-evaluate when temporal-name endpoints become a profiled hotspot. [src/Hexalith.Parties/Controllers/PartiesController.cs:401; src/Hexalith.Parties/Mcp/GetPartyNameAtMcpTool.cs:1588-1591]
- `LocalFuzzyPartySearchProvider` rejects fuzzy match on tokens containing digits — intentional tradeoff to suppress cross-id false positives, but legitimate fuzzy hits on identifier typos (e.g., passport-number transposition) are lost. Revisit if identifier-typo recovery becomes a use case. [src/Hexalith.Parties/Search/LocalFuzzyPartySearchProvider.cs:1672-1677]
- Three independent string tables map search mode → axis (`MemoriesPartySearchService.MapModeToAxis`, `PartiesController.ParseSearchMode`, `FindPartiesMcpTool` mode parsing). Style/maintenance only — factor when the next axis is added. [Multiple call sites]
- `PartiesServiceCollectionExtensions` performs boot-time validation that duplicates `PartyMemorySearchOptionsValidator`. Two enforcement paths can drift; collapse to validator-only on next DI refactor. [src/Hexalith.Parties/Extensions/PartiesServiceCollectionExtensions.cs:1133-1137]
- `EnabledAxes` validator does not deduplicate the array; `IsAxisEnabled` linear-scan tolerates duplicates but wastes work. Cosmetic. [src/Hexalith.Parties/Search/PartyMemorySearchOptions.cs:2447]
- `PartyEventTypeResolver` `s_resolvedCache` has no eviction; bounded by event-type string space (closed-world). Adversarial expansion via `HandleSerializedEventAsync` requires authenticated ingress, so not a practical DoS vector. [src/Hexalith.Parties.Projections/Actors/PartyEventTypeResolver.cs:3173]
- `PartyMemoryUnitMapper` does not bound `context.Timestamp` against `DateTimeOffset.MinValue`/`MaxValue` — rare and the test fixture does not currently produce it. [src/Hexalith.Parties/Search/PartyMemoryUnitMapper.cs:2649]
- `MemoriesPartySearchService.SearchAsync` materializes `entries` via `[..]` enumeration; if the underlying yield-iterator throws partway, the request 500s instead of degrading. Rare and probably indicates state-store corruption that should not be masked. [src/Hexalith.Parties/Search/MemoriesPartySearchService.cs:1796]
- `IPartySearchService` and the `PartySearchRequest`/`PartySearchResponse` DTOs are declared `public` in CommandApi. The Authority-Boundary constraint focuses on Memories types not leaking into Parties.Contracts (which holds), but exposing CommandApi-internal contracts as `public` couples future sibling integration projects. Tighten to `internal` when next reviewing module boundaries.
- `ProbingMemoriesClient` test fixture inherits the real `MemoriesClient` rather than mocking an interface. If the SDK becomes `sealed` upstream every test breaks. Switch to a mockable abstraction when the SDK forces it. [tests/Hexalith.Parties.Tests/Search/MemoriesPartySearchServiceTests.cs ProbingMemoriesClient]

## Deferred from: code review of 9-6-hexalith-memories-backed-party-search (2026-05-03)

- `LocalFuzzyPartySearchProvider` performs synchronous in-memory scoring without observing cancellation once the request enters local fallback. This is real for large tenant indexes, but the fuzzy provider predates the Memories integration and should be handled as local-search hardening rather than blocking the Search Core review chunk. [src/Hexalith.Parties/Search/LocalFuzzyPartySearchProvider.cs:55]

- Static `s_lastKnownDetails` ConcurrentDictionary on `PartyDetailProjectionActor` — pre-existing pattern (not introduced by 9-6). Static actor state is an anti-pattern that compounds under the new synchronous-projection load path. Hardening belongs in a separate Tier-3 story (e.g., move the cache to per-actor instance state with a bounded eviction policy, or remove it once the actor service can satisfy reads from the projection store). [src/Hexalith.Parties.Projections/Actors/PartyDetailProjectionActor.cs]
- AppHost does not wire `Parties__MemoriesSearch__ApiToken` env var — deployment-config concern. Follow up when the cleanup-auth patch lands so dev/staging/prod all have a coherent token-injection path. [src/Hexalith.Parties.AppHost/Program.cs]
- `ContractsArchitectureFitnessTests` resolves the contracts csproj via hardcoded `..\..\..\..\..\src\Hexalith.Parties.Contracts\...` — works in current CI structure but breaks if the test runner cwd or output layout changes. Refactor to `[CallerFilePath]` or an MSBuild-injected source-project path when next touching this test. [tests/Hexalith.Parties.Tests/FitnessTests/ContractsArchitectureFitnessTests.cs]

## Deferred from: code review of 9-5-temporal-name-queries (2026-05-03)

- `ApplyErasure` wipes `ConsentRecords` along with PII — GDPR Art.7(1) requires retaining the lawful-basis audit trail; revisit with the consent-management owners (story 9.4). [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:822-830]
- `HandleConsentRevoked` returns `null` when the consent id is unknown — out-of-order or replayed revocations are silently dropped, leaving the read model showing the consent as still active. (story 9.4) [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:912-919]
- `UpdatePersonDetails` typed-shape JSON branch does not fall back to the route id when the body omits `partyId`, inconsistent with the flat-shape branch a few lines below. Composite-command area (epic 4). [src/Hexalith.Parties/Controllers/PartiesController.cs:312-331]
- `GetPartyDetailAsync` JSON-string emptiness check trims to `"{}"` only, while the byte-array path uses `JsonNode.Parse` — `"{ }"` slips through the string path and produces a non-null `PartyDetail` with default fields. Refactor to a single `IsEmptyJsonPayload` helper. [src/Hexalith.Parties/Controllers/PartiesController.cs:428-440]
- `ParseSearchMode` returns `Hybrid` for any unknown value — caller typos silently switch mode instead of returning 400. Search area (story 9.6). [src/Hexalith.Parties/Controllers/PartiesController.cs:557-564]
- `SetSearchMetadataHeaders` strips only CR/LF from `DegradedReason`; other control chars (NUL, VT, FF, DEL) still break Kestrel header serialization. Search area (story 9.6). [src/Hexalith.Parties/Controllers/PartiesController.cs:545-555]
- `MessageId = Guid.NewGuid()` is minted per-request inside the controller, defeating command-level idempotency on client retries. Should come from a client-supplied correlation id. CQRS-pattern multi-story concern. [src/Hexalith.Parties/Controllers/PartiesController.cs:257, 274]
- `TryUpdateProjectionAsync` swallows non-cancellation exceptions and still returns 202 Accepted — clients depending on read-after-write get a false-positive accept and a stale read with no degraded-header signal. Search/projection area (story 9.6). [src/Hexalith.Parties/Controllers/PartiesController.cs:287-308]
- New `ConsentRecorded`, `ConsentRevoked`, `ProcessingRestricted`, `RestrictionLifted` handlers ship without any unit-test coverage in the 9.5 diff. Consent/restriction area (story 9.4). [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:894-932]
- Diff range `c870bcb^..HEAD` includes substantial work outside 9.5 acceptance criteria (search modes, consent records, restriction events, command-DTO reshape, MessageId injection) — expected because the original combined story 9.5 was split after the work was already merged. Flagged so future reviewers know not to chase the same items again.
- `NameHistoryEntry.ChangedAt` is stamped from `DateTimeOffset.UtcNow` at apply time, not the original event timestamp — on projection rebuild every entry collapses to the rebuild wall-clock, weakening AC5 ("rebuilt deterministically") and turning historical `?at=...` queries into 404 / current-name. Equal-millisecond ties also make `LastOrDefault(e => e.ChangedAt <= at)` non-deterministic. Fix needs to thread the envelope timestamp through `Apply(string, IEventPayload, PartyDetail?)` (handler signature + `ProjectionRebuildService` plumbing) or stamp `ChangedAt` on the event payload at command time — both touch the parallel `PartyIndexProjectionHandler`. Split into a dedicated projection-determinism hardening story. [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:91, 119, 141]
- `PartyCreated` arriving for a party whose detail state is non-null is silently no-op'd — the existing test pins this behavior intentionally for replay idempotency, but if state was seeded by an out-of-order non-creation event first, `NameHistory` stays empty forever and the temporal endpoint returns 404 indefinitely. Needs a design discussion on projection-rebuild semantics before flipping the guard. [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:15]
- `NameHistory` grows unbounded inside `PartyDetail` actor state and `GET /name-history` has no pagination or size guard — once the per-key state payload exceeds the Dapr state-store limit the projection wedges. Needs a GDPR retention policy + pagination protocol designed with the consent/legal owners. [src/Hexalith.Parties.Projections/Handlers/PartyDetailProjectionHandler.cs:130-146; src/Hexalith.Parties/Controllers/PartiesController.cs:354-382]

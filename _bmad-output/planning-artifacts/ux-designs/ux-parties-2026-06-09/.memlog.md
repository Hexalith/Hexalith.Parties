---
topic: Parties UI — Admin + Consumer GDPR portals UX
updated: 2026-08-18T23:33
---

- (event) 2026-08-18 validation re-run (rubric + implementation-drift + accessibility + regulated-language lenses): 0 critical / 7 high / 22 medium / 24 low; consolidated in validation-report.md. Update pass launched to roll spine-side findings in. Prior run history lives in legacy .decision-log.md.
- (decision) Freshness contract stays 3-state (fresh / stale 'as of HH:MM' / degraded=Info) — code fix queued: shipped indicators collapse to 2-state Warning and no real surface shows as-of; host component already supports both.
- (decision) Per-area density contract stays (--fc-spacing-unit 4px admin / 6px consumer) — code fix queued to wire the shell density posture; DESIGN spacing section unchanged.
- (decision) Object (Art. 21): consumer surface offers an honest contact path until a backend objection command exists; spine row marked blocked-on-backend with assessment-not-toggle framing. Rendered-but-disabled rights buttons are banned.
- (decision) Spine adopts code where code made the better call: gdpr-destructive-button rebinds to matched pair DangerBackground3 + DangerForegroundInverted; 'command result toast' becomes inline status region (no toasts); consumer erasure request = reversible pattern, single FluentDialog confirm, no typed transcription (typed-name stays Admin-only); erase trigger blessed as Outline with danger-filled in-dialog Confirm; sign-in documented as delegated to the tenant IdP hosted page (3.3.8 binds the IdP config).
- (decision) TenantUnavailable warming-up copy stays spine-mandated — code copy fix queued (AdminPortalLabels.cs). Picker FAST-to-Fluent-2 re-skin debt retired (resolved in code); standing picker requirements added: real clear button, shadow-root focus ring, role=status result announcements.
- (change) Spines updated 2026-08-18 from validation findings. EXPERIENCE: /no-party-binding IA+state row back-ported from shipped surface; Restricted (Art. 18(3)) / Export preparing-ready state rows; erasure row gains consent-paused, cancel-too-late verbatim rejection, PII-free request reference, verified-deletion wording; Flow 5 (consumer erasure, Marc); /me/* routes + nav-reachability rule + Help & contact IA row (3.2.6); Foundation notes IdP-delegated sign-in + production-KMS launch gate; Accessibility Floor gains 3.3.8 (binds IdP config), 2.4.11 focus-not-obscured, forced-colors surviving selection indicators; detail-sheet focus contract; toast renamed status region; picker gains role=status result announcements + real clear button. DESIGN: gdpr-destructive-button rebound to matched danger pair; freshness pinned 3-state + as-of; dark-mode contrast acceptance check; links-never-raw-accent; FluentSwitch/Dialog/MessageBar/Skeleton added to as-is inventory; picker re-skin debt retired with two standing requirements; forced-colors + FAST-token Do/Don't rows; 'Data-freshness indicator' renamed 'Freshness indicator'.
- (change) Mockups patched: signin .alt a link to brand-fill; admin restricted badge tint to #fff9f5 matched pair; create-edit rejected state wired (role=alert, aria-invalid, aria-describedby), picker clear as real button, per-option ids, focus-within ring; consumer-privacy neutral Off sub-labels, switch aria-describedby state lines, Object button replaced with contact-path text, freshness role=status; consumer-profile 'erase' to 'delete', freshness role=status. Note: consent switch rows carry no lawful-basis sub-text element in the mock — only the state line is describedby-wired (no basis copy invented).
- (assumption) Code fixes queued for bmad-build (spine wins, not spine work): consumer nav reachability (/me/consent nav entry, /me/privacy nav entry, Edit action on My profile); portals off FAST --*-rest tokens onto Fluent 2; consumer erasure confirm to real FluentDialog; admin danger-fill hex to token pair; freshness 3-state + as-of on real surfaces; wire --fc-spacing-unit density per area; shared PartyStateBadge (FluentBadge, Circular) consumed by both portals; TenantUnavailable warming copy; remove dead Object button (contact path per spine); extend a11y style-guard roots to all four UI projects + axe on real routes; stale auto-refresh consumes shipped SignalR mechanism.
- (event) spines finalized

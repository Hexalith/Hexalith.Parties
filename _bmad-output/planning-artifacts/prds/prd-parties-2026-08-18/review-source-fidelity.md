# Source-Fidelity and Currency Review — parties-ui-prd.md

Reviewed: `_bmad-output/planning-artifacts/parties-ui-prd.md` against the repository as of 2026-08-18.

## Method

Verified all six §Source Artifacts paths on disk; cross-checked the §Traceability Matrix FR→Epic mapping and route claims against `_bmad-output/planning-artifacts/epics.md` (Requirements Inventory, FR Coverage Map, per-epic "FRs covered") and the actual `@page` directives in `src/Hexalith.Parties.AdminPortal` and `src/Hexalith.Parties.ConsumerPortal`; compared §Current Implementation Evidence against today's `_bmad-output/implementation-artifacts/sprint-status.yaml` (`last_updated: 2026-08-18`); ran `git log --follow` on the PRD to audit frontmatter-date maintenance; enumerated consumers via `rg -l "parties-ui-prd"` and read the fitness tests in `tests/Hexalith.Parties.Tests/FitnessTests/` (including `EpicEightClosureFitnessTests.cs` and `DocumentationFitnessTests.cs`); spot-checked NFR8/NFR9 technical claims against `src/Hexalith.Parties.UI/Program.cs`, `Directory.Build.props`, `Directory.Packages.props`, the `.slnx` files, test csprojs, `tests/e2e/specs/`, and `.gitmodules`; and verified §Out of MVP Scope claims against `src/Hexalith.Parties.Mcp/Tools/PartiesMcpToolNames.cs`, `docs/deferred-search-and-temporal-queries.md`, and the git history of `docs/deployment-security-checklist.md`.

## Findings

- **[high]** Frontmatter date was never maintained across later edits (§frontmatter, `date: 2026-06-27`) — `git log --follow` shows the PRD was materially edited twice after creation: f93534e (2026-07-06, added the whole "Post-MVP maintenance status" block including the Epic 8 paragraph citing `sprint-change-proposal-2026-07-06.md`) and df2cc6e (2026-07-16, added the "Scope invariant" paragraph). The frontmatter still says 2026-06-27, so the document is internally inconsistent: a body paragraph cites an SCP dated nine days after the document's declared date. Readiness tooling treats this file as the selected PRD and records its currency (`_bmad-output/planning-artifacts/implementation-readiness-report-2026-07-07.md` lines 12–13 select it and line 57 logs "modified 2026-07-06 21:47"), so a false date marker directly undermines currency assessment of the canonical requirements source. *Fix:* update `date` to the true last-edit date (2026-07-16) or replace with `created`/`last_updated` fields — but see the freeze finding below before editing.

- **[high]** The PRD is frozen byte-for-byte by an active fitness gate; any corrective edit will fail CI (§whole document) — `tests/Hexalith.Parties.Tests/FitnessTests/EpicEightClosureFitnessTests.cs` (`EpicEightAddsNoPrdFunctionalRequirement`, lines 153–178) asserts `git diff --name-only 37f4ec8 -- _bmad-output/planning-artifacts/parties-ui-prd.md _bmad-output/planning-artifacts/epics.md` is empty ("Epic 8 cannot change the canonical PRD or epic/FR inventory") and that no untracked copies of those two paths exist; `BaselineCommit` 37f4ec8 is the current HEAD. This is the strongest consumer dependency found: it depends on the PRD's exact bytes, not on section names or FR IDs. Every fix suggested in this review therefore breaks the Epic 8 zero-PRD gate unless the baseline constant is advanced (or the change is governed as an explicit non-Epic-8 correction with the test updated in the same change). *Fix:* batch all PRD corrections into one governed docs change that also advances `BaselineCommit`, ideally after Story 8.10 closes; do not hand-edit the PRD in isolation.

- **[high]** §Current Implementation Evidence is stale against today's sprint status (§Current Implementation Evidence) — the section's facts remain individually true (see Verified accurate) but it no longer describes reality: `sprint-status.yaml` (`last_updated: 2026-08-18`) shows `epic-8: in-progress` with 8-1…8-6 and 8-11…8-13 `done`, 8-7 and 8-8 `blocked` (G5 payload engine / owner-surface prerequisites), 8-9 `backlog`, and 8-10 `review` with a detailed 2026-08-18 closure-evidence annotation. The PRD describes Epic 8 only as "approved by `sprint-change-proposal-2026-07-06.md`" backlog scope and knows nothing of stories 8.11–8.13 (added by the 2026-07-07/07-08 correct-course SCPs), nor that Epic 6 is `done` (epic-6, sprint-status line 109). A reader treating this section as evidence would conclude Epic 8 is un-started. The section's own escape hatch ("Readiness validation after this date must reconcile…") mitigates but does not cure it, because the "as of" anchor (2026-06-27) predates even the section's own Epic 7/8 content. *Fix:* refresh the section with a current as-of date, Epic 6/7 done status, the Epic 8 story-level state (8.1–8.6, 8.11–8.13 done; 8.7/8.8 blocked; 8.9 backlog; 8.10 in review), and note the 8.11–8.13 scope additions — coordinated with the baseline-freeze fix above.

- **[medium]** "Epic 7 … preserves rollback paths" is now superseded for the projection path (§Current Implementation Evidence, Epic 7 bullet) — the PRD says Epic 7's final readiness record "preserves rollback paths and deferred deletion-safe cleanup". As a description of the historical Epic 7 record that is true, but as current state it misleads: the Epic 7 projection-rollback retention action item in `sprint-status.yaml` (action_items, epic 7) was closed 2026-08-01 under Story 8.6 — "the rollback-only actor/rebuild/adapter paths were removed" after governed parity evidence, with a targeted revert of the 8.6 change set as the remaining code rollback. Only the crypto/key-management retention (Story 8.7 gate) remains open. *Fix:* qualify the bullet: projection rollback-only paths were removed 2026-08-01 under the governed retention SCP; crypto/key-management rollback paths remain preserved pending Story 8.7.

- **[medium]** KMS prerequisite's documented home was retired; the PRD's source artifact still points at the deleted file (§Out of MVP Scope, first bullet; §Source Artifacts conflict rule) — the PRD's claim itself ("Production KMS provisioning is a deployment prerequisite … not a UI feature story") still holds and is still documented (`docs/index.md:86` "Provision a real KMS first", `docs/getting-started.md:446` production KMS gate, `docs/development-guide.md:163`). But `docs/deployment-security-checklist.md` — the checklist that owned this gate since Story 1.10 — was deleted by commit 30c5fd9 "Retire legacy deployment artifacts" (Story 8.13), and `_bmad-output/planning-artifacts/architecture.md:41` (a §Source Artifacts document that "wins" on system decisions per the PRD's own conflict rule) still references the deleted file, as does `_bmad-output/project-context.md:257`. A readiness run following the PRD's conflict rule into architecture.md lands on a dead path. *Fix:* no PRD text change strictly required; flag architecture.md:41 and project-context.md for repointing to the surviving KMS documentation (docs/index.md §GDPR / docs/getting-started.md).

- **[low]** NFR8 "The UI host uses ServiceDefaults" predates the Story 8.4 Commons cutover (§NFR8) — still literally true, but the local `Hexalith.Parties.ServiceDefaults` wrapper the sentence originally referred to no longer exists (no such project under `src/`; Story 8-4 leaf-project retirement is `done`; historical story record `_bmad-output/implementation-artifacts/1-10-…` still cites `src/Hexalith.Parties.ServiceDefaults/Extensions.cs`). The host now consumes `Hexalith.Commons.ServiceDefaults` directly: `src/Hexalith.Parties.UI/Program.cs` line 1 `using Hexalith.Commons.ServiceDefaults;`, line 33 `builder.AddHexalithServiceDefaults(…)`, line 226 `app.MapHexalithDefaultEndpoints(…)`; `Hexalith.Parties.UI.csproj` lines 48–49 reference the Commons package/source project. A reader could search for a local ServiceDefaults project and conclude NFR8 is unmet. *Fix:* reword to "uses Hexalith.Commons.ServiceDefaults" when the PRD is next unfrozen.

## Verified accurate

- All six §Source Artifacts paths exist on disk: `architecture.md`, `epics.md`, the three `ux-designs/ux-parties-2026-06-09/` files (DESIGN.md, EXPERIENCE.md, validation-report.md), and `docs/index.md`.
- §Traceability Matrix FR→Epic ownership matches `epics.md` exactly: FR Coverage Map (epics.md lines 342–350) and per-epic "FRs covered" headers (Epic 1 → FR-Shell; Epic 2 → FR-Admin-1/2/3; Epic 3 → FR-Admin-4; Epic 4 → FR-Consumer-1/2; Epic 5 → FR-Consumer-3/4).
- All nine matrix routes exist as real page routes: `/admin/parties`, `/admin/parties/{RoutePartyId}`, `/admin/parties/{RoutePartyId}/gdpr` (`src/Hexalith.Parties.AdminPortal/Components/PartiesAdminPortal.razor`), `/admin/parties/new`, `/admin/parties/{RoutePartyId}/edit` (`CreateEditPartyPage.razor`), `/me`, `/me/edit`, `/me/consent`, `/me/privacy` (`src/Hexalith.Parties.ConsumerPortal/Components/`).
- "sprint-status.yaml marks Epics 1–5 and their stories as done" — still true today (all epic-1…epic-5 rows and every story row `done`), and "Epic 7 is completed" matches `epic-7: done` with all eight stories done.
- All five "Known completed dependency evidence" stories (1.4, 3.5, 3.6, 4.1, 4.2) are `done` in sprint-status.yaml.
- The Epic 7/8 "maintenance scope only, zero new PRD FRs" invariant is consistently enforced downstream: the Epic 8 spec requires "Add no PRD functional requirement." and the architecture spine requires "zero new PRD FRs" (asserted by `EpicEightClosureFitnessTests`). `sprint-change-proposal-2026-07-06.md` exists. No epic-9+ exists in sprint-status.yaml.
- NFR9 claims all hold: `net10.0` target, `TreatWarningsAsErrors=true` (`Directory.Build.props:38`), central package management (`Directory.Packages.props` `ManagePackageVersionsCentrally=true`), `.slnx` solutions (`Hexalith.Parties.slnx`, `Hexalith.Parties.Standalone.slnx`), `xunit.v3` in test projects, Playwright accessibility checks (`tests/e2e/specs/parties-accessibility.spec.ts`), and all eight submodules under `references/` only (`.gitmodules`).
- §Out of MVP Scope temporal deferral holds: `src/Hexalith.Parties.Mcp/Tools/PartiesMcpToolNames.cs` defines only `get_party`, `find_parties`, `create_party`, `update_party`, `delete_party` (no `get_party_name_at`), and `docs/deferred-search-and-temporal-queries.md` confirms "MVP exposes no temporal query endpoint, typed client method, MCP tool, REST route…".
- The PRD's stated purpose (readiness tooling consumes it as the PRD-shaped source) is real: readiness reports select it as the `prd` document (`implementation-readiness-report-2026-07-07.md` lines 12–13), and `epics.md`/`architecture.md` cite it as the canonical FR home. No consumer parses specific PRD section names or FR-ID strings programmatically — the only code-level dependency is the byte-for-byte freeze in `EpicEightClosureFitnessTests.cs`.
- `DocumentationFitnessTests.cs` does not reference the PRD; its route/inventory assertions target `README.md` and `docs/*` only, so PRD edits cannot break it.

# Current sample

Run `deploy/k8s/publish.ps1 -ConfirmContext <sandbox>` and validate with `deploy/validate-deployment.ps1 -Format json`.

Canonical reference: `docs/kubernetes-deployment-architecture.md`.

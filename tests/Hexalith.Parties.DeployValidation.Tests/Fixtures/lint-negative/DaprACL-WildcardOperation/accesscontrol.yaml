apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: accesscontrol-wildcard-operation
spec:
  accessControl:
    defaultAction: deny
    policies:
      - appId: parties
        defaultAction: deny
        operations:
          - name: /**
            action: allow

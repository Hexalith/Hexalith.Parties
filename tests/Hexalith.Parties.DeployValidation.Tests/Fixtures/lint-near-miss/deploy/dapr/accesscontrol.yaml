apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: accesscontrol
spec:
  accessControl:
    defaultAction: deny
    policies:
      - appId: eventstore-admin
        defaultAction: deny
        operations:
          - name: /api/v1/commands/**
            action: allow

apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor
spec:
  template:
    spec:
      containers:
        - name: vendor
          image: redis:latest
          readinessProbe:
            tcpSocket:
              port: 6379
          livenessProbe:
            tcpSocket:
              port: 6379

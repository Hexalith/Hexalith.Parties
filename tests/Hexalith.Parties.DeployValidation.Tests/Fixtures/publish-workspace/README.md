Minimal publish/teardown shim workspace fixture.

Tests may copy this tree to a temp directory, add the production scripts and command shims, and then exercise publish/teardown behavior without touching the committed deployment tree.

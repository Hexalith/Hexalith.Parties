# Project Overview — Hexalith.Parties

> Regenerated by BMAD *Document Project* (**full rescan**, deep, 2026-06-02) and reconciled for Epic 8 closure on 2026-08-18.

## Purpose

**Hexalith.Parties** is a ready-to-deploy **party-management domain service** for people and organizations — the party/contact data backbone (contact channels + identifiers) behind your own application logic. It is **not** an authentication provider, CRM, or identity server.

Public command and query traffic goes through **Hexalith.EventStore**; the `parties` service runs an EventStore SDK domain-service host behind that gateway, and .NET consumers normally use the typed client package (`Hexalith.Parties.Client`). The repo now also includes `parties-ui`, a Blazor Server browser UI/BFF that owns OIDC sign-in and hosts the Admin/Consumer UI shell.

## Executive summary

| | |
|---|---|
| **Domain** | Party records (Person / Organization), contact channels, identifiers, GDPR consent & erasure |
| **Style** | Event sourcing + CQRS + EventStore SDK domain/projection/query handlers, fronted by the EventStore gateway |
| **Repository** | Monolith - exactly 13 projects under `src` plus one sample project, 15 runnable .NET test projects plus one support host, and Playwright e2e |
| **Language / runtime** | C# / **.NET 10** (SDK `10.0.302`) |
| **Orchestration** | .NET Aspire 13.4 (`dotnet aspire run`) |
| **Eventing** | DAPR pub/sub — CloudEvents 1.0 on `{tenant}.parties.events` (Redis local; Kafka/RabbitMQ/Service Bus in prod) |
| **Integration** | Typed .NET client · `parties-ui` Blazor Server BFF · `parties-mcp` (AI tools, 5) · DAPR event subscription · Blazor Picker, AdminPortal & ConsumerPortal |
| **License** | MIT |

## Key features

- **EventStore gateway ingress** — `POST /api/v1/commands` and `/api/v1/queries` with `Domain="party"`.
- **Parties domain-service host** — owns domain execution and SDK projection/query handlers behind EventStore; deny-default DAPR access admits only `eventstore` to the exact SDK routes.
- **Typed client package** — `IPartiesCommandClient` / `IPartiesQueryClient` hide the EventStore envelope.
- **Parties UI/BFF** — `Hexalith.Parties.UI` provides the FrontComposer/FluentUI Blazor Server host, host-owned OIDC, role-gated Admin/Consumer shell, accessibility guardrails, and live freshness foundations.
- **Embeddable party picker** — Blazor / custom-element selector (`<hexalith-party-picker>`).
- **Admin portal** — FrontComposer-hosted Blazor admin records management with GDPR operations surfaces, including erasure certificate display and verification retry through the EventStore-fronted client.
- **Consumer portal** — FrontComposer-hosted `/me*` RCL for Consumer My Profile, Edit My Profile, consent, data export, erasure request/cancel, and processing-transparency views, using ConsumerPortal-owned ports adapted by the UI host to the fail-closed self-scoped accessor.
- **Separate MCP host** — `parties-mcp` exposes `create_party`, `get_party`, `find_parties`, `update_party`, `delete_party`.
- **DAPR event subscription** — downstream apps consume party events with idempotent handlers.
- **.NET Aspire** — one-command local topology (EventStore, Parties, Parties UI, Tenants, DAPR sidecars, Redis, optional Keycloak).

## Technology stack (summary)

| Category | Technology | Version |
|----------|-----------|---------|
| Runtime | .NET | net10.0 (SDK 10.0.302) |
| Orchestration | .NET Aspire | 13.4.6 |
| Actors / pub-sub | DAPR | 1.18.5 |
| Validation | FluentValidation | 12.1.1 |
| Mediation | MediatR | 14.2.0 |
| AuthN | JWT Bearer | 10.0.11 |
| MCP | ModelContextProtocol | 2.2.0 |
| UI | FluentUI Blazor + CustomElements | 5.0-rc.4-26180.1 / 10.0.11 |
| Rich search (opt) | Hexalith.Memories.Client.Rest | 2.21.3 |
| Testing | xUnit v3 / Shouldly / NSubstitute / bunit / Testcontainers | 3.2.2 / 4.3.0 / 6.2.0 / 2.9.0 / 4.14.0 |

Full table with all packages: [architecture.md §3](architecture.md). Versioning is git-tag SemVer via MinVer (prefix `v`).

## Repository structure

Single cohesive solution. **Adopter-facing** packages (`Client`, `Contracts`, `Picker`, `AdminPortal`, `ConsumerPortal`, `Mcp`) are what consumers reference directly; `Hexalith.Parties.UI` is the browser UI/BFF host for this product experience. **Internal** projects (`Hexalith.Parties` host, `Authentication`, `Projections`, `Security`, `Testing`) are private implementation or published dependency packages, not direct adopter surfaces. Story 8.4 retired the local `Hexalith.Parties.ServiceDefaults` wrapper; service-default consumers should use `Hexalith.Commons.ServiceDefaults` directly. Shared Hexalith repositories under `references/` provide build, platform, EventStore, FrontComposer, serialization, and tenancy source dependencies. Runtime deployment orchestration is externally owned; this repository publishes immutable workload images and keeps local AppHost wiring only. Annotated tree: [source-tree-analysis.md](source-tree-analysis.md).

## Important notes

- **GDPR posture:** the **crypto-shredding subsystem is implemented and enabled by default** (`Parties:CryptoShredding:IsEnabled=true`), distinct from the MVP-warning switch (`Parties:Compliance:GdprFeaturesActive`). The only key store registered by default is `LocalDevKeyStorageBackend` (in-memory, dev-only), so a production KMS or secret-store-backed key provider is required before real personal data is stored — see [architecture.md §8](architecture.md), [data-models.md §6](data-models.md), and the README notice.
- **MCP exposes 5 tools** (`get_party_name_at` does **not** exist; temporal name queries are deferred — [api-contracts.md §3](api-contracts.md)).
- Do **not** store regulated EU personal data in the MVP; use synthetic data for evaluation.

## Where to go next

- New here? → [getting-started.md](getting-started.md)
- System design → [architecture.md](architecture.md)
- Data/domain model → [data-models.md](data-models.md)
- Integrating (client/MCP/HTTP) → [api-contracts.md](api-contracts.md)
- Consuming events → [event-subscribing.md](event-subscribing.md) + [event-handler-patterns.md](event-handler-patterns.md)
- Deploying → [deployment-guide.md](deployment-guide.md)
